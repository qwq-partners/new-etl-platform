# 신규 ETL Platform PoC 시험·합격 기준서 v1

- 문서 상태: 초안 v1 (8차 — v1.2.3.1 정합: FI-50 앵커·(iv) 합격식·(v) 버전 분기 교정, 제출 프로토콜 소유권(poll 채번·최초 resubmit_no=0), canonical hash NUMBER·TIMESTAMP 실행 규격과 V-15 교정, FI-41g/h/i 분리 · 이하 7차 내용: v1.2.3 정합: FI-50 5 timing case 재작성, 이중 commit 4-불변식(allowlist 없음), canonical hash NFC→AL32UTF8 2단, G0 정식화, SC-02d 신설 · 이하 6차 내용: v1.2.2 정합: Codex 2차 리뷰 oracle 자기모순 5건(P0-10-A~E) 정정, §5.1 lane·설명된 누락·main-ancestry, §2.5/SC-04b backlog 5분해, §3.2 `STANDARD_HASH` 양방향 비교, 신규 FI 9(FI-50·51 + 파생 7)·SC-11 보강·§8.6 반영 매핑; 5차 — v1.2.1 정합: Codex 리뷰 P0-09/P0-10 판정 oracle 교체, 신규 FI 13·SC 3, 게이트 2분리). 목표 아키텍처 v1.2.3과 짝을 이루는 별도 문서
- 작성일: 2026-08-23
- 대상 아키텍처: `etl-platform-target-architecture-v1.2.3.1.md` (이하 "v1.2" — 절 번호는 v1.1~v1.2.3 동일; v1.2.1·v1.2.2·v1.2.3 신설 규범은 각각 ‘v1.2.1’·‘v1.2.2’·‘v1.2.3’로 표기). §8.4의 14건은 v1.2 변경 이력 A1~D3으로, §8.5·§8.6은 v1.2.1·v1.2.2 변경 이력으로 반영됐고 본문 시험은 v1.2 의미론 기준으로 갱신했다
- 출처: v1.1 19장(PoC 범위와 합격 기준)·20장 Phase 0/1, 리뷰 v1.1 §1.4 "② PoC에서 판정" 13개 가설, 리뷰 SCL-10(stub Source 요건)·D-1(측정 지점)·D-2(freshness SLO)·ORA-11(Oracle 측 지표)
- 역할 분담: v1.2는 **무엇을 만들지**를 확정하고, 이 문서는 **그것이 맞는지 어떻게 증명할지**와 **PoC에서만 답할 수 있는 가설**을 다룬다. v1.2 19장은 이 문서를 참조한다(판정 등급·게이트·즉시 No-Go 14건·가설 id 요약). 두 문서가 다르면 **v1.2.1 19장의 4단 — (1) v1.2 규범 문장(상태·전이·불변식·API 의미) > (2) 실행 가능한 schema·API 계약(§8.1 8번 DDL 부록, v1.2 17장) > (3) 이 문서의 절차·임계값 > (4) 양쪽의 설명 산문** 순으로 읽고, 이 문서가 v1.2를 이기지 않는다. 불일치는 어느 쪽의 승리가 아니라 **traceability failure**로 §8.2 기록에 남기고 §8.4 반영 후보로 올려 v1.2.x 개정(변경 이력)으로 해소한다 — 시험을 고쳐 설계를 통과시키지 않는다(§2.4 “컬럼·enum·쓰기 규칙은 v1.2 6.1이 권위다”와 같은 원칙). 이 문서가 v1.1에 없어 전제해야 했던 동작은 §8.4에 남기고 v1.2 반영 결과를 병기한다.

---

## 1. 목적과 판정 원칙

### 1.1 PoC가 증명해야 하는 것

1. v1.2의 실행 의미론(Occurrence/Contract/Attempt, Guard, Commit Adjudication, SourceVisibilityFence, lease)이 **장애 상황에서도** "정상 Run 중복/누락 0, **양쪽 다 채택된 이중 commit 0 ∧ 미탐지·미복구 이중 snapshot 0**(v1.2.3 19장 7번 — late-apply 착지 자체는 예방되지 않고 `DETECT_AND_REPAIR`로 닫힌다, A 13.1·13.2), Source 절대 한도 초과 0, watermark gap/regression 0"을 지킨다.
2. 10,000 Job / 40,000 Run/일 / 정시 burst 500 규모에서 Dagster OSS + Control Plane 조합이 7일간 **발산하지 않는다**(queue 깊이, DB 크기 증가율, lateness 추세).
3. 13개 가설(§4)에 대해 **측정값으로** 답을 내고, 답에 따라 v1.2에서 결정할 항목을 확정한다.

### 1.2 판정 등급

| 판정 | 조건 |
|---|---|
| **Go** | §6 즉시 No-Go 0건, §5 '확정' 행 중 **Phase 1 범위 행**(§8.3 판정 범위) 전부 충족, 가설 13건 모두 결론 도출 — §8.3 축소로 미뤄진 가설은 "미결 — v1.2 기본값 채택"으로 결론 기록 가능(H-06 미결 시 WAP 미채택, SC-04 미실시 시 병목 순위표는 SC-03 관측으로 대체). Go는 §8.3의 **세 게이트**를 **따로** 기록한다(v1.2.3 — v1.2.2의 ‘두 게이트’ 정정) — **(G0) Executable Spec Go**(§8.1 8번 — DDL·판정 SQL·canonical hash 벡터·제출 경로를 같은 `versions.lock` 아래에서 실증. G0 PASS 없이 주차 2 soak를 시작하지 않으며, G0가 무효화되면 그 시점부터 G1·G2 판정은 ‘미판정’이다), **(G1) Scale/Control Go**(stub 모집단), **(G2) Oracle ZERO_GAP Go**(실 DG 모집단 — No-Go 6번의 권위). 후자를 통과하지 못한 Source는 `SourceCapability.zero_gap_verified = false`로 남아 ZERO_GAP publish가 막히므로(v1.2.1 7.2 5번) 플랫폼 Go는 가능하되 그 Source의 ZERO_GAP 출시는 불가 |
| **Conditional Go** | No-Go 0건, 확정 기준 중 성능 행(latency·throughput)만 미달이고 원인과 개선 계획이 식별됨. 단 **ρ < 0.7인 Source의 Job에서 H-12 freshness 미달**이면 보호 지연이 아닌 플랫폼 원인이므로 Conditional Go 불가 — 원인 해결 후 재시험 |
| **No-Go** | §6 항목 1건 이상 재현. 원인 해결 후 해당 시험을 재실행해 재평가 |

"가설 기준" 행(§5에서 `가설`로 표시)은 합격/불합격이 아니라 **결정 입력**이다. 미달이어도 No-Go가 아니며, 결과가 v1.2의 설계 선택(대안 채택 여부)을 정한다. "Phase 2"로 표시된 확정 행은 Phase 1 판정에서 제외하고 Phase 2 진입 게이트(Phase 2 구현 후·24 Job shadow 비교 시작 전 — Phase 1의 실 DR 정합성 시험은 이 게이트 이전)에서 판정한다.

### 1.3 원칙

- 생산 장비 DB에 burst를 쏘지 않는다. 규모·장애 시험은 stub Source, 정합성 시험은 DBA가 승인한 DR read-only 시간대에서 24개 실 Job으로만 한다(v1.2 20장 Phase 2 전제와 동일).
- 모든 시험은 **기대 동작을 v1.2 절 번호로 지정**하고, 합격 조건은 Control DB·Iceberg metadata·Oracle/stub 뷰에서 **쿼리로 판정 가능한** 형태여야 한다. "정상 동작 확인" 같은 서술적 기준은 쓰지 않는다.
- **0건 조건은 양성 증거와 짝을 이룬다.** "Hold 중 SA 제출 0", "primary 접속 0" 같은 조건은 같은 구간에 제출 시도·거부 기록(`guard_result` ≥ 1, `SKIPPED_BY_HOLD` ≥ 1, stub 접속 로그 ≥ 1 등)이 함께 있어야 합격이다. 시도가 없어 0인 것은 합격이 아니다.
- 한 시험이 여러 No-Go 항목을 동시에 검증하면 §3.4 표의 "No-Go 연결" 열에 모두 적는다. §6의 "관련 시험" 열은 이 열에서 파생한다.
- 결과는 §8.2 양식으로 기록하고, 실패한 시험은 재현 절차·로그·contract_id를 남긴다.

---

## 2. 전제와 환경

### 2.1 Phase 0 baseline 입력 (PoC 착수 전 확보)

| 입력 | 출처 | 쓰이는 곳 |
|---|---|---|
| 대표 24 Job의 Source 부하(CPU·Read IO·세션 수), 처리량, 수행 시간 분포 D(median·p95), **실패율·재시도 횟수·재시도 원인 분포** | 기존 Airflow 실측 + DBA | §2.3 stub 지연 모델, §4 H-11 용량 모델, §5 Oracle 행, §7 `max_auto_attempts`·`adjudication_pending_alert_after` |
| Source 수, Source별 Job 수·주기 분포, DBA 승인 동시성 C(세션 수) | HAflow 메타데이터 + DBA | §2.3, §4 H-11, §3.3 규모 시나리오 |
| 테이블별 크기(Full 상위 10% 포함), 주기당 실제 변경 row 비율 | Oracle dictionary + 샘플 측정 | §4 H-08 용량, Full→Merge 전환 후보 |
| Source capability: Flashback Query 가능 여부·`undo_retention_seconds`·`retention_guarantee`, Data Guard lag 신호 종류, **`max_commit_minus_watermark_seconds` + `bound_kind`(ENFORCED면 DB 장치 근거 첨부, 아니면 OBSERVED)**, 대표 24 Job watermark 컬럼의 `timestamp_origin`·`not_null`·`updated_on_every_change`(v1.2.1 6.1 `watermark_column_facts`), hard delete 여부, NLS_CHARACTERSET·`NLS_NCHAR_CHARACTERSET`·DB 시간대(v1.2.3 — §3.2 canonical hash STRING 경로의 `TO_NCHAR` 필수 여부와 `LENGTHB` 가드 임계값을 결정하는 입력), `sqlnet_expire_time_seconds`·`idle_time_seconds`, `legacy_concurrent_sessions` | DBA | §4 H-03, §3.1 cutoff 선택·ZERO_GAP 대상 선정(ENFORCED Source만), §3.4 FI-41·FI-43·FI-44, §7 파라미터, §3.2 미비교 규칙 |
| **Source charset·문자열 상한 2항**(v1.2.3 신설 — §3.2 canonical hash의 전제): (i) `NLS_CHARACTERSET`·`NLS_NCHAR_CHARACTERSET`(`NLS_DATABASE_PARAMETERS`) — 비-Unicode DB charset이면 canonical STRING 경로에서 `TO_NCHAR` 경유가 **필수**임을 확인(비-Unicode 입력에서 `DECOMPOSE`/`COMPOSE`는 무동작), (ii) `MAX_STRING_SIZE`(`V$PARAMETER`) — 규격은 `STANDARD`(SQL RAW 2000B)를 가정하며 `EXTENDED` 전환은 비가역이고 뷰 무효화·함수기반 인덱스 UNUSABLE 부작용이 있어 **운영 standby에 요구하지 않는다**; 소스별 값은 `SourceCapability.max_string_size`로 등록한다 | DBA (v1.2.3 22장) | §3.2 canonical hash(`LENGTHB` 사전 가드 임계값·`0x05 LOB_DIGEST` 강제 조건), §8.1 G0-3 `oracle_env{nls_characterset, nls_nchar_characterset, max_string_size}`(v1.2.3.1 — NCHAR charset 추가), G0 무효화 규칙(세 값 중 하나라도 변경되면 `g0_pass := false` — §8.1 무효화 (5)(6)(8)) |
| 세션 가시 SCN의 출처와 ADG에서의 의미(`DBMS_FLASHBACK.GET_SYSTEM_CHANGE_NUMBER` vs `V$RECOVERY_PROGRESS`), Source 모니터 세션용 DBA 승인 뷰 목록 — v1.2.2: **`V$CONTAINERS`(또는 `V$PDBS`) SELECT 권한** 포함(`db_identity` PDB tuple `pdb_dbid`·`pdb_con_uid`·`pdb_guid`를 `WHERE con_id = TO_NUMBER(SYS_CONTEXT('USERENV','CON_ID'))`로 읽는 6.1·11.2·11.3 조회; non-CDB는 `CON_ID = 0` → `NOT_APPLICABLE`), **fence 시각 witness**(v1.2.2 11.3 `fence_time_witness ∈ {HEARTBEAT_TABLE, SCN_TO_TIMESTAMP}` — 신설): DBA 소유 `ETL_HEARTBEAT(ts)` 1행의 소유 schema·갱신 주체(primary `DBMS_SCHEDULER`)·주기(3~5초)·standby에서의 가시성 확인 — 없으면 `SCN_TO_TIMESTAMP`만이라 그 Source의 ZERO_GAP publish 불가(BEST_EFFORT) | DBA (v1.2.2 22장 3·9번) | §3.2 정합성 기준, §4 H-03, §3.4 FI-42e·FI-41e, §8.3 G2 |
| **`versions.lock`**(v1.2.1 20장 Phase 0 산출물): Dagster·dagster-k8s·Python·PostgreSQL·Kubernetes·Cilium·Spark·Iceberg·Polaris·Spark Operator(제출 경로 submitter vs controller·replica/worker 값·SparkApplication CRD schema digest)·AIStor S3 호환 수준·Oracle JDBC의 정확한 버전 + 컨테이너 image digest + 기능 probe 결과(13.1 commit-wrapper `snapshot-property.*`/`CommitMetadata.withCommitProperties` 전파, Iceberg `isolation-level`·`validate-from-snapshot-id`, `write.wap.enabled`·`fast_forward`, Polaris `loadTable`·`GET /v1/config`·`Idempotency-Key`, Dagster `run_status_sensor`·`max_tick_retries`·`run_monitoring` 키 이름(5.1), `sessionInitStatement` 동작, **v1.2.3 canonical hash probe 3항**: Spark `normalize(str, 'NFC')` built-in의 4.0~4.2 함수 registry 부재 확인과 이를 대체하는 `java.text.Normalizer` 결정론 UDF의 소스 digest, `STANDARD_HASH`의 RAW 인자 허용 여부, `UTL_I18N`·`UTL_RAW`·`DBMS_CRYPTO`의 SQL 직접 호출 가능 여부 — 셋 다 §8.1 G0-3에서 실증하고 결과를 `canonical_hash_spec_digest`의 입력으로 쓴다) | 플랫폼팀 (22장 1번) | 모든 시험의 재현 조건(§2.2 환경 고정 — 모든 시험은 이 lock으로 빌드, §8.2 ‘실행 환경’에 lock digest 기재), §4 H-06, H-10, 13.1 commit-wrapper 검증, §7 dagster 값 |
| ETL 전용 계정 profile(`FAILED_LOGIN_ATTEMPTS`, `SESSIONS_PER_USER` 등), Resource Manager의 ADG 적용 가능 여부; **DB hard cap 증거 4항**(v1.2.2 11.2 — SourceCapability 등록 증거, 없으면 `pool_cap`만으로는 11.1 'DB-enforced hard limit' 겹이 없는 것으로 기록): (i) `RESOURCE_LIMIT = TRUE`(`V$PARAMETER` — FALSE면 `SESSIONS_PER_USER`·`IDLE_TIME` 모두 비집행), (ii) ETL 계정에 실제 적용된 `SESSIONS_PER_USER` 값(`DBA_PROFILES`·`DBA_USERS.PROFILE` — `pool_cap` < 이 값), (iii) 적용 범위(PDB 단위 / RAC standby instance별 집계 — `ADG_ACCOUNT_INFO_TRACKING`과 같은 단위), (iv) cap+1 양성 대조 — DBA 입회 하에 세션 cap+1 접속으로 ORA-02391 1회 재현(§3.2 'ORA-02391 0' 조건의 양성 증거). **이 대조는 soak 집계 창 밖에서, 플랫폼이 아닌 DBA 호스트·DBA 세션으로 수행하고 수행 시각(`session_limit_evidence.cap_plus_one_verified_at`)과 클라이언트 식별(`MACHINE`·`OSUSER`·접속 계정)을 §8.2에 기록한다**(v1.2.3 — 그렇지 않으면 이 양성 증거가 §5·§6의 'ORA-02391 0'을 스스로 위반한다); **`PASSWORD_ROLLOVER_TIME`**(v1.2.2 6.2 — gradual rollover 설정 여부·값: `≥ max_runtime_seconds`로 등록된 Source만 credential 상한식에서 Job 수 기준 완화, 그 외는 최대 총 JDBC connection weight 항) | DBA (22장 9번) | §3.4 FI-16, FI-17, FI-43c, §3.3 SC-05, §5 Source 절대 한도 행, §7 `credential_breaker_failures` 행, §3.2 'ORA-02391 0' 지표의 전제 |
| **`bound_evidence` 5항**(v1.2.3 — `mechanism ∈ {SYNC_COMMIT_GUARD, TXN_AGE_KILL_JOB, OTHER}`; **ENFORCED 근거는 `SYNC_COMMIT_GUARD`뿐이고 kill job 등록은 overlap 보수화·감사용이다**, A 11.3)·`threshold_seconds`·`repeat_interval_seconds`(= `P_kill`)·`kill_latency_bound_seconds`(= `(Q+L)ₘₐₓ,ₒᵦₛ` 실측)·`verified_at`, 그리고 C1 확인값 `DBA_2PC_PENDING` 상시 0·dblink/XA 미사용, C2 확인값 `JOB_QUEUE_PROCESSES`·PDB Scheduler 활성·kill job 마지막 성공 실행 시각 | DBA (v1.2.3 22장 4번) | §3.1 ZERO_GAP 대상 선정, FI-41g, §5 overlap 부등식(A 7.2 13번), §8.3 G2 |
| **Data Guard 시험 인스턴스**(primary → physical standby, READ ONLY WITH APPLY; DBA 운영·장애 주입 권한 포함 — long transaction·late commit·switchover·undo churn·세션 kill 가능, 생산 DR과 분리) | DBA (22장 2·3번) | §8.3 'Oracle ZERO_GAP Go'(G2), §3.5 6번 실 Oracle 회차(FI-12·13·41·44·45 절차), §5.1 (4) `AS OF SCN` truth. 확보 불가 시 G2 "미통과"로 기록 |

### 2.2 시험 환경

- Kubernetes 클러스터: 운영 목표와 같은 노드 유형. burst 500 × (Run Pod + driver + executor) 동시 수용 용량을 §4 H-10 측정 전에는 넉넉히 잡고, 측정 후 축소 시험을 1회 더 한다.
- Dagster OSS: Webserver 2(read-only 1 + write 1), Daemon 1, Code Location 8~16 shard(§4 H-13과 함께 결정), PostgreSQL HA. dagster.yaml은 v1.2.1 5.1 기준값(`run_retries.max_retries: 1`·`max_resume_run_attempts: 0`·`poll_interval_seconds: 60` 포함 — §7 dagster 행과 동일).
- Control Plane: Java Modular Monolith + PostgreSQL HA(5.4). Outbox → Kafka(시험용 토픽). §2.4의 PoC 계측 테이블 포함.
- Polaris + AIStor: 운영과 같은 버전. 시험 namespace 분리.
- Source:
  - **stub Source**(규모·장애 시험): §2.3 요건을 만족하는 Oracle 호환 stub(또는 실제 Oracle 시험 인스턴스 + 프록시).
  - **실 DR read-only**(정합성 시험): 24개 실 Job. DBA 승인 시간대, 동시성 C 이내, Airflow와 이중 읽기 금지(extract-once staging 공유 또는 순차 시간대).
- 관측: Prometheus/Grafana, OpenSearch, Spark History Server, Control DB 직접 쿼리 권한(판정 쿼리용 read-only), K8s 감사 로그(SparkApplication 생성 시각).

**구현 범위(Phase 1에 앞당겨 구현하는 Phase 2 항목)** — v1.2 20장 Phase 1 산출물만으로는 실행할 수 없는 시험을 위해(v1.2 20장 Phase 1이 이 표를 인용) 다음을 최소 구현으로 Phase 1에 포함한다. 포함하지 않기로 하면 해당 시험은 §8.3 규칙대로 Phase 2 게이트로 이관한다.

| 항목 | 최소 구현 | 의존 시험 |
|---|---|---|
| Data Reconciliation Audit(12.3) | PK+UPDATE_DT 비교 → repair REPLAY 생성, UI 없음 | FI-12(b), §3.1 BEST_EFFORT, §5 BEST_EFFORT 행 |
| extract-once 경로(11.5) | Append-large 2개에만 적용 | FI-28 |
| CredentialRevision 상태 전이(6.2) — ACTIVE 전이의 credential breaker 닫기·Hold 자동 해제 포함 | API만, Wizard 없음 | FI-16, FI-33 |
| ConnectionRevision revoke API(`…/connection-revisions/{rid}/revoke {reason, force}`, 6.2) | API만 | FI-27 |
| `retry`·`abort`·`resolve`·`dq:accept`·`accept-empty`·`cancel`·`watermark:seed` API | API만 | FI-10 변형, FI-21(accept-empty까지), FI-22, FI-25, FI-36 — FI-23은 `DQ_FAILED` 전이·검사 1·5가 v1.1 20장 Phase 2 'MVP DQ' 산출물이라 Phase 2 게이트로 둔다 |
| repair REPLAY(14.3)·단일 Job BackfillPlan start/pause/resume/cancel(14.4) | API만 | FI-20, FI-36, §3.1 닫힌 구간 backfill |
| Control 측 lateness sensor(16.4 `freshness breach`, `expected occurrence missing`) | PLANNED stale 루프와 같은 스케줄러, `planned_scan_interval`(60초) 주기 읽기 전용 검사, Outbox unique key dedup(v1.2 16.4) | §5 freshness breach 행, FI-38 |

**시험 훅(PoC 빌드 한정, 운영 이미지에서 제거)**: Run Pod `FAULT_KILL_AFTER_RECEIPT=<n>`(chunk n receipt 수신 직후 `chunks/{n}:commit` 호출 전 SIGKILL), `FAULT_ZOMBIE_CALLS=1`, `FAULT_DELAY_FINALIZE=<s>`, `FAULT_HOLD_PROCEED=<n>`(chunk n receipt 수신 후 `proceed` 발신만 차단, Run Pod 생존), `FAULT_DELAY_ATTEMPT_FAILURE=<s>`(Run Pod가 FAILURE로 exit한 뒤 s초 후 `attempt-failure` 호출 — FI-09c), `FAULT_HOLD_FINALIZE=1`(`stop` 후 SA terminal을 확인하고도 `finalize` 호출을 보류 — FI-23c 대조군); Control API `FAULT_KILL_AFTER_POLL_COMMIT=1`(v1.2.3.1 신설 — `submissions:poll`이 `resubmit_no` 채번 + `submission_in_flight` set을 commit한 직후·응답 반환 전 프로세스 SIGKILL, FI-35(e)); driver `FAULT_EXIT_AFTER_COMMIT=<n>`(chunk n commit 성공 직후 receipt 전송 전 exit 137), `FAULT_EXIT_BEFORE_WRITE`(쓰기 전 예외); Polaris 앞 프록시 `FAULT_COMMIT_RESPONSE={forward_then_500 | drop_then_500 | forward_then_504 | strip_etl_keys | hold_then_apply}` — 이 중 **`hold_then_apply(release_anchor)`**(v1.2.3.1 명시 — FI-50·SC-11 (e)가 이미 인용하고 있으나 훅 목록에 없던 값): client에는 즉시 504를 돌려주되 그 `updateTable` 요청을 **보관**했다가 `release_anchor` 시점에 Polaris로 전달하고, 도착·전달 시각과 **전달 응답의 상태·본문**(200 또는 409)을 로그에 남긴다. 앵커는 **클라이언트 신원**을 **attempt 단위**로 분류해(Run Pod IP — Template이 REST catalog `header.*`로 붙인 `X-Contract-Id` 헤더만으로는 **같은 계약의 attempt 1·2를 가르지 못하므로** 프록시 로그에 Run Pod IP를 반드시 남긴다, v1.2.3.1) 그 client의 **카탈로그 호출**로만 정의한다(프록시는 Control API를 보지 않으므로 Guard·`chunks:begin` 같은 Control 호출을 앵커로 쓸 수 없다). 지원 값: `before_first_load_table(client)`(그 client의 첫 `loadTable`이 도착하면 보관분을 먼저 전달하고 그 응답을 받은 뒤 `loadTable`을 통과시킨다 — FI-50 (i)), `after_load_table_gap(client, t_gap)`(그 client의 마지막 `loadTable`로부터 `t_gap` 경과 — (ii)), `before_update_table(client)`(그 client의 `updateTable`이 도착하면 붙잡아 둔 채 보관분을 먼저 전달 — (iii)), `after_update_table_ok(client)`(그 client의 `updateTable`이 200으로 끝난 뒤 — (iv)). 프록시는 어떤 변형에서도 429·`Retry-After` 503을 반환하지 않는다(A 13.2 운영 제약 — 하네스 불변식); Dagster `run_status_sensor` 일시 정지 API; Adapter launch 실패율 주입.

### 2.3 stub Source 요건

stub이 "즉시 응답하는 빈 테이블"이면 lease·한도 초과·Polaris commit 경합·AIStor IOPS가 측정되지 않고, 데이터 가시성이 SCN과 연동되지 않으면 lag·late-commit 시험이 항상 통과한다(trivial pass). 다음을 충족해야 규모·장애 시험이 의미를 가진다.

| 요건 | 목적 |
|---|---|
| 동시 connection 카운터와 Source별 상한 초과 시 거부(ORA-00018 유사 오류), 카운터 조회 API | "Source 절대 한도 단 한 번도 초과 금지"(§6) 검출 |
| Phase 0 D 분포(lognormal, median·p95)로 쿼리 지연 주입, Source별·테이블별 파라미터화 | Run Pod 동시 점유·lease 대기·K8s churn 재현 |
| 테이블별 row 수·크기 분포로 **실제 데이터 생성** — Spark가 실제 Iceberg commit을 수행하게 함 | Polaris commit 처리량·AIStor IOPS·snapshot 용량 측정 |
| Source 30개 × C 한도 실제 값 재현 | H-11 용량 모델 검산 |
| **SCN 기반 가시성**: 모든 row에 `commit_scn`·`commit_ts`를 부여하고(commit 순 = SCN 순), 세션이 보는 SCN(`DBMS_FLASHBACK.GET_SYSTEM_CHANGE_NUMBER`)과 `AS OF SCN :x`가 `commit_scn ≤ x`인 row만 반환. **system tick(v1.2.2 정정)**: stub은 table commit과 무관하게 3초마다 system tick(`tick_scn`·`tick_ts`, row 없음 — 실 Oracle의 SMON_SCN_TIME 전진·Data Guard heartbeat redo 모사)을 같은 commit 스트림에 넣는다. apply lag 주입은 **시간 커서** 모델이다 — `visible_scn` = `ts ≤ stub_now − injected_lag`(table commit은 `commit_ts`, tick은 `tick_ts`)인 **마지막 항목(tick 포함)**의 SCN(contiguous prefix; lag는 시간이고 SCN은 순번이므로 SCN에서 lag를 감산하지 않는다), `SCN_TO_TIMESTAMP(visible_scn)` = 그 항목의 `ts`(3초 granularity로 내림), `V$DATAGUARD_STATS`의 apply lag·`DATUM_TIME`은 같은 커서에서 파생(apply lag = `stub_now − ts(visible_scn)` — idle에서도 ≈ `injected_lag`이며 source idleness와 apply lag는 다른 값이다, `DATUM_TIME` = 커서 갱신 시각). 커서 이후(`ts > stub_now − injected_lag`) 항목은 어떤 세션에도 노출되지 않는다. **idle 모드**(수 분간 **table commit 0** — system tick은 계속 들어오므로 커서는 tick을 따라 전진)와 **burst 모드**(초당 수천 commit)를 모두 지원하고 주입 API 응답에 현재 커서(`visible_scn`, `ts`, `tick_count` — 신설)를 돌려준다 | FI-10·FI-11·FI-26·H-03이 "누락이 가능한 환경"에서 측정되게 함 — 이 행이 없으면 해당 시험은 무효. 모델 자체는 FI-40이 자가검증한다(§3.5 1번 — idle 회차에서 apply lag가 idle 시간으로 읽히면 stub 결함) |
| **late-commit 주입**: `UPDATE_DT = :past`인 row를 `commit_at = :future`에 commit(`commit_scn`은 commit_at 시점 값). Source가 `bound_kind = ENFORCED`로 등록된 경우 stub 트리거 에뮬레이션이 `commit_at − UPDATE_DT > max_commit_minus_watermark_seconds`인 commit을 거부하고 주입 API가 `REJECTED_BY_BOUND`를 응답한다(OBSERVED Source는 허용) | FI-12, FI-41, FI-47, H-03 |
| **heartbeat witness 에뮬레이션**(v1.2.2 11.3): `ETL_HEARTBEAT(ts)` 1행을 stub 시계(`stub_now`)로 3~5초마다 갱신·commit(`commit_scn` 부여 — 커서 모델대로 `commit_scn ≤ visible_scn`일 때만 `AS OF SCN :visible_scn`에 보이며 값 = 그 commit 시점의 `stub_now`, 따라서 보이는 `ts ≤ commit_ts(visible_scn)` 불변); 주입 API로 heartbeat 갱신 정지·지연(`heartbeat_delay_seconds`)과 `ETL_HEARTBEAT` 조회 오류(권한 없음 에뮬레이션) 가능; `SourceCapability.fence_time_witness`를 `HEARTBEAT_TABLE`·`SCN_TO_TIMESTAMP`로 등록한 Source 각 ≥ 1 | FI-40(`T_lb ≤ commit_ts(visible_scn)` 자가검증), FI-41(e), §3.1 ZERO_GAP 대상 선정(`HEARTBEAT_TABLE` Source만) |
| `V$DATABASE`(DATABASE_ROLE/OPEN_MODE), `V$DATAGUARD_STATS`(apply lag·DATUM_TIME), `GV$SESSION`(MODULE/ACTION/CLIENT_IDENTIFIER), `ALL_TAB_COLUMNS`를 **조작 가능한** 뷰로 제공 | Source 모니터 세션(11.2)과 fence(11.3)·역할 검증·schema drift 시험 |
| 장애 주입 API: apply lag 값 변경(Guard 시점·**실행 중** 모두), DATUM_TIME stale, role 전환(PRIMARY/PHYSICAL STANDBY, "N번째 이후 세션부터" 조건 지원), 인증 실패(ORA-01017), 세션 강제 종료, 응답 지연/중단, 모니터 세션 kill, undo 만료(`undo_expire_before_scn = :scn` — 이후 `AS OF SCN ≤ :scn` 조회는 ORA-01555), 테이블 DDL 주입(이후 `AS OF` 조회 ORA-01466) | §3.4 FI-10~FI-13, FI-16, FI-26, FI-44 |
| **역할별 2 endpoint**: standby 리스너·primary 리스너를 따로 두고 시험 descriptor의 ADDRESS_LIST에 **둘 다** 포함(FAILOVER=ON). 접속·쿼리 로그에 endpoint·role·SQL 텍스트·MODULE/ACTION 기록 | FI-13·FI-26 음성 대조군 |
| **identity 상이 endpoint**: `db_identity` tuple 6항(`cdb_dbid`·`db_unique_name`·`resetlogs_change_no`·`pdb_dbid`·`pdb_con_uid`·`pdb_guid`, v1.2.2 6.1) 중 (B) `cdb_dbid`·`db_unique_name`이 다른 세 번째 리스너와 (C) 같은 CDB(`cdb_dbid`·`db_unique_name`·`resetlogs_change_no` 동일)·다른 PDB(`pdb_dbid`·`pdb_con_uid`·`pdb_guid`·`CON_NAME` 상이, 같은 schema/table 존재) 네 번째 리스너(둘 다 role은 PHYSICAL STANDBY), `V$DATABASE`·`V$CONTAINERS`·`SYS_CONTEXT` 응답 조작 가능(non-CDB 에뮬레이션 `CON_ID = 0` 포함); 세션 수명 로그에 `db_identity` tuple 열(6항 + `con_name`) | FI-42, FI-45(c), FI-27(a) |
| **세션 수명 로그**: connect/disconnect 시각·MODULE/ACTION/CLIENT_IDENTIFIER·종료 사유를 이벤트로 기록; 클라이언트 TCP 단절 시 세션 종료 지연을 `stub_session_linger_seconds`로 구성 가능(**기본 off** = 세션이 `SQLNET.EXPIRE_TIME` 에뮬레이션 값 또는 세션 강제 종료 API까지 잔존 — Oracle 기본 `EXPIRE_TIME=0`과 같은 의미; FI-17은 linger 0, FI-43은 900으로 실행). v1 stub의 ‘≤ 5초 종료’는 Oracle 속성이 아니라 stub 속성이었으므로 합격 조건에서 제외(v1.2.1) | FI-17, FI-43, §3.2 Run당 세션 수, H-09 |
| **계정 프로파일 에뮬레이션**: Phase 0의 `FAILED_LOGIN_ATTEMPTS` 값으로 실패 로그인 카운트·ORA-28000 잠금 재현, 계정별 실패 카운터 조회 API; `PASSWORD_GRACE_TIME` 경고 재현(ORA-28002 = **로그인 성공 + `SQLWarning`**, 실패 카운트 비증가); `ADG_ACCOUNT_INFO_TRACKING` LOCAL/GLOBAL 모드(LOCAL은 instance별 카운터) | FI-16 |
| **`CLIENT_IDENTIFIER` 무절단 반환**(v1.2.3): `GV$SESSION` 조회가 `{contract_id}/a{attempt_no}`(≤ 48B)를 절단 없이 돌려주고 64B 초과분은 Oracle과 같게 절단한다 | A 11.2 회계 join key의 유일성 전제, FI-17(c)·FI-43c(b) |
| **`V$TRANSACTION`/`GV$TRANSACTION`·`DBA_2PC_PENDING` 에뮬레이션**(v1.2.3): 열린 트랜잭션의 `START_TIME`(초 절삭)·`instance_id` 조회와 in-doubt 0 확인, 장기 트랜잭션 kill job 동작(`repeat_interval`·kill 지연 주입) | A 11.3 C1~C4, FI-41g, §8.3 G2 |
| **`NLS_CHARACTERSET`·`NLS_NCHAR_CHARACTERSET`·`MAX_STRING_SIZE` 조회**(v1.2.3): canonical hash 규격이 가정하는 값(§3.2)과 `g0_evidence.oracle_env` 기록의 입력 | §3.2, §8.1 8번 G0-3 |
| **ORA-02391 클라이언트 식별**(v1.2.3): 실패 로그온에도 `MACHINE`·`OSUSER`(또는 동등 식별자)를 남겨 플랫폼 세션과 DBA 점검 세션을 분리 집계할 수 있게 한다 | §5·§6의 ORA-02391 모집단 분리, §2.1 (iv) 양성 대조 |
| 실행계획 제공(`DBMS_XPLAN` 호환) — 실 DR 시험에서만 필요, stub은 literal 형변환 경고 로그로 대체 | §3.1 #6 |
| **`zero_gap_verified` 시험 fixture**: stub Source는 PoC 빌드 한정 플래그로 `SourceCapability.zero_gap_verified = true`를 등록하되, **같은 트랜잭션에서 A 6.1 형식의 `zero_gap_evidence{g0_report_id = 그 시점 G0 보고서 id, g2_report_id, verified_at, db_identity, capability_digest}`를 함께 기록한다** — 이 기록이 없으면 A 6.1의 무효화 전파 조회(`zero_gap_evidence.g0_report_id`)가 대상 Source를 하나도 찾지 못해 FI-41i가 공집합으로 합격한다. `zero_gap_verified = true` stub Source는 **같은 `g0_report_id`에 의존하는 것으로 2개 이상** 둔다(운영 경로 아님 — v1.2.1 6.1 ‘stub 결과로 설정 불가’의 명시 예외, §8.2 ‘실행 환경’에 기재). 대조군으로 `zero_gap_verified = false` stub Source 1개를 두어 ZERO_GAP publish가 `422 VALIDATION_FAILED` rule `ZERO_GAP_REQUIRES_VERIFIED_SOURCE`로 거부됨을 FI-41(b)(c)와 같은 회차에 1건 확인한다 | FI-10·12·40·41·47, §3.1 #3 ZERO_GAP Job publish 전제; G2(§8.3) 권위 유지 |

### 2.4 측정 지점과 계측 전제

**PoC 계측 전제(Control DB)** — v1.1 6.1 데이터 모델은 현재 상태 컬럼 + AuditEvent뿐이라 "전이 n건", "재결합 1회", "RECLAIMED 전 재부여 0" 같은 조건을 판정할 수 없었고, v1.2 6.1이 아래 테이블을 운영 1급으로 채택했다. 다음을 Control DB에 둔다 — v1.2 6.1이 운영 1급 테이블로 채택했으며 컬럼·enum·쓰기 규칙(상태 UPDATE 트리거, append-only)은 v1.2 6.1이 권위다.

| 테이블 | 컬럼 | 쓰는 곳 |
|---|---|---|
| `contract_state_history` | contract_id, attempt_no\|null, from_state, to_state, reason, actor(GUARD / RUNPOD / ADJUDICATION / SENSOR / OPERATOR / STALE_LOOP / HOLD_RELEASE / RELEASE_CHECK / EXPIRY / GAP_RECOVERY / RESYNC), dagster_run_id\|null, at | 모든 FI의 "전이 n건" 조건 |
| `attempt_state_history` | attempt_id, from_state, to_state, reason, verdict\|null, bound_dagster_run_id, rebind_count, last_observed_sa_status\|null, terminal_ingested_at\|null, at — 재결합은 `from_state = to_state`·reason `REATTACH` row(`bound_dagster_run_id` UPDATE 트리거가 생성, v1.2.1 6.1); `WRITER_FENCED` 판정(FI-22 등)은 이 테이블의 `FENCED` 전이 row로 한다(`contract_state_history`에는 없음) | FI-06~09 재결합·fencing, FI-22 |
| `lease_state_history` | lease_id, kind(SOURCE/TARGET), lease_type, from_state, to_state(GRANTED / EXPIRING / RECLAIMED / RELEASED), token_weight, granted_to_contract, attempt_no, at | FI-17, §6 lease 행 |
| `guard_result` | contract_id, dagster_run_id, attempt_no\|null, result(OK / FINALIZED_NO_DATA / 10.2 거부 사유), contract_state_after, next_eligible_at\|null, run_started_at, lease_grant\|null(v1.2.3 A 6.1 — `stage ∈ {SOURCE_TOKEN, TARGET_LEASE}` 판별자를 싣는 **분기형**: `SOURCE_TOKEN`은 `{observed, reserved_unrealized, requested_weight, pool_cap, fresh, stage}`, `TARGET_LEASE`는 `{table_id, requested_type, conflicting_lease_id\|null, conflict_rule, stage}`이며 Guard 8번에 닿지 못한 거부(1~7번)는 전체가 null. §6 9번의 grant 식 평가값이 어느 단계의 것인지와 FI-51의 충돌 상대 특정이 이 판별자에 달려 있다), at — Guard 호출마다 1 row(savepoint 롤백 뒤에도 commit) | 거부 횟수·보호 지연·0건 조건의 양성 증거 |
| `attempt_timeline` | attempt_id, t0_queued_at, t1_launch_at, t2_pod_started_at, t3_guard_ok_at, t4_sa_created_at, t5_driver_running_at, t6_first_receipt_at, t7_finalized_at, resubmit_t0[] — t0~t2는 Run Pod가 `guard` 본문 `run_stats`에 자기 Dagster run의 enqueued/launch/start 시각을 실어 Guard 트랜잭션이 기록하고(거부된 run은 `guard_result.run_started_at`만 남음), t7은 `contract.finalized_at`의 복사이며, t4·t6은 Run Pod가 `chunks/{1}:commit` 본문에 실어 보내며(chunk 1의 `chunks:begin`은 SA 생성 전 호출이라 t4를 실을 수 없음; t4 보조 출처 = K8s 감사 로그의 SparkApplication 생성 시각), t5는 SA watch 콜백이 기록 | §2.4·§5 지연 행 |

`CommitEvidenceLedger` 컬럼(§5.1 판정 쿼리가 참조, v1.2.1 13.1): `contract_id, attempt_no, chunk_no, expected_chunk_count, base_snapshot_id, committed_snapshot_id, sequence_number, summary, metadata_location, dq_result, confidence, cas_applied, cas_at, window_low, window_high, actor(RUNPOD / ADJUDICATION / RESYNC / OPERATOR — `dq:accept`의 CAS row는 OPERATOR, v1.2.1 13.1; `contract_state_history`의 actor 집합과 별개), extracted_rows, written_rows, dedup_dropped_rows, anti_join_dropped_rows, overlap_recovered_rows, extract_window_low(v1.2.1 13.1 — FI-12·FI-41·FI-47·§3.2 비교 A 입력), dq_basis(v1.2.2 13.1 — Merge 검사 1 근거 `ENGINE_METRIC | APP_COUNTER`, receipt 복사, **Merge 외 NULL**; FI-23 Phase 2 게이트 입력), published_main_snapshot_id(v1.2.2 13.1 — WAP 경로에서 `fast_forward` 성공 뒤 main head = `committed_snapshot_id`, 그 외 NULL; §3.2 비교 A main-ancestry·H-06 변형 입력)`. **열 순서는 A 13.1과 같고 `dq_basis` 선언은 한 번뿐이다**(v1.2.3 — 6차 패치가 만든 중복 선언을 삭제한다). §5.1의 gap/regression 쿼리는 `cas_applied = true` row만 잇고, FI-06·FI-15의 "ledger row actor" 조건은 이 `actor` 컬럼이다. **DDL 부록**(v1.2.1 6.1 구현 규약의 5종 이력·계측 테이블 + ledger + contract/attempt 핵심 컬럼 + 제약 10종(v1.2.2 (10) operation 단일성 포함))을 주차 1에 확정하고 §5.1 판정 쿼리 전부를 그 DDL 위에서 dry-run(빈 테이블 + 합성 row로 EXPLAIN·실행)한 결과가 주차 1 산출물이다(§8.1 8번, §8.3) — 존재하지 않는 열을 참조하는 판정 쿼리가 PoC 착수 후 발견되는 일을 막는다.

측정 지점:

| 지점 | 정의 | 출처 |
|---|---|---|
| `t_logical` | occurrence의 `logical_scheduled_at` | Control |
| `t0` | 해당 contract에 **처음** 바인딩된 Dagster run의 QUEUED 시각(재결합·재제출 run은 `resubmit_t0[]`) | Run Pod `guard` 본문 `run_stats` → `attempt_timeline` |
| `t1` | daemon `launch_run` 호출 | Run Pod `guard` 본문 `run_stats` |
| `t2` | Run Pod STARTED(정의 import 완료) | Run Pod `guard` 본문 `run_stats` |
| `t3` | Guard 성공(`ATTEMPT_ACTIVE` 진입) — 거부 시 `guard_result`에 사유와 `next_eligible_at` | Control |
| `t4` | SparkApplication CR 생성 | Run Pod(`chunks/{1}:commit`) / K8s 감사 로그 |
| `t5` | driver RUNNING | SA watch |
| `t6` | 첫 `chunk_receipt` 수신 | Run Pod |
| `t7` | `contract.finalized_at` — `FINALIZED`·`FINALIZED_NO_DATA`·`RESOLVED` 진입 시각(`attempt_timeline.t7_finalized_at`은 복사; `COALESCED_INTO(c)` occurrence는 c의 값). lateness의 권위 원천 | Control |

표기 주의: 리뷰 D-1의 t3(SA CR 생성)·t4(driver RUNNING)은 이 문서의 t4·t5다. Guard 성공(t3)을 추가해 보호 지연을 분리했기 때문이며, SLO 구간 "QUEUED → SA CR 생성"은 동일하다.

파생 지표:

- **lateness** = `t7 − t_logical` (Job별 freshness, H-12)
- 플랫폼 지연 = `t2 − t0`; 보호 지연 = `guard_result.result ∈ {LEASE_BUSY, SOURCE_LAG_EXCEEDED, FENCE_UNAVAILABLE}`인 row에 대해 Σ(`at − run_started_at`) + Σ(`next_eligible_at − at`)(v1.2 6.1·16.4 공통 정의 — `TARGET_UNAVAILABLE`·`CONTROL_API_UNAVAILABLE`은 플랫폼 지연); 제출 지연 = `t5 − t4`(Spark Operator); 실행 = `t6..t7`
- "queue→launch" SLO는 `t0 → t4`로 고정하되 **보호 지연을 제외**한다. 모집단은 `guard_result = OK`인 attempt만이다.

### 2.5 soak 기록 항목

7일 soak 동안 다음을 시간대별로 기록한다. 누락되면 §4 가설의 산식을 검산할 수 없다.

- run 수, lateness 분해(§2.4), `guard_result` 사유별 건수
- Source별 utilization ρ = Σ_i(D_i × w_i / period_i) / C_S(i ∈ 그 Source의 Job, D_i·period_i 같은 단위 — 무차원; H-11·v1.2.1 19장과 같은 식. 주기 band별 분해는 보조 집계), lease 대기 시간, `GV$SESSION` 집계 vs pool 회계 차이(부여/회수 이벤트 ±5초 밖 샘플)
- Run Pod RSS/CPU(shard 정의 로드 후), 이미지 pull·cold/warm start 분포, 동시 Run Pod 수
- daemon tick 소요·실패·재시도 수, batch create-or-get p50/p99, dequeue 처리량
- Dagster PostgreSQL: event_logs row/바이트 증가량, runs 수, table bloat, autovacuum 실행, UI 목록/검색 p95
- Control PostgreSQL: contract/attempt/ledger row 증가량, exclusion constraint 충돌 수, Guard 트랜잭션 p99
- etcd 객체 수·db size·apiserver p99, SparkApplication 생성→driver RUNNING 분포, Operator workqueue 깊이
- Polaris commit latency·conflict retry 수, AIStor PUT/GET IOPS·용량 증가율(GB/일), snapshot 수
- OpenSearch ingest GB/일, Kafka outbox 지연·재전송 수
- freshness breach 이벤트 건수(Control lateness sensor) vs §2.4 lateness로 계산한 기대 breach 건수, "기대 시각 occurrence 없음" 알림 건수
- 추세선: queue 깊이, DB 크기 증가율, lateness p95 — 2~7일차 선형회귀 기울기. queue 깊이는 합계 대신 **backlog 5분해**(v1.2.2 — 계약 단위로 서로소)로 기록한다: `runnable`(PLANNED ∧ `next_eligible_at ≤ now()` ∧ Hold 없음 ∧ **tag `contract_id`의 non-terminal Dagster run 없음** — 9.3 재제출 조건과 같은 술어), `submitted_queued`(신설 — PLANNED ∧ tag `contract_id`의 non-terminal run 있음, 계약별 distinct: 9.1 shard sensor `RunRequest`(v1.2.3 — tick은 제출하지 않는다; 9.3 재제출·14.2 CATCHUP·14.3 RETRY 등 5개 경로가 같은 poll 큐로 합류한다)가 나갔으나 Guard 전인 구간 — v1.2.1의 `runnable`·`dagster_nonterminal` 이중 계산을 없앤다), `executing`(ATTEMPT_ACTIVE ∪ COMMIT_OBSERVED), `safety_blocked`(ADJUDICATION_PENDING ∪ DQ_FAILED ∪ RECONCILIATION_REQUIRED — queue에서 제외하지 않는다), `terminal`(FINALIZED*·RESOLVED·VOID·CANCELLED*·ABORTED_NO_COMMIT — 합계 대조용). `dagster_nonterminal`(Dagster run QUEUED ∪ STARTING ∪ STARTED ∪ CANCELING)은 run 단위 보조 지표로만 남긴다(`submitted_queued + executing`과 대조 — 계약당 non-terminal run ≤ 1이면 두 값이 일치). 집합마다 count·oldest age·p95 age·기울기(증가/감소)를 같은 시간대 단위로 남긴다 — §5 'Source별 ρ, queue 깊이' 행 (a)는 `runnable + submitted_queued + executing` 합계로 판정하고, `safety_blocked`의 oldest age는 FI 창 밖에서 `adjudication_pending_alert_after` 초과 0이 정상(SC-04b 합격 조건의 입력). FI 수행 창은 §8.2 기록 시각으로 표시해 집계에서 구분한다

---

## 3. 시험 matrix

### 3.1 실 Job 24개 + 추가 케이스 (정합성·의미론 시험)

각 유형 4개. 1/2/4/6/12/24시간 주기, dt/mt/yt, 컬럼 가공, business WHERE, schema change를 골고루 배치한다.

| # | 유형 | cutoff / 모드 | 확인 항목 |
|---|---|---|---|
| 1 | Full-small master | `FULL_STATIC_REPLACE` | 0건 정책(12.1), `change_detection`, `EXCLUSIVE_TABLE` lease |
| 2 | Full-large 상위 10%급 | `FULL_STATIC_REPLACE` | snapshot 용량 증가(H-08), Source Read IO, row 급감 DQ(13.1 검사 5) |
| 3 | Append-small | `APPLICATION_TIMESTAMP_WITH_OVERLAP`, `ZERO_GAP` | overlap anti-join 범위(12.2), watermark CAS, 0-row chunk(FI-37) |
| 4 | Append-large event | 위와 동일 + chunk 다수 | chunk 루프(10.2), `PARTIAL_COMMIT` 재개(13.2), `APPEND` lease와 compaction 병행(SC-11, H-07) |
| 5 | Merge-small | `UNION_DEDUP`, copy-on-write | dedup 규칙(12.3), MERGE cardinality, `PARTITION_OR_FILESET` lease |
| 6 | Merge-large `INSERT_DT OR UPDATE_DT` | A/B predicate 비교, CoW vs MoR | 실행계획(`DBMS_XPLAN`), literal 형변환(12.3), `merge_metrics` 대조(13.1 v1.2.2: `written_rows == merge_metrics.inserted + updated + deleted + ignored`; 버전 gate 실패 시 `dq_basis = APP_COUNTER` fallback으로도 commit되고 receipt·ledger `dq_basis` 기록 — `versions.lock` 확정 전 회차는 전부 `APP_COUNTER`, 확정 뒤 `ENGINE_METRIC` 전환 확인; gate 통과 뒤 `merge_metrics = null` receipt는 `DQ_FAILED`) |
| 7 | Merge-large **CLOB 컬럼 보유**(v1.2.3 신설 — Merge-large 4개 중 1개를 CLOB 보유 테이블로 지정하므로 실 Job 총 24개는 불변) | `UNION_DEDUP`, copy-on-write | §3.2 canonical hash의 `0x05 LOB_DIGEST` 경로 **필수** 적용(비교 제외 선택지 없음), CLOB은 `DBMS_CRYPTO.HASH`의 CLOB overload를 쓰지 않고 명시적 AL32UTF8 변환 후 BLOB overload(`HASH_SH256`)로 산출, 좁은 행 `STANDARD_HASH(RAW,'SHA256')` 경로와의 교차검증(§8.1 G0-3 벡터 V-15), `LENGTHB` 사전 가드로 `DECOMPOSE` 조용한 절삭 회피(초과 시 `0x05` 강제), `uncompared_columns` 0건 |

추가 케이스:

- **신규 Append-large/Merge-large 초기 적재(`INITIAL_LOAD`) 각 1건** — 합격 조건: (1) occurrence 키 `job + publish_at` 1개, BACKFILL occurrence·BackfillItem 0건; (2) 실행 중 도착한 NORMAL tick 전부 occurrence `SUPERSEDED_BY(INITIAL_LOAD)` + contract `VOID(SUPERSEDED_BY_INITIAL_LOAD)`이고 해당 tick의 Dagster run 0건(9.1); (3) tick 이후 Guard에 도달한 수동 NORMAL은 Guard 7번에서 같은 disposition으로 마감(Spark 제출 0); (4) chunk마다 CAS 전진, chunk 간 간격 ≥ `initial_load.cooldown`, 동시 세션 ≤ lease weight; (5) 마지막 chunk CAS 후 production watermark = 마지막 `high_k`, 이후 첫 NORMAL window `low` = 그 값; (6) INITIAL_LOAD 진행 중 REPLAY/BACKFILL 요청 409 `OVERLAPS_OPEN_RANGE`
- 타입 경계: 정밀도 미지정 NUMBER·38자리 초과, 자정 근처 DATE(KST/UTC 경계), 한글 VARCHAR2(BYTE) 길이 초과, 9999-12-31 sentinel — 12.4 부록 매핑표대로 변환되고 §3.2 비교 A에서 차이 0; v1.2.3: 각 경계값이 §3.2 검증 벡터 V-01~V-16의 대응 벡터(V-02 비-UTF8 charset 한글, V-09 CHAR trailing space, V-10 NUMBER canonical, V-11 시간대 4종)와 같은 canonical 바이트열·같은 `h`를 내는지 함께 대조한다
- Merge-large copy-on-write vs merge-on-read 비교(파일 churn, compaction 필요량, 쿼리 지연)
- `BEST_EFFORT` 등급 Job 1개(lag 신호 없는 stub Source) — Data Reconciliation Audit이 gap을 탐지·repair하는지(12.3, FI-12(b)); 합격 조건 추가(v1.2.1 11.3): 이 Job의 계약 100%가 `confidence = DEGRADED`·`confidence_reason = NO_LAG_SIGNAL`이고 ledger row에 복사됨 ∧ 이 Source의 Outbox `degraded confidence` 0(대조군: FI-11 BEST_EFFORT 변형의 `DATUM_STALE` 계약은 1건) ∧ Audit 우선 대상 목록에 포함
- `STANDBY_VISIBLE_SCN` cutoff 1개(PoC 한정, 11.3) — `VERSIONS BETWEEN SCN` 또는 `ORA_ROWSCN`의 비용과 false positive 비율
- Append-large 닫힌 구간 BackfillPlan 1건(`window.high ≤ current_watermark`, item 3개) — start 후 item 1 FINALIZED 시점에 `pause`(진행 중 item 완주·다음 item 미제출) → `resume` → item 2 FINALIZED 후 `cancel`(item 3 미실행 마감); 각 item이 별도 contract·SA 1개, watermark 불변(13.4, 14.4), 각 호출 AuditEvent(actor·idempotency_key) 1건 — §6 운영 절차 체크리스트의 BackfillPlan 증거
- **WAP 경로(H-06)**: Merge-large 2개 + Full-large 1개를 attempt branch(`branch_{contract_short}`, 사전 생성 RETAIN) → DQ → `fast_forward`로 2회차 이상 실행하고, 같은 Job의 main 직접 commit 회차와 metadata.json·manifest list 증가량·refs 잔존·Polaris 조회 오류를 대조(13.2 제약). v1.2.2 13.1 WAP 규칙 확인: `chunks/{n}:commit`·CAS는 ff 성공 뒤에만(ff 전 ledger row 0), ledger `committed_snapshot_id = published_main_snapshot_id`(= `branch_head_snapshot_id`)이며 전부 current main의 ancestor, WAP attempt의 target lease는 Merge여도 `EXCLUSIVE_TABLE`(`lease_state_history`); ff 기각 변형은 H-06

### 3.2 정합성 판정 방법

"설명되지 않은 row/hash 차이 0"은 비교 기준 시점이 정의되어야 측정된다. 두 가지 비교를 쓴다.

- **비교 A(contract 정확성)**: contract FINALIZED 직후 판정 스크립트가 자동 실행(`fence_ts + undo_retention × 0.5` 이내). Source `SELECT pk, ETL_CANON.ROW_HASH(<매핑표 순서 컬럼 목록>) AS h FROM t AS OF SCN :visible_scn WHERE <window predicate + business WHERE>`(v1.2.3 — `ETL_CANON`은 아래 'canonical hash 규격(v1.2.3)'을 구현한 **PL/SQL 함수 기본안**이다. `UTL_I18N`·`UTL_RAW`·`DBMS_CRYPTO`의 SQL 직접 호출 가능성이 미확인이므로 SQL 인라인 표기는 쓰지 않으며, physical standby는 읽기 전용이라 **객체 생성·EXECUTE 권한 부여는 primary 선행 작업**이다) vs Iceberg `committed_snapshot_id`들의 `[etl.logical_window_low, etl.logical_window_high)` 구간 row에 같은 canonical 함수를 적용한 `(pk, h)`(`STANDBY_VISIBLE_SCN`은 `etl.scn_low/high`). **판정 = 양방향 PK 차집합 0 ∧ 공통 PK의 `h` 불일치 0**(Source − target = 누락, target − Source = 초과·삭제 미반영 — v1.2.2); `h` 불일치 PK만 typed column exact 비교로 `(pk, column, source_value, target_value)`를 리포트한다(typed exact는 권위가 아니라 불일치 설명용). Iceberg 측 `committed_snapshot_id`는 판정 시점 `<table>.refs['main']`의 ancestry(`snapshots.parent_id` 추적)에 있어야 하며 branch에만 있는 snapshot은 '미publish'로 불합격(v1.2.2 13.1 WAP — ledger `committed_snapshot_id = published_main_snapshot_id`, H-06 변형). v1.2.1의 `ora_hash(row)`는 유효 SQL이 아니므로 폐기한다(`row` pseudo-column 없음, `ORA_HASH`는 단일 scalar expr·32비트). **canonicalization 규칙**(v1.2.3에서 **전면 교체** — 아래 'canonical hash 규격(v1.2.3)' 블록이 유일한 권위다): v1.2.2의 구분자 `'|'` 연결(값 안의 `'|'` → `'\|'`)·NULL sentinel 문자열 `<<NULL>>`·RAW의 `RAWTOHEX`·"CLOB/BLOB은 비교에서 제외하고 '설명된 차이'로 기록" 선택지는 **모두 폐기**한다. 문자 단위 연결로는 Oracle(DB charset)과 Spark(UTF-8) 사이에 **SHA-256 입력 바이트열 차이**가 남기 때문이다. Source SQL(`ETL_CANON`)과 Spark Template은 12.4 부록 pinned 매핑표에서 같은 태그·길이-prefix 규칙을 생성하고 **항상 2단 고정** SHA-256으로 `h`를 만든다(주차 1 dry-run에서 실 실행 — §8.1 8번 (iii); 벡터 V-01~V-16의 일치·불일치가 기대대로여야 G0-3 PASS). 차이 0. Incremental은 논리 window `[low, high)` 안의 row에 더해 **overlap 회수 row**(v1.2.1 12.2)를 별도 행으로 비교한다: Source `AS OF visible_scn`의 `[ledger.extract_window_low, contract.original_logical_low)` row 중 `attempt.base_snapshot_id` 시점 target에 없던 PK 집합 = overlap을 적용한 chunk 1(v1.2.2 12.2 — `window.low == original_logical_low`인 attempt의 chunk 1; 첫 CAS 전에 끝난 attempt 뒤면 재개 attempt의 chunk 1) snapshot의 그 구간 row 집합, 건수 = ledger `overlap_recovered_rows`(차집합 0); chunk ≥ 2와 `window.low > original_logical_low`인 재개 attempt의 `extract_window_low = low_k`(overlap 0), 첫 CAS 전 실패 뒤 재개 attempt의 chunk 1은 `extract_window_low = original_logical_low − overlap`(overlap 재적용, v1.2.2 12.2) 확인 — 두 조건은 ledger `cas_applied` row 존재 여부와 일치해야 한다.
- **비교 B(Job 최종 일치, late-commit 포함)**: 마지막 시험 회차 FINALIZED + overlap + `max_commit_minus_watermark_seconds` 경과 후 1회(`delete_semantics = PK_RECONCILE` Source는 + `PK_RECONCILE.interval` 경과 후 — v1.2.2 11.3 데이터 계약 노출 항목). Source 현재 SCN `AS OF`로 `UPDATE_DT < last_watermark − overlap` row 전체의 `(pk, h)`(비교 A와 같은 canonical hash — v1.2.3 'canonical hash 규격'의 2단 SHA-256이며, 엔진 경로는 좁은 행 `STANDARD_HASH(RAW,'SHA256')`·광폭 및 LOB `DBMS_CRYPTO.HASH(BLOB, HASH_SH256)` 둘로 나뉜다) vs Iceberg 현재 main snapshot(해당 Job 파티션 범위)의 `(pk, h)`. **양방향 차집합**: Source − target(누락)·target − Source(hard delete 미반영·초과)를 각각 집계하고 공통 PK의 `h` 불일치 PK만 typed exact 비교로 리포트. 차이 = 설명된 차이만이며, v1.2.3에서 **수치 판정으로 정정**한다: `target − Source`의 각 PK는 `(비교 시각 − 마지막 Source 변경 관측 시각) ≤ delete_lag_slo_seconds`이면 **설명된 차이**, 초과면 **설명되지 않은 차이**로 센다(`delete_lag_slo_seconds`는 `delete_semantics`에서 전순 결정되는 파생값 — `SOFT_DELETE` = `0`, `PK_RECONCILE {interval}` = `duration(interval) + reconcile Job의 freshness_slo`, `CDC_LATER`·`NONE_DECLARED` = `null`, v1.2.3 7.2). `Source − target`(누락)은 값과 무관하게 전부 설명되지 않은 차이다(`delete_semantics`에 따른 기대 — `SOFT_DELETE`는 watermark와 동기, `PK_RECONCILE`은 interval 이내 반영; `CDC_LATER`·`NONE_DECLARED`는 ZERO_GAP publish 불가라 ZERO_GAP 표본에 없다, v1.2.2 7.2·11.3).
- 비교 시점이 `fence_ts + undo_retention × 0.5`를 넘기면(Full-large·24h 주기) Source 측 `AS OF SCN` 대신 해당 attempt의 extract-once staging manifest(11.5, 같은 `visible_scn`으로 추출된 증거)를 비교 기준으로 쓰고 리포트에 "비교 기준 = staging"으로 표기한다. 비교 불가(ORA-01555)는 "설명된 차이"가 아니라 **"미비교"**로 별도 집계하며 24 Job 중 미비교 0건이 합격 조건이다.
- **LOB·미비교 규칙(v1.2.3 정정)**: v1.2.2의 "CLOB/BLOB → hash로 치환하거나 **비교에서 제외하고 '설명된 차이'로 기록**" 중 후자 선택지를 **삭제**한다. LOB 컬럼은 `0x05 LOB_DIGEST` 태그로 **hash가 원칙**이며, 비교에서 빠질 수 있는 경우는 둘뿐이다 — (i) LOB 컬럼의 `RETENTION`/`PCTVERSION`이 미확인이거나 예상 추출 시간 대비 부족해 `AS OF SCN` 재독이 불가능한 컬럼(v1.2.3 A 22장 2번, `fence_mode = EXTRACT_ONCE`로도 부족한 경우), (ii) hash 산출 자체가 불가능한 `LONG`·`LONG RAW`. 단 (ii)는 A 12.4 부록 rule `LONG_NOT_EXTRACTABLE`로 publish가 422이므로 24 Job 모집단에 들어올 수 없고, 실제로 관측될 수 있는 제외는 (i)뿐이다. 그런 컬럼이 있는 Job은 그 컬럼을 `hash_participation = EXCLUDED`로 표시하고, Job별 해당 컬럼 목록(= `uncompared_columns` — 별도 필드가 아니라 `hash_participation = EXCLUDED` 컬럼의 §8.2 기록 표기이며 §8.1 8번 G0-3이 기록하는 `hash_participation = EXCLUDED` 목록과 같은 대상이다)을 §8.2에 적은 뒤 **ZERO_GAP 표본에서 탈락**시킨다 — `h`가 행 전체를 덮지 못하므로 "설명되지 않은 차이 0"의 모집단이 될 수 없다. 24 Job 중 `uncompared_columns`가 비지 않은 Job 0건이 목표이고, 1건이라도 있으면 탈락 Job 목록과 남은 ZERO_GAP 표본 수를 §8.2에 함께 적는다. 이 집계는 ORA-01555로 인한 "미비교"(위 항)와 별개다.
- "설명된 차이": Source hard delete(`delete_semantics`에 따라 기대됨 — v1.2.3: 비교 B의 `(비교 시각 − 마지막 Source 변경 관측 시각) ≤ delete_lag_slo_seconds` 수치 판정을 통과한 건만 해당하고, 초과 건은 "설명되지 않은 차이"로 센다), `BEST_EFFORT` 등급의 audit 전 gap, 운영자 `dq:accept`/`accept-empty` 기록. 각 건을 `(job_id, pk, 사유, 근거 row id)`로 §8.2에 첨부한다. 그 외는 전부 "설명되지 않은 차이"로 센다.
- **Run당 Oracle 세션 수**: stub에서는 세션 수명 로그(§2.3)로 contract별 동시 세션 최대값을 정확히 센다. 실 DR에서는 DBA 승인 하에 unified audit LOGON 레코드 또는 `GV$SESSION` 1초 샘플(`CLIENT_IDENTIFIER = {contract_id}/a{attempt_no}` 완전 일치 — v1.2.3 11.2 회계 join key, `MODULE`·`contract_short`는 판정에 쓰지 않는다)을 쓰고, 샘플 방식이면 "p99 ≤ weight, 최대 ≤ weight + 1(driver 세션 관측 오차)"로 기록한다.
- Oracle 측 지표(리뷰 ORA-11): ORA-02391 발생 0(절대 한도 초과 그 자체, §6); ORA-03172/ORA-01555 발생 건수(의도된 fail-fast만 허용); apply lag p95·최대와 `confidence = DEGRADED` 계약 비율(BEST_EFFORT 모집단); Source별 시간대 ρ.

**canonical hash 규격(v1.2.3 — 비교 A·B 공통, `canonical_hash_spec_digest`의 대상)**

```text
column_bytes(i) = TAG(1B) ‖ BYTELEN(4B BE) ‖ BYTES
d_i             = SHA256(column_bytes(i))
g_j             = SHA256(0xFE ‖ COUNT_j(4B BE) ‖ d_{j,1} ‖ … )      -- 매핑표 순서 32개씩
h               = SHA256(0xFF ‖ COLCOUNT(4B BE) ‖ g_1 ‖ … ‖ g_m)
```

- **항상 2단 고정**이다(컬럼 수에 따라 단수를 바꾸는 분기 자체가 drift 원인). 컬럼 수 **> 1024는 publish validator가 거부**한다.
- 타입 태그: `0x00 NULL`(BYTELEN 0 — sentinel `<<NULL>>` 폐기) / `0x01 STRING` / `0x02 NUMBER`(고정 십진 텍스트, `-0` 금지) / `0x03 TIMESTAMP`(UTC ISO-8601 µs) / `0x04 BINARY`(원바이트 — `RAWTOHEX` 폐기) / `0x05 LOB_DIGEST`(BYTES 자리에 SHA-256 32B).
- **STRING(Oracle)**: `UTL_I18N.STRING_TO_RAW( COMPOSE( DECOMPOSE( TO_NCHAR(<col>), 'CANONICAL' ) ), 'AL32UTF8' )`. `COMPOSE` 단독은 문서상 NFC가 아니며, `TO_NCHAR`는 선택이 아니라 **필수**다(비-Unicode DB charset에서는 `DECOMPOSE`/`COMPOSE`가 무동작). `DECOMPOSE` 결과가 최대 길이를 넘으면 **오류 없이 조용히 절삭**되므로 `LENGTHB` 사전 가드를 두고 초과 컬럼은 `0x05` 경로로 강제한다.
- **NULL vs 빈 문자열**: Oracle에서 `''` = NULL이므로 **Spark 측도 `''`를 NULL(`0x00`)로 정규화**한다(V-07).
- **NUMBER(`0x02`) 실행 규격**(v1.2.3.1 — ‘고정 십진 텍스트’만으로는 Oracle·Spark 양측이 같은 바이트를 내지 못한다): ① **scale 확정** — 12.4 부록 pinned 매핑표가 컬럼마다 `(p, s)`를 고정하고, 정밀도 미지정 NUMBER는 등록된 `(38, s)`로 고정한다(**매핑표에 `(p, s)`가 없는 컬럼이 있으면 그 자체가 G0-3 FAIL(매핑표 미확정)이며 `hash_participation = EXCLUDED` 사유를 늘리지 않는다** — 제외 사유는 §3.2 ‘LOB·미비교 규칙’의 (i) LOB `RETENTION` 부족, (ii) `LONG`·`LONG RAW` 둘뿐이다, v1.2.3.1). ② **반올림** — 값을 그 `s`로 **HALF_UP**(0.5는 절대값이 큰 쪽) 반올림한다: Oracle `ROUND(x, s)`, Spark `BigDecimal.setScale(s, RoundingMode.HALF_UP)`. 절삭·HALF_EVEN 금지. ③ **음수 scale** — `s < 0`이면 `ROUND(x, s)`로 반올림한 뒤 **소수부 0자리** 텍스트로 쓴다(`NUMBER(p,−2)`의 `1234` → `1200`; 버려진 자리는 `0`으로 보존하며 지수 표기 금지). ④ **후행 0** — `s > 0`이면 소수부를 **항상 정확히 `s`자리** 채운다(`0.1` @ `s = 2` → `0.10`), `s ≤ 0`이면 소수점을 쓰지 않는다. ⑤ **부호·표기** — 음수만 선행 `-` 1개, `+` 금지, 반올림 후 값이 0이면 `-0`이 아니라 `0`, 정수부가 비면 `0` 1개(`0.10`), 선행 0 padding·그룹 구분자·지수 표기(`1E2`) 금지. ⑥ **로케일 차단** — 텍스트는 ASCII이고 소수점은 항상 `.`이다. Oracle 측은 NLS 의존 `TO_CHAR(x)`·`'TM'` 포맷을 쓰지 않고 세션 `NLS_NUMERIC_CHARACTERS = '.,'`를 `sessionInitStatement`로 고정한 뒤 명시 포맷 모델로 만든다. ①~⑥이 모두 고정돼야 V-10의 `0` / `-0` / `0.10` / `1E2`가 결정론적으로 같은 바이트가 된다.
- **TIMESTAMP(`0x03`) 실행 규격**(v1.2.3.1 — ‘UTC ISO-8601 µs’만으로는 NTZ 해석 주체와 fraction 처리가 미정이다): ① **표기** — 고정 폭 `YYYY-MM-DDTHH24:MI:SS.ffffffZ`(연 4자리 zero-pad, 소수부 항상 6자리, 항상 `Z` 접미) ASCII 27B. ② **fraction** — µs 초과 자리는 **절삭(truncate, 0 방향)이며 반올림하지 않는다**(반올림은 초·일·연 경계를 넘겨 양측 불일치를 만든다). `TIMESTAMP(9)`는 하위 3자리를 버리고, `DATE`는 `.000000`이다. ③ **`TIMESTAMP`(NTZ)·`DATE`** — 시간대 정보가 없으므로 **DB 시간대로 해석하지 않고 저장된 벽시계 값을 그대로 UTC로 간주**한다(변환 0). 어느 한쪽만 세션 시간대를 적용하면 바이트가 갈리므로 Spark Template은 `spark.sql.session.timeZone = UTC`를 고정하고 `ETL_CANON`은 NTZ·DATE에 어떤 시간대 변환도 적용하지 않는다. ④ **`WITH TIME ZONE`** — `SYS_EXTRACT_UTC` 적용 후 ①로 표기한다(저장된 offset이 변환 기준이며 세션 시간대는 무관). ⑤ **`WITH LOCAL TIME ZONE`** — 반환값이 **세션 시간대에 의존**하므로 세션 `TIME_ZONE = '+00:00'`을 `sessionInitStatement`로 고정한 뒤 읽고(Spark JDBC 세션도 같은 init), 그 값을 ③ 규칙으로 표기한다. ⑥ ③~⑤의 귀결 — **같은 벽시계 값의 NTZ와 같은 순간을 가리키는 TSTZ는 서로 다른 `h`일 수 있다**. 같은 `h`를 요구하는 것은 ③~⑤ 적용 후 **같은 UTC 순간으로 수렴하는 값들뿐**이며 V-11 fixture는 그렇게 구성한다.
- **CLOB**: `DBMS_CRYPTO.HASH`의 **CLOB overload를 쓰지 않는다**(적용 charset이 문서에 없다). 명시적 AL32UTF8 변환 후 **BLOB overload**로 넘긴다. 결과적으로 엔진 경로는 좁은 행 `STANDARD_HASH(RAW,'SHA256')`과 광폭·LOB `DBMS_CRYPTO.HASH(BLOB, HASH_SH256)` 둘이며, **두 경로의 동일성은 가정하지 않고 §8.1 G0-3에서 교차검증**한다(V-15).
- **Spark**: `sha2(binary, 256)`은 hex 문자열을 돌려주므로 중간 단계는 `unhex()`로 바이트로 되돌린다. `normalize(str, 'NFC')` built-in은 4.0~4.2 함수 registry에 **없으므로** `java.text.Normalizer` 결정론 UDF를 Template에 고정하고 **UDF 소스 digest를 `canonical_hash_spec_digest`에 포함**한다(UDF 교체 = hash spec 변경 = G0 무효화).
- **미확인 표기**: `STANDARD_HASH`의 RAW 인자 허용은 배제 규칙에 의한 **추론**이며 G0-3에서 실증한다. `UTL_I18N`·`UTL_RAW`·`DBMS_CRYPTO`의 SQL 직접 호출 가능성도 미확인이라 **기본안은 PL/SQL 함수(`ETL_CANON`)로 감싸고**, physical standby는 읽기 전용이므로 **객체 생성·EXECUTE 권한은 primary 선행 작업**이다(v1.2.3 22장 DBA 항목). Oracle ↔ JVM의 Unicode 버전 일치도 미확인이므로 V-03~V-05 픽스처로 실측한다.
- **`canonical_hash_spec_digest`의 명시 입력**(v1.2.3.1 — 지금까지 산문에 흩어져 있던 것을 목록으로 고정): {(i) 12.4 부록 pinned 매핑표 digest, (ii) 이 블록의 타입 태그·정규화 규칙·NUMBER/TIMESTAMP 실행 규격 텍스트 digest, (iii) **`ETL_CANON` 함수 source digest** — 패키지·함수 본문 원문(배포 스크립트 또는 `ALL_SOURCE` 조회 결과)의 SHA-256, (iv) Spark `java.text.Normalizer` 정규화 UDF source digest, (v) `versions.lock`의 canonical hash probe 3항 결과}. 다섯 중 하나라도 바뀌면 digest가 바뀌며 그것은 §8.1 8번 G0 무효화 사유 (4)다 — **`ETL_CANON`을 고쳐 배포하는 것은 hash 규격 변경**이고, 배포하면서 새 source digest를 `g0_evidence.canonical_hash_spec_digest`에 다시 기록하지 않으면 G0은 PASS가 아니다.
- **`MAX_STRING_SIZE`**: 규격은 `STANDARD`(SQL RAW 2000B)를 가정한다. `EXTENDED` 전환은 비가역이고 뷰 무효화·함수기반 인덱스 UNUSABLE 부작용이 있어 **운영 standby에 요구하지 않으며**, 소스별 값은 §2.1에서 `SourceCapability.max_string_size`로 등록해 `LENGTHB` 가드 임계값으로 쓴다.

**검증 벡터 V-01~V-16**(Oracle `ETL_CANON`과 Spark Template이 같은 `h`를 내는지 — §8.1 G0-3 PASS 조건이며 결과는 `hash_vector_result`에 기록)

| ID | 벡터 | 기대 |
|---|---|---|
| V-01 | ASCII 기본 행(전 타입 1컬럼씩) | 양측 `h` 일치 — 회귀 기준선 |
| V-02 | 비-UTF8 DB charset(예: `KO16MSWIN949`) 저장 한글 | `TO_NCHAR` 경유 후 AL32UTF8 바이트열이 Spark UTF-8과 동일 → `h` 일치. `TO_NCHAR`를 뺀 변형은 **불일치**(양성 대조) |
| V-03 | NFD 입력(한글 자모 분리·라틴 분해형) | V-04와 같은 `h` |
| V-04 | NFC 입력(V-03과 논리적으로 같은 문자열) | V-03과 같은 `h`. `COMPOSE` 단독 변형은 불일치로 관측되어야 한다 |
| V-05 | 결합 마크 다중 부착(결합 순서만 다른 두 값) | 정규화 후 같은 `h` — Oracle↔JVM Unicode 버전 불일치 탐지용 |
| V-06 | BMP 밖 문자(U+1F600 등 surrogate pair) | 4바이트 UTF-8로 같은 `h`, BYTELEN이 문자 수가 아니라 **바이트 수** |
| V-07 | NULL / `''` / 리터럴 문자열 `'<<NULL>>'` 3분기 | 앞 둘은 `0x00`+BYTELEN 0으로 같은 `h`, 셋째는 `0x01` STRING이라 **다른 `h`**(sentinel 폐기의 양성 증거) |
| V-08 | 구분자 주입 2종 — `('a\|b','c')` vs `('a','b\|c')`, 그리고 백슬래시 이스케이프를 포함한 값 | 세 조합이 모두 **서로 다른 `h`**(길이-prefix가 주입을 차단) |
| V-09 | `CHAR(n)` trailing space(값 `'A'` vs `'A '`) | 매핑표 규칙대로 처리한 뒤 양측 `h` 일치, 규칙 미적용 변형은 불일치 |
| V-10 | NUMBER canonical — `0` / `-0` / `0.10` / `1E2` / 정밀도 미지정 38자리 초과 | `-0`은 `0`과 같은 고정 십진 텍스트(`-0` 표기 금지), `0.10`·`1E2`는 매핑표 scale 고정 표기로 결정론적 일치 |
| V-11 | 시간대 4종 — `DATE` / `TIMESTAMP` / `TIMESTAMP WITH TIME ZONE` / `WITH LOCAL TIME ZONE`(자정·DST 경계 포함) + µs 초과 fraction 경계값(`...59.9999999`) | v1.2.3.1 정정: fixture는 위 ‘TIMESTAMP 실행 규격’ ③~⑤를 적용했을 때 **같은 UTC 순간이 되도록** 구성하며, 그때 넷 다 같은 `h`를 낸다(수렴 표기 `YYYY-MM-DDTHH24:MI:SS.ffffffZ`). 세션 `TIME_ZONE = '+00:00'`·`spark.sql.session.timeZone = UTC`를 고정하지 않은 변형은 **불일치**(양성 대조 — LTZ가 세션 의존임을 드러낸다). fraction은 절삭이므로 `...59.9999999` → `...59.999999`이고 초가 넘어가지 않는다(반올림 변형은 불일치) |
| V-12 | 선행 `0x00` 포함 RAW(`00FF00`)와 길이 0 RAW | 원바이트 그대로 `0x04`로 들어가며 절단·hex 왕복 없이 `h` 일치 |
| V-13 | 32·33·64·65 컬럼 행(그룹 경계) | 2단 고정 하에서 그룹 수 `m` = 1/2/2/3으로 정확히 나뉘고 양측 `h` 일치 |
| V-14 | RAW 상한 경계 — `MAX_STRING_SIZE = STANDARD` 기준 1999·2000·2001B와 `DECOMPOSE` 후 상한을 넘는 값 | 2000B 이하는 `0x01`/`0x04` 경로로 일치, 초과는 `LENGTHB` 가드가 잡아 `0x05` 강제(조용한 절삭 0건) |
| V-15 | 엔진 경로 교차 2분기(v1.2.3.1 재작성) — ⒜ **같은 logical type 안에서** 같은 입력 바이트열(**AL32UTF8 기준 ≤ 2000B** — `MAX_STRING_SIZE = STANDARD`의 SQL RAW 상한이라 두 경로를 모두 태울 수 있는 유일한 구간이다)을 `STANDARD_HASH(RAW,'SHA256')` 경로와 `DBMS_CRYPTO.HASH(BLOB, HASH_SH256)` 경로로 각각 산출(같은 CLOB 값 1건·같은 BLOB 값 1건), ⒝ 같은 내용의 CLOB·BLOB·VARCHAR2 3종의 최종 `d_i` 교차 | ⒜ **두 경로의 content digest(같은 입력에 대한 SHA-256 32B)가 바이트 단위로 동일** — 이것이 두 엔진 경로 동일성의 실증이며, 그 32B가 그 컬럼 태그(CLOB은 `0x05 LOB_DIGEST`, BLOB은 `0x04`)의 BYTES 자리에 들어간다. ⒝ 규격이 **타입 태그를 `column_bytes`에 넣으므로** 세 값의 최종 `d_i`는 **서로 다름**이 기대값이다(CLOB → `0x05`, BLOB → `0x04`, VARCHAR2 → `0x01`; BYTELEN도 32B vs 원바이트 길이로 갈린다). v1.2.3의 ‘같은 내용의 CLOB·BLOB·VARCHAR2의 `d_i`가 동일’ 기대는 규격과 모순이므로 폐기한다 — 세 `d_i`가 같게 나오면 태그·길이-prefix가 빠진 것이므로 **G0-3 FAIL** |
| V-16 | 1바이트 음성 대조 — 마지막 바이트 하나만 다른 두 행 | `h` **불일치**(비교 A·B가 실제로 차이를 잡는다는 양성 증거) |

### 3.3 규모 시험

| ID | 시나리오 | 부하 모델 | 측정 | 기준 유형 / 합격 |
|---|---|---|---|---|
| SC-01 | 10,000 Asset definition cold load / 단일 Job publish reload / Bundle 전달 방식 | shard 8·12·16 × 전달 방식 2종(이미지 내장 / AIStor bundle pull) = 6 조합 | shard당 cold load, reload 중 tick 실패 수, webserver 메모리, asset 검색 p95, publish→가시화 단계별 시간(compile+validate / 승격 / reload / webserver 갱신), 이미지 빌드·push 또는 AIStor pull 시간, **Control API 차단 상태에서 code location cold start 성공 여부** | 가설(H-13) — cold load 10분·reload 중 tick 손실 0·publish p95 3분은 H-13 판정 기준 |
| SC-02 | 정시 burst 500 occurrence + Run queue | 500 Job이 같은 cron, 2개 Source에 편중(worst case 단일 shard 500 포함) | batch create-or-get p99, tick 소요·실패, **RunRequest 생성 시간**(500건 — shard sensor 평가 안에서 `asset_selection` subset 생성부터 yield 완료까지, v1.2.3 9.1; tick은 `RunRequest` 0 + `SkipReason`이므로 tick 소요가 아니라 **sensor 평가 소요에서 `submissions:poll` 왕복 시간을 뺀 값**으로 분리 집계한다 — end-to-end 제출 지연은 SC-02d), `t0→t4` 분포, 동시 Run Pod 수·RSS | 확정 + 가설(H-01, H-10) |
| SC-02b | SC-02와 같은 burst에서 500건 중 1건에 항목 고유 오류 주입(poison item — 제약 위반을 stub으로 유발, 별도 회차 1회는 cron 파싱 오류); 주입한 `(job_id, logical_scheduled_at)`를 §8.2에 기록 | SC-02 부하 모델 그대로, 연속 3 tick 반복 | 해당 항목 `ROLLBACK TO item_k` → 응답 `{launch: false, error: ITEM_REJECTED}` + Outbox `occurrence item rejected`(payload `job_id`·`logical_scheduled_at`·`reason`, v1.2.2 9.1·16.4) 1건, 나머지 499 `launch=true`, tick SUCCESS(9.1 v1.2.1); batch p99 변화 | 확정 — 합격: tick당 `RunRequest` **0** ∧ `SkipReason` 1(v1.2.3 A 9.1 — tick은 occurrence 생성 전용) ∧ 응답 항목 `launch = true` 499 ∧ 그 499 계약이 뒤이은 shard sensor 평가의 `submissions:poll`에 실려 계약당 run 정확히 1개 ∧ `ITEM_REJECTED` 1(양성 증거) ∧ tick FAILURE 0·`max_tick_retries` 소진 0 ∧ Outbox 1건/tick ∧ 거부 Job만 `expected occurrence missing` 1건 ∧ 499 Job의 occurrence row·contract `PLANNED` 존재 ∧ 거부 Job의 occurrence row 0 ∧ 같은 `(job_id, logical_scheduled_at)`의 Outbox `occurrence item rejected` 정확히 1건(= §5.1 (1) 설명된 누락 `ITEM_REJECTED` — 3 tick 합계가 주입 건수와 일치, 그 외 설명된 누락 0); No-Go 연결: 정상 Run 중복/누락(§5.1 (1)의 설명된 누락 규칙으로 판정 — No-Go 2번과 양립) |
| SC-02c | 정시 burst 500 Job이 **단일 Source**에 집중(skew worst case — §3.5 5번, soak 종료 후) | SC-02와 같은 cron의 500 Job을 모두 Source S 하나(C = Phase 0 최대 C)에 배치, stub 지연 D 분포 유지. 회차 1 = v1.2 기본(Guard 6번 조회를 모니터 세션 1개에서 직렬 수행, 10.2), 회차 2 = **fence precursor 비교안**(모니터 세션이 `datum_stale_seconds` 이내 주기로 role·`visible_scn`·lag·`ALL_TAB_COLUMNS` digest를 immutable row로 두고 Guard가 row lock 안에서 age 검사 후 소비 — `DATUM_TIME` stale 판정과 같은 기준) | Guard 트랜잭션 p50/p99, 모니터 세션 큐 깊이·조회 대기, `monitor_query_timeout_seconds` 초과 수, `LEASE_BUSY` 거부 수·backoff 분포, stub connection 카운터 최대값, `t0→t4`(보호 지연 제외), 회차 2의 `fence_ts − precursor 시각` | 확정 + 가설(H-09·H-11). 합격(회차 1): stub 카운터 거부 0 ∧ 1초 샘플 전부 동시 세션 ≤ C(양성 증거: 카운터 최대값 = C 도달 ≥ 1회 ∧ `guard_result = LEASE_BUSY` ≥ 1) ∧ Guard p99 ≤ `monitor_query_timeout_seconds` + 5초 ∧ `FENCE_UNAVAILABLE` 0 ∧ 모니터 세션 조회 timeout 0 ∧ §5.1 (1)(2) 중복 0; 회차 2는 같은 조건에서 Guard p99와 `visible_scn` 보수화(`fence_ts − precursor 시각` ≤ `datum_stale_seconds`, 안전 방향) 기록 — 회차 1이 p99를 넘기면 precursor 채택을 v1.2.2 입력(H-11)으로. No-Go 연결: 절대 한도 초과, 중복/누락 |
| SC-02d | shard sensor 단일 제출 채널의 burst 500 제출 지연(v1.2.3 9.1~9.3 — schedule tick은 occurrence 생성 전용이고 `RunRequest` 0, 제출은 shard sensor `run_key` 한 채널) | SC-02와 같은 정시 burst 500(worst case 단일 shard 500 포함)을 shard sensor로만 제출; `dagster.yaml`의 `sensors: {use_threads: true, num_workers, num_submit_workers}`에서 `num_submit_workers`만 **1 / 4 / 16** 3수준으로 바꾼 대조, 각 수준 3회차(부하 모델·`minimum_interval`·poll `limit`은 고정) | (a) tick(occurrence 생성) → **마지막 run 제출**까지 end-to-end p50/p99, (b) sensor 평가 안 `fetch_existing_runs` 구간 소요(500 `run_key` 직렬 조회), (c) 수준별 (a)와 예측식 `minimum_interval + poll + (limit × 직렬 제출비용 / num_submit_workers)`의 차, Control `submissions:poll` 응답 시간, sensor 평가 실패·`SkipReason`·tick FAILURE 수 | **측정·기록 + 상한 선언**(SLO 판정 아님) — 3수준 전부 완주(제출 누락 0 ∧ 같은 `(contract_id, resubmit_no)`의 run 정확히 1 ∧ tick FAILURE 0) ∧ (a)(b)(c)를 §8.2에 수치로 남기고 그 관측 최대값 + margin을 **burst 제출 지연 상한 선언값**으로 §7·§8.1 6번에 올린다 ∧ 선언 상한을 만족하는 최소 `num_submit_workers`를 §7 확정 파라미터로 기록하고, 어느 수준도 만족하지 못하면 검토서 v3.1 §7.3 차선책(private adapter) 조건 (d)의 입력으로 기록 |
| SC-03 | 40,000 Run/일 7일 soak | Phase 0 주기 분포, stub 지연 주입, ≥30일치 event_logs 사전 seed. **7일간 부하 모델을 바꾸지 않는다** | §2.5 전 항목, 추세선 기울기 | 확정 |
| SC-04 | stress 60,000/일, 750 burst (soak 종료 후) | SC-03 × 1.5 | 어느 구성요소가 먼저 한계에 닿는지 병목 순위표 | 가설(H-10) |
| SC-04b | queue cap 실제 포화(soak 종료 후, SC-04 직후 — §3.5 5번) | dagster `max_concurrent_runs`를 200으로 축소(§7 시작값 800 대조군, FI-35(b)와 같은 수단)한 상태에서 정시 burst 500 + Source 2개를 C까지 포화(SC-05 부하 모델 축소판) 60분 유지 → cap 800 복원. 1,200 burst는 두지 않는다(포화 자체는 cap 축소로 재현) | §2.5 backlog 5분해(`runnable`·`submitted_queued`·`executing`·`safety_blocked`·`terminal`, 보조 `dagster_nonterminal`)의 count·oldest age·p95 age·기울기를 30초 단위로, `dagster/priority` 상위 10% Job(critical_bonus, 9.1)의 lateness vs 하위 10%, `EXPIRED_UNLAUNCHED`·`COALESCED_INTO` 건수, 복원 후 backlog 소진 시각 | 확정. 합격(v1.2.2 식): (a) 포화 60분 동안 전 샘플에서 `runnable + submitted_queued + executing + safety_blocked ≤ expected occurrence 수`(집합이 서로소이므로 합계 > expected = 중복 제출, 0건 — §5.1 (1)·(2)) ∧ 계약당 tag `contract_id` non-terminal Dagster run ≤ 1(§5.1 (2)와 연결; v1.2.1의 `runnable + dagster_nonterminal` 식은 QUEUED run의 PLANNED 계약을 이중 계산하므로 폐기) ∧ `dagster_nonterminal` = `submitted_queued + executing`의 run 수(양성 증거: 포화 중 `submitted_queued` ≥ `max_concurrent_runs` 도달 샘플 ≥ 1); (b) priority starvation 없음: 상위 10% Job의 lateness p95 ≤ 하위 10%의 p95 ∧ 상위 Job `EXPIRED_UNLAUNCHED` 0(양성 증거: 하위 Job `EXPIRED_UNLAUNCHED` ≥ 1 또는 `COALESCED_INTO` ≥ 1); (c) `safety_blocked`가 포화로 증가하지 않음(oldest age ≤ `adjudication_pending_alert_after`, 증가 기울기 ≤ 0); (d) 복원 후 `runnable + submitted_queued` 기울기 음수(recovery slope 양수)이고 backlog 0 도달 ≤ 2 × 최소 period; (e) Source 동시 세션 ≤ C 전 샘플 ∧ `guard_result = LEASE_BUSY` ≥ 1. No-Go 연결: 중복/누락, 절대 한도 초과 |
| SC-05 | Source 포화 (soak 종료 후) | 단일 Source에 J=400, period 1h, D=4분, C=10(ΣD/C = 160분 > period) | queue 깊이 추세, `COALESCED_INTO` 비율, lateness, Guard `LEASE_BUSY` backoff | 가설(H-11). 합격: Job당 비종결 contract(PLANNED·ATTEMPT_ACTIVE·ADJUDICATION_PENDING) ≤ 2(13.4 열린 window 1 + 대기 1), 시간당 비실행 disposition 비율 = (`EXPIRED_UNLAUNCHED` + `COALESCED_INTO`) / expected ≥ 1 − period·C/ΣD − 10%p(period 1h: 1 − 60·10/1600 = 62.5% → ≥ 52.5%; `LEASE_BUSY`로 PLANNED 대기한 계약은 열린 window가 아니라 coalesce되지 않고 9.3의 `expires_at`에서 `EXPIRED_UNLAUNCHED`로 마감되므로 `COALESCED_INTO`는 직전 계약이 window를 가진 상태(ATTEMPT_ACTIVE/COMMIT_OBSERVED/ADJUDICATION_PENDING, 13.4)인 경우만 발생 — 별도 집계), Source 동시 세션 ≤ C 전 샘플, lateness는 발산해도 합격 — 대신 ρ 예측 vs 실측 오차 < 20% |
| SC-06 | Hold 해제 catch-up 폭주 | 500 Job Hold 1시간(그중 Hold 구간에 tick 없는 24h 주기 Job 100개 포함) 후 해제; 해제 핸들러 제출 중 Adapter 30% 실패 주입 | CATCHUP occurrence 수, `pinned_release`, Adapter 실패분 회수, NORMAL coalesce, 동시 SA, 202 응답 예상 소요 vs 실측 | 확정. 기대(14.2): CATCHUP(키 `job + hold_release_at`, 현재 ACTIVE release)는 Hold 기록 있는 400 Job에만 생성, 100 Job은 다음 정규 tick; 해제 트랜잭션 commit 직후 핸들러가 계약을 **poll 큐에 적재**(`resubmit_no` 채번 + `submission_in_flight = {resubmit_no, requested_at}` 선 commit, v1.2.3 14.2·9.3)하고, 실제 Run은 다음 shard sensor 평가가 `RunRequest`(run_key = \"{contract_id}:{resubmit_no}\", tags `contract_id`·`resubmit_no`·`dagster/priority`·`dagster/max_runtime`)로 생성, 실패분 `next_eligible_at=now()` → stale 루프가 `planned_stale_after` 면제로 회수; catch-up 중 NORMAL `COALESCED_INTO`. 합격: CATCHUP occurrence = 400(100 Job은 0), CATCHUP contract `pinned_release` = 해제 시점 ACTIVE, Adapter 실패분 120건(400 × 30%) 전부 `planned_scan_interval` 2회 내 재제출, CATCHUP 계약이 `ATTEMPT_ACTIVE`/`COMMIT_OBSERVED`/`ADJUDICATION_PENDING`(열린 window)인 Job의 NORMAL tick은 `COALESCED_INTO` 100%(CATCHUP이 `LEASE_BUSY`로 PLANNED 대기 중인 Job은 NORMAL PLANNED 공존 허용 — 단 같은 Job 동시 `ATTEMPT_ACTIVE` ≤ 1, 중복 0·누락 0, CATCHUP `EXPIRED_UNLAUNCHED` 건수 기록), 해제 후 동시 SA ≤ Source C, 예상 소요 vs 실측 오차 < 30%, 재개 `window.low` = Hold 전 마지막 CAS. 해제 202 응답의 `expires_before_completion` 값과 실제 CATCHUP `EXPIRED_UNLAUNCHED` 발생 여부 일치(예상 소요 > 최소 period일 때만 true); CATCHUP 만료 알림은 `hold_id` 단위 1건(Job 단위 0); 만료된 CATCHUP의 Job에서 후속 NORMAL `window.low` = Hold 전 마지막 CAS(누락 0, v1.2 9.3). No-Go 연결: 중복/누락, 절대 한도 초과, Hold 중 SA 제출 |
| SC-07 | Spark Operator 제출 처리량 (soak 종료 후) | SA 500건 동시 생성 | CR 생성→driver RUNNING p95/p99, workqueue 깊이, webhook 지연 | 가설(H-10) |
| SC-08 | Dagster·Control PostgreSQL 장기 증가 | SC-03 + 30일 seed | event_logs/runs 증가율, UI p95; **history retention 비용 외삽** — Dagster event_logs/runs + Control 이력 3종·`guard_result`·ledger·`attempt_timeline`의 일일 증가량(§2.5)으로 보관 30/90/365일 등급별 GB·인덱스 크기·§5.1 판정 쿼리 p95·UI 목록 p95를 외삽(22장 7번 확정 입력, §8.1 7번); purge 도구 처리량(Phase 2 게이트); `attempt_timeline` 90일 집계 전환의 절감분을 분리 | 확정(purge 행은 Phase 2; retention 외삽은 관측·22장 7번 입력) |
| SC-09 | K8s 객체 churn | SC-03 | 객체 생성/삭제/일, steady-state 객체 수, etcd db size, apiserver p99, TTL GC 동작 | 확정 |
| SC-10 | AIStor·Polaris | SC-03 | commit latency, conflict retry, PUT/GET IOPS, 용량 증가(GB/일), snapshot 수 | 가설(H-08) |
| SC-11 | compaction 동시성 + maintenance 보호집합(v1.2.2 fixture 보강) | Append-large 4개(stub, 1h 주기) 위에 `PARTITION_OR_FILESET` lease로 data compaction(30분급) + manifest rewrite를 매시 :20에 주입, MoR Merge-large 1개 포함; 같은 회차에 (a) `expire_snapshots`를 만료 제외 집합 계약 1건(`DQ_FAILED`로 열어 둔 Append 계약 — FI-23 fixture)이 있는 table에 실행, (b) `remove_orphan_files(older_than = orphan_min_age)`를 chunk 2 진행 중(미commit data file 보유) attempt가 있는 table에 실행, (c) `older_than < orphan_min_age`·snapshot retention `< RPO + RTO + margin`인 maintenance JobSpec publish 각 1회, (d) WAP 경로 Critical table 1개에서 attempt branch(`branch_x`)가 열린 채 `expire_snapshots` 실행(18장 branch ref 보호); (e) compaction 1건의 `PARTITION_OR_FILESET` lease를 driver heartbeat 차단으로 TTL 만료시켜 Control이 SA 삭제·pod 부재 확인으로 `RECLAIMED` 처리한 뒤, 보관해 둔 그 compaction commit을 프록시 `hold_then_apply`(FI-50)로 착지 | ingestion lateness 변화, compaction 완료 시간·기아, conflict retry, `use-starting-sequence-number` on/off, Adjudication의 maintenance snapshot 제외 정확도; (a) 제외 집합 계약의 `base_snapshot` 이후 snapshot 만료 0(같은 table의 그 밖 만료 대상 ≥ 1 — 양성 증거); (b) 진행 중 attempt의 파일 삭제 0 ∧ 후속 CAS 성공·`FINALIZED`(orphan 후보 목록에 그 attempt 경로 ≥ 1 — 양성 증거); (c) 422 `VALIDATION_FAILED` 2건(`rule_id` 각각 — 18장 하한); (d) branch ref 삭제·만료 0 ∧ 계약 종결 후 다음 회차에 정리 1; (e) `lease_state_history` maintenance lease `EXPIRING → RECLAIMED` 1 ∧ 그 `RECLAIMED` 전이가 head-settle로 target lease row에 기록한 `reclaimed_head_snapshot_id` non-null(v1.2.3 18장 — 유효성 판정은 lease 구간 시각이 아니라 이 head 기준이다: snapshot `timestamp-ms`는 writer가 쓴 값이라 쓸 수 없다) ∧ 착지 snapshot이 그 head의 ancestor-or-self가 **아님**(양성 증거: 착지 snapshot의 `sequence_number` > reclaimed head) → 다음 ingest의 `chunks/{n}:commit` 또는 `chunks:begin(1)`에서 `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 정확히 1 ∧ 그 snapshot을 자기 chunk로 CAS한 row 0 ∧ fencing generation·commit 직전 lease 재검증 호출 0(18장 v1.2.2 — 사후 분류가 설계 선택이며 fencing으로 격상하지 않는다) ∧ 대조군: 정상 완료 compaction snapshot(=`RECLAIMED` row 없는 lease)은 제외 집합 유지로 `RECONCILIATION_REQUIRED` 0 | 가설(H-07) + 확정(보호집합 (a)(b)(d)·하한 422 (c) — §7 `orphan_min_age` 행과 일치) |

### 3.4 장애 주입

각 항목은 **주입(절차) → 기대 동작(v1.2 절) → 합격 조건(쿼리 가능, 0건 조건은 양성 증거 동반) → No-Go 연결**로 기술한다. 모든 FI는 soak 중 실 부하 위에서 1회 이상(정시 burst 직전·직후 각 1회 포함) 수행하며, FI는 동시에 1건만 수행한다(특히 Hold·Global Hold를 만들거나 전제하는 FI-05·FI-05b·FI-13·FI-16·FI-18·FI-28·FI-30·FI-32·FI-33은 서로 겹치지 않게 — 예외는 FI-49뿐이며 Hold 겹침 자체가 주입이다). FI별 시작·종료 시각을 §8.2에 기록한다.

**하네스 불변식(FI 프록시)**: 모든 FI 프록시 변형은 **429와 `Retry-After`를 실은 503을 반환하지 않는다**. Iceberg `HTTPClient`가 429는 메서드 무관, 503 + `Retry-After`는 조건부로 같은 POST를 자동 재전송하므로(v1.2.3 13.1), 이를 허용하면 프록시가 시험이 의도하지 않은 **5번째 주입 창**을 만든다. 위반이 관측된 회차는 무효로 기록하고 재수행한다. 같은 이유로 운영 배포에서도 Spark ↔ Polaris 사이에 rate-limiting 프록시·LB를 두지 않는다(22장).

**설정 단언(FI-14·FI-50·H-06 공통)**: Polaris `polaris.config.rollback.compaction.on-conflicts.enabled = false`(기본값)임을 회차 전후로 확인해 §8.2 '실행 환경'에 기록한다 — `true`면 assert 실패를 롤백으로 통과시켜 FI-50 (iii)·(iv)의 409 기대값과 대조군의 '착지 실패' 판정이 무너진다(v1.2.3 13.1).

| ID | 주입 | 기대 동작 (v1.2) | 합격 조건 | No-Go 연결 |
|---|---|---|---|---|
| FI-01 | Daemon 강제 종료 25분 (정시 :55~:20, 1h 주기 Job 500개 포함) | 복구 후 최신 tick 1건만 평가(9.3), Full coalesce, Incremental `[last_wm, fence)` | (a) 복구 시간 = daemon 프로세스 재기동 시각 → (모든 schedule의 다음 tick SUCCESS ∧ :00 expected occurrence를 가진 Job 전부가 contract `ATTEMPT_ACTIVE` 이상 또는 설명된 disposition) 까지 ≤ 30분(내부 5분); (b) 양성 증거: :00 expected tick 집합 크기 N ≥ 500이고 occurrence row N개 존재; (c) 각 contract에 Dagster run(tag contract_id) 중 non-terminal 동시 존재 ≤ 1, attempt 수 = 1, SA 수 ≤ 1(`attempt_state_history`·K8s 감사 로그); (d) 복구 직후 Source별 동시 token ≤ C(절대 한도 초과 0) ∧ `guard_result = LEASE_BUSY` ≥ 1(양성 증거), 초과분은 backoff로 대기; (e) 누락 = expected − (EXECUTED ∪ 설명된 disposition) = 0 | 중복/누락, 30분 초과 복구 |
| FI-02 | Daemon 종료 **2h 15분**(10:10~12:25, 1h 주기 Job 200개 + 24h 주기 Job 50개(cron 11:00) 대상) — 마지막 heartbeat 10:10 + `daemon_heartbeat_gap_seconds`(120분) = 12:10에 자동 기동되며 이 시각에 expected tick 11:00·12:00이 모두 지나 있다. 별도 1회는 70분 종료(10:10~11:20, `daemon_heartbeat_gap_seconds` 미만 — 자동 기동 없음, 11:00 tick은 daemon 자체 catch-up) 후 `POST /v1/schedule-gap-recoveries {scope: {job_ids}, dry_run: true}` → 같은 본문 `dry_run: false` 2회(멱등) → `range.to − from > gap_recovery_max_range_seconds`인 요청 1회; (g) **변형(v1.2.3)**: 자동 Gap Recovery operation이 `IN_PROGRESS`인 동안 그 operation을 소유한 scheduler 인스턴스를 kill해 다른 인스턴스가 인수하도록 한다(9.3 owner failover — `completed_step` 다음 단계부터 재수행) | Gap Recovery(12:10)가 expected tick 11:00·12:00을 전개: 12:00 NORMAL create-or-get, 11:00 `COALESCED_INTO(12:00 contract)`, 12:00 contract PLANNED+binding 없음이면 Adapter launch(QUEUED, daemon 복귀 후 dequeue)(9.3). 12:25 daemon 복귀 시 Dagster 자체 catch-up tick(12:00)은 같은 row를 get하고 `launch=false`(9.1). 24h Job(11:00)은 expected tick 1개 → NORMAL 실행. 70분 케이스 자동 기동 0건 | (a) 1h Job별 expected tick 2개·24h Job 1개 전부 occurrence row 존재; (b) 12:00 occurrence의 contract에 non-terminal run ≤ 1, attempt 1, SA 1, 12:00 Dagster tick의 RunRequest 0(`launch=false`); (c) 11:00 disposition = `COALESCED_INTO`, contract `VOID(COALESCED)`; (d) Gap Recovery 기동 시각 − (마지막 heartbeat + gap) ≤ 60초(AuditEvent) ∧ 기동 시각 ≥ 12:00; (e) 복구 직후 5분 창에서 Source별 동시 token ≤ C; (f) 70분 케이스: 자동 기동 0건(AuditEvent actor=SYSTEM 0); dry_run은 expected − 설명된 누락 목록만 반환·쓰기 0; 수동 기동 1회차 202 `operation_id` → `GET /v1/operations/{id}`: expected tick 수 기록, 11:00 occurrence는 이미 존재하므로 새 row 0·launch 0; 2회차는 진행 중이면 200 기존 `operation_id`, 완료 후면 새 row 0·새 Run 0; 범위 초과 요청 400; operation 1건당 Outbox `schedule gap recovery completed` 1건(actor=운영자; 자동 경로는 actor=SYSTEM으로 12:10 케이스에서도 1건); (g) leader kill 변형: 인수한 인스턴스가 같은 `operation_id`로 재수행 ∧ operation 종결 1회 ∧ `completed_step` 이전 단계의 재실행 0 ∧ occurrence 중복 생성 0 ∧ launch된 Run 중복 0 ∧ Outbox `schedule gap recovery completed` 정확히 1건(재수행이 새 `event_id`를 만들지 않음) | 중복/누락 |
| FI-03 | Code Location 종료 / reload 실패 / 정시 tick 중 reload | 해당 shard tick 실패 → `max_tick_retries` 재시도, 그 외 shard 무영향, VERIFIED 실패 시 직전 bundle fallback(6.2) | 다른 shard tick 실패 0(양성 증거: 해당 shard tick FAILURE ≥ 1), fallback 후 schedule RUNNING 대조 통과, 누락 0 | 30분 초과 복구 |
| FI-04 | Dagster PostgreSQL failover | Run·tick이 재시도로 회복 | 데이터 중복/누락 0, `run_status_sensor` 이벤트 유실 0(Idempotency-Key 재전송 ≥ 1 기록) | 중복/누락 |
| FI-05 | Control PostgreSQL failover + PITR 복구(의도적 10분 과거 시점) — **soak 종료 후** 수행, failover 직전 Control DB 논리 dump(ground truth) 확보; dump 구간에 snapshot 없는 0-row chunk CAS ≥ 1이 포함되도록 회차를 고른다 | 5.4 재동기화 Runbook(**conservative recovery** — v1.2.2 명문화): Global Hold → snapshot summary·run tag로 contract/attempt/watermark 재구성. 0-row CAS는 snapshot 증거가 없어 재구성되지 않으며 watermark는 그만큼 **후퇴만** 한다(중복·누락 아님, 재추출 비용) — FI-05b (c)와 같은 의미; 미발행 Outbox는 16.4 결정론 `event_id`로 재생성 | 재구성된 contract 상태·lease 집합 = dump + 이후 Iceberg snapshot summary로 재현한 기대 집합과 불일치 0(스크립트 diff, exact); 재구성 watermark = dump 기대 집합에서 **`committed_snapshot_id` 없는 0-row CAS row를 제외**한 값과 일치 ∧ Job별 재구성 wm ≤ dump wm(전진 0, 후퇴 방향만 — 양성 증거: 0-row CAS 포함 Job의 후퇴 ≥ 1) ∧ 해제 후 CATCHUP이 후퇴 구간을 다시 읽어 §3.2 비교 A 차이 0; Global Hold 구간 SA 생성 0 ∧ 그 구간 tick ≥ 1(`SKIPPED_BY_HOLD`), 재동기화 소요 ≤ RTO 30분; 재생성 Outbox의 `event_id` = dump의 미발행 row `event_id`(consumer 중복 0) | 중복/누락, Hold 중 SA 제출 |
| FI-05b | FI-05 변형 — **soak 중** 수행(soak 5일차 정시 burst 직후, §3.5 3번): PITR 시점을 5분 과거로 잡되 그 5분 안에 (i) chunk 2 진행 중인 Append-large 계약 2개(SA·driver 생존, 복구 직후 old driver가 chunk 2 commit을 계속 진행하도록 둔다), (ii) 0-row chunk CAS 1건, (iii) 미발행 Outbox row ≥ 3, (iv) `FAULT_DELAY_FINALIZE`로 finalize 직전에 멈춘 계약 1개를 포함시킨다. failover 직전 논리 dump 확보는 FI-05와 같다 | 5.4 resync 프로토콜 5단계 순서: (1) Global Hold `DRAIN` + `launchRun` 0 → (2) 비종결·미인지 attempt 전부 fencing 단계(`WRITER_FENCED`) → (3) catalog head 안정 후 재구성 + contract별 13.2 Adjudication(actor `RESYNC`) → (4) lease 재구성 ∧ 모니터 세션 `GV$SESSION` ETL 세션 0 → (5) 해제 → CATCHUP(14.2). 복구 Control이 모르는 `(contract, attempt)` 호출은 `412 ATTEMPT_FENCED {fence_reason: RESYNC}`(17장) | (a) 순서: `contract_state_history`·`lease_state_history`·K8s 감사 로그 시각으로 (1) Hold 생성 < (2) 마지막 SA 삭제·pod 부재 확인 < (3) 첫 `actor=RESYNC` row < (4) lease `GRANTED` 재구성 완료 ∧ stub `GV$SESSION` ETL 세션 0 샘플 < (5) Hold 해제 — 역전 0; (b) (i)의 old driver commit: 같은 `(contract, chunk)`에 Iceberg snapshot 2개 0, fencing 전 착지분은 재구성 ledger에 `actor=RESYNC` row로 정확히 1회, 그 chunk에 대한 old Run Pod의 `chunks/{n}:commit` 호출이 `412 ATTEMPT_FENCED {fence_reason: RESYNC}` ≥ 1(양성 증거); (c) (ii) 0-row CAS 소실: 재구성 watermark가 `high_k` 이전으로 **후퇴만** 하고(전진 0) CATCHUP이 같은 구간을 다시 읽어 §3.2 비교 A 데이터 차이 0; (d) (iii) consumer sink `event_id` 집합 = dump 기준 기대 집합, 중복 0·유실 0; (e) (iv) 계약은 Adjudication verdict `COMMIT`으로 `FINALIZED`, attempt 증가 0; (f) Hold 구간 SA 생성 0 ∧ `launchRun` 0 ∧ 그 구간 tick ≥ 1(`SKIPPED_BY_HOLD`), 재동기화 소요 ≤ RTO 30분, 해제 후 CATCHUP이 (i)(ii) Job을 포함; (g) FI-05 합격 조건 전부 | 중복/누락, Append 이중 commit, Hold 중 SA 제출, `WRITER_FENCED` 확정 없는 새 attempt |
| FI-05c | FI-05 변형(**soak 종료 후, FI-05 직후** — 같은 dump fixture 재사용, §3.5 5번): 5.4 resync 각 단계 (1)~(4)의 완료 트랜잭션 commit 직후 Control 프로세스 SIGKILL 4회(단계마다 1회) + (3) 재구성 **중간**(전체 계약의 절반에 `actor=RESYNC` row 기록 뒤) 1회 = 총 5회 재사망, 매번 Control 재기동 | v1.2.2 5.4: resync는 operation row(kind `RESYNC`)에 `completed_step`을 단계 완료 트랜잭션에서 기록하고, 재기동한 Control은 reason `RESYNC`인 open Global Hold가 있으면 새 resync를 만들지 않고 `completed_step + 1`부터 재개한다; (2) fencing·(3) 재구성(create-or-get)·(4) lease 재구성은 멱등이라 단계 중간 재사망은 같은 단계 재실행으로 닫힌다(conservative recovery, journal 없음) | (a) 재기동 5회 모두 새 `RESYNC` operation row 0(기존 `operation_id` 재개) ∧ `completed_step` 단조 증가 ∧ 완료된 단계의 재실행 0(AuditEvent 단계 시작 기록 = 단계당 1회, (3) 중간 재사망 케이스만 2회); (b) 재구성 row당 `actor=RESYNC` history row 정확히 1(중간 재사망 뒤 재실행이 이미 재구성된 row에 row를 더하지 않음 — 양성 증거: 재실행 (3)의 skip 건수 ≥ 1); (c) 단계 순서 역전 0 ∧ Hold 해제 전 SA 생성 0 ∧ `launchRun` 0(FI-05b (a)(f)와 같은 쿼리); (d) Outbox consumer sink `event_id` 중복 0·유실 0(재생성 id 동일 — 16.4 결정론); (e) FI-05 합격 조건 전부, 누적 재동기화 소요(재사망 5회 포함) ≤ RTO 30분 | 중복/누락, Hold 중 SA 제출, `WRITER_FENCED` 확정 없는 새 attempt |
| FI-06 | Run Pod OOM (`FAULT_KILL_AFTER_RECEIPT=2`, expected_chunk_count ≥ 4인 Append-large). 전제: `chunk_proceed_timeout_seconds` > `reattach_grace_seconds` + 탐지 지연(§7); (c) **변형(v1.2.3)**: 재결합한 run을 chunk 3 receipt 직후 다시 kill해 **같은 attempt에 재결합을 2회** 유발한다(새 attempt 없음) | `run_status_sensor` FAILURE → `ADJUDICATION_PENDING(RUN_WORKER_LOST)`, SA running이면 fencing 보류 + `reattach_deadline` 기록, `run_retries` Run의 Guard 2번이 반입된 terminal 사실을 확인 → 3.2 재결합(rebind, 직전 상태 복귀, 새 attempt 없음), Pipes 재생으로 chunk 2 `chunks/{2}:commit` 대행(10.1, 10.2) | (a) 재결합 1회(`rebind_count = 1`, `dagster_run_ids` 길이 2), `reattach_deadline` 전 fencing 0; (b) kill 시점 ledger 최대 chunk_no = 1(양성 증거), 재결합 후 chunk 2 ledger row actor = 재결합 run; (c) Iceberg snapshots 중 `etl.chunk_no=2` ∧ 해당 contract = 정확히 1개(이중 commit 0); (d) 이후 chunk 정상, FINALIZED; (e) 재결합 소요(`t_terminal_event → guard`) 기록; (f) (c) 변형: `rebind_count = 2` ∧ `dagster_run_ids` 길이 3 ∧ `attempt_no` 불변(새 attempt 0) ∧ 두 번의 재결합이 만든 Outbox 이벤트 2건의 `event_id` **상이**(transition key에 `rebind_count` 포함 — 16.4, 양성 증거) ∧ 같은 chunk의 ledger `cas_applied` row 중복 0 ∧ FINALIZED | Append 이중 commit, fencing 없는 attempt |
| FI-06b | FI-06과 같은 Append-large(expected_chunk_count ≥ 4)에서 (a) chunk 2 진행 중(chunk 1 CAS 완료) Dagster write 인스턴스(SSO proxy 경유)에서 Run terminate; (b) 별도 계약에 Job별 `max_runtime_seconds`를 예상 chunk 시간 × 0.5로 두어 tag `dagster/max_runtime` 초과 유도; 각 3회 | `run_status_sensor` CANCELED 반입 → 원인 분류 `ADJUDICATION_PENDING(OPERATOR_CANCELLED)` / `(MAX_RUNTIME_EXCEEDED)`, `run_retries` Run 없음·`reattach_deadline` 기록 없음, 즉시 SA 삭제 + pod 부재 확인(fencing 단계) → `WRITER_FENCED` → Adjudication `PARTIAL_COMMIT`(chunk 1 CAS 유지) → 자동 attempt 0, 운영자 RETRY/ABORT 대기 + `adjudication_pending_alert_after` 알림(10.2 반입 표 CANCELED 행, 5.1 `run_retries` v1.2.1) | (a)(b) 각 3/3: `attempt_state_history` TERMINAL_OBSERVED → FENCED → ADJUDICATED 1건 ∧ reason = `OPERATOR_CANCELLED` / `MAX_RUNTIME_EXCEEDED`(`RUN_WORKER_LOST` 0) ∧ Dagster `run_retries` 재시도 run 0 ∧ `rebind_count = 0` ∧ attempt 수 = 1(자동 증가 0) ∧ `reattach_deadline IS NULL` ∧ SA 삭제 AuditEvent actor=Control 1건·fencing 시각 − terminal 반입 시각 ≤ `adapter_sync_timeout_seconds` + 60초 ∧ 알림 1건; (a) proxy 감사 로그에 terminate actor 1건 기록; (b) run 소요 ≥ `dagster/max_runtime` 기록; 양성 증거: 운영자 RETRY 202 → attempt 2 Guard 응답 `window.low` = chunk 1 high ∧ chunk 1 snapshot 정확히 1개 ∧ 최종 FINALIZED; 대조군 FI-06(FAILURE)은 재결합 1회 | fencing 없는 attempt, Append 이중 commit |
| FI-07 | Run Pod 사망(`FAULT_KILL_AFTER_RECEIPT=1`, expected_chunk_count ≥ 3) + 재결합 차단(`run_retries` 비활성 또는 FAILURE sensor 정지로 재시도 Run 미발생) → `reattach_deadline`(360초) 도달 → Control이 SA 삭제·pod 부재 확인으로 `WRITER_FENCED`. 변형: Run Pod는 살려 두고 `FAULT_HOLD_PROCEED=1`로 `proceed` 발신만 차단 → driver가 `chunk_proceed_timeout_seconds`(480초) 후 `proceed_timeout{last_chunk_no}` exit 1 → SA FAILED → Run Pod `attempt-failure {SPARK_TERMINATED_WITHOUT_RECEIPT}`(10.1, 10.2) | 본 케이스: `WRITER_FENCED` → Commit Adjudication → `PARTIAL_COMMIT`, reason `RUN_WORKER_LOST` 유지 → `max_auto_attempts` 내 자동 새 attempt가 `window.low = 마지막 CAS` 재개(13.2); 변형: `PARTIAL_COMMIT` 운영자 RETRY | 본 케이스: fencing 주체 = Control(SA 삭제 AuditEvent) ∧ `rebind_count = 0` ∧ attempt 2 chunk 1의 `window_low` = attempt 1 마지막 CAS 값(attempt별 chunk 재번호 — 13.4·§5.1 (3)(a); 접두 구간은 `(contract_id, attempt_no, chunk_no)`로 식별, v1.2.2 정정) ∧ ledger `cas_applied` row의 `cas_at` 순 `window_low = prev.window_high` 연속(§5.1 (4)) ∧ staging manifest 재사용(11.5) ∧ `attempt_state_history`에 FENCED → ADJUDICATED 기록; 변형: Pipes 로그에 `proceed_timeout{last_chunk_no}` 1건 ∧ Control SA 삭제 0 ∧ RETRY 후 접두 구간 일치 | fencing 없는 attempt |
| FI-08 | Run Pod 사망 직후 `run_retries` Run이 sensor보다 먼저 도착: FAILURE sensor 정지 → `FAULT_KILL_AFTER_RECEIPT=1` → run_retries Run의 Guard 완료 확인 → sensor 재개. 10회 반복 | Guard 2번이 Adapter를 동기 조회(`adapter_sync_timeout_seconds`)해 terminal 확인 후 재결합(10.2). 뒤늦은 sensor 이벤트(구 run_id)는 "불일치(이전 binding) 무시" | `ATTEMPT_ALREADY_BOUND` 오거부 0, 재결합 성공 10/10, Guard AuditEvent에 `adapter_sync(runId)=FAILURE` 10건, 뒤늦은 sensor 이벤트로 인한 상태 전이 0 | — |
| FI-09 | zombie Run Pod 생성: K8s Job을 `kubectl delete job --cascade=orphan`으로 지워 run_monitoring이 FAILED로 표시하되 pod 프로세스는 살려 둠. `FAULT_ZOMBIE_CALLS=1`로 (A) `dagster-terminal-event` 반입 확인(`attempt.terminal_ingested_at IS NOT NULL`) **후**·재결합 **전** 창에서, (B) 새 run 재결합 **후**, 각각 `chunks:begin`·`chunks/{n}:commit`·`finalize`를 원래 `(contract_id, attempt_no, dagster_run_id)`로 호출; (A0) 대조군: 반입 **전** 1회(`run_status_sensor` 일시 정지로 반입 지연). 5회 반복 | (A): `412 ATTEMPT_FENCED {fence_reason: RUN_TERMINAL}`(10.2 소유권 검사 조건 (2)); (B): `412 ATTEMPT_FENCED {fence_reason: REBOUND}`; zombie는 driver에 어떤 Pipes 메시지도 보내지 않고 SUCCESS 종료; (A0): 반입 전 호출은 정당한 소유자 호출로 200 정상 처리(SA는 아직 그 Run Pod 소유) — 이후 재결합 Run Pod가 `low = 마지막 CAS 값`부터 이어감(10.2 'terminal 반입과 경합한 chunk commit' (a)); 재결합 시 `terminal_ingested_at` NULL 초기화 | (a) (B) 호출 전부 412 `fence_reason=REBOUND`(≥ 3 × 5회); (b) (A) 호출 전부 412 `fence_reason=RUN_TERMINAL`(≥ 3 × 5회) ∧ ledger row 생성·CAS·finalize 성공 0건 ∧ 지표 `attempt_fenced_total{fence_reason}` 증가 = 호출 수 ∧ Kafka 알림 0(감사 이벤트만); (b′) (A0) 호출 200 ∧ 그 chunk ledger row 1개(재결합 후 중복 0) ∧ 재결합 후 `terminal_ingested_at IS NULL`; (c) 살아 있는 attempt의 driver가 zombie `stop`으로 종료된 건 0(SA RUNNING 유지, Pipes 로그에 zombie run_id 발신 `stop` 0); (d) 대조군: 정상 Run Pod 호출은 200 | Append 이중 commit |
| FI-09c | terminal event와 late safety evidence 순서 역전(§3.5 2번, FI-09 직후): FI-16 절차(stub 비밀번호 변경, `FAILED_LOGIN_ATTEMPTS=10`)로 driver precheck ORA-01017을 유발하되, Run Pod의 `attempt-failure {CREDENTIAL_FAILURE}` 발신을 훅 `FAULT_DELAY_ATTEMPT_FAILURE=30`(PoC 빌드 한정 — Run Pod는 FAILURE로 exit한 뒤 30초 후 호출)으로 지연해 `run_status_sensor` FAILURE 반입이 먼저 오게 함. 5회. 변형: `precheck_failure{SOURCE_ROLE_MISMATCH}`(FI-13(b) 절차) 1회 | 반입이 먼저 → `ADJUDICATION_PENDING(RUN_WORKER_LOST)` + SA 상태 조회(10.2 반입 표); 지연 도착한 `attempt-failure`는 소유권 검사 (2)로 `412 ATTEMPT_FENCED {fence_reason: RUN_TERMINAL}`(증거 전용 endpoint는 두지 않는다). SA terminal(또는 미생성) 확인 → `WRITER_FENCED` → Adjudication 서비스가 Pipes 경로(`pipes/{contract_id}/a{attempt_no}/messages`, 10.1)를 재생해 `precheck_failure{reason}`·receipt `exception_class`를 읽고 verdict reason을 `CREDENTIAL_FAILURE`로 **확정**(v1.2.1 13.2) → 같은 트랜잭션에서 credential breaker 개방 + Source `HOLD_NEW` + Outbox `source credential failure`(6.2 — breaker 입력에 'Adjudication이 읽은 precheck_failure' 포함). reason이 `RUN_WORKER_LOST`로 남아 `max_auto_attempts` 예산을 소비하는 경로 없음 | (a) 5/5 `attempt-failure` 응답 412 `RUN_TERMINAL`(양성 증거) ∧ `attempt_state_history`의 `FENCED → ADJUDICATED` row `reason = CREDENTIAL_FAILURE`(`RUN_WORKER_LOST` verdict 0); (b) `maintenance_hold(reason=CREDENTIAL_FAILURE, credential_revision_id=구 revision)` 1건 ≤ ADJUDICATED + 10초 ∧ 자동 attempt 0(`max_auto_attempts` 소비 0) ∧ stub 실패 로그인 ≤ 1/attempt ∧ Hold 중 `SKIPPED_BY_HOLD` ≥ 1; (c) Adjudication AuditEvent에 Pipes 재생 메시지 수 ≥ 1 ∧ `precheck_failure` 1건 기록; (d) 변형: verdict reason `SOURCE_ROLE_MISMATCH` → `CANCELLED(SOURCE_ROLE_MISMATCH)` + Source Hold(FI-13(b)와 같은 종결) | Adjudication 없는 attempt, 중복/누락(자동 attempt 0) |
| FI-10 | stub apply lag 12분(threshold 10분)을 tick 시점에 주입(보류 row N ≥ 1,000건, 주입 API 응답으로 확정); 변형: lag를 `period + 10분` 유지 | Guard 6번 `SOURCE_LAG_EXCEEDED`, Spark 미제출, PLANNED + backoff, lag 회복 후 stale 루프 재제출(11.3, 9.3); 변형: `expires_at` 초과 → `EXPIRED_UNLAUNCHED` + 다음 회차가 덮음 | (a) 회복 후 **첫 FINALIZED contract**까지 Iceberg에 적재된 보류 row = N; (b) lag 구간 `guard_result = SOURCE_LAG_EXCEEDED` ≥ 1이고 그 contract `window_range` 비어 있음·`visible_scn IS NULL`(fence 없는 예약 0); (c) 변형: occurrence `EXPIRED_UNLAUNCHED` + contract `VOID(EXPIRED_UNLAUNCHED)` 1건, 다음 회차 `window.low` = 직전 watermark, (a) 방식으로 누락 0; (d) cancel 변형: lag 대기 중 PLANNED 계약 1건에 `cancel {reason}` → contract `VOID(OPERATOR)` + occurrence `CANCELLED(actor, reason)`(6.2·17장) + AuditEvent 1건, 다음 회차가 `[current_watermark, fence)`로 누락 0 — §6 운영 절차 체크리스트의 cancel 증거 | watermark gap |
| FI-11 | `DATUM_TIME` stale(60초) / 모니터 세션 kill(stub API) — Guard 시점 각각, ZERO_GAP Job과 BEST_EFFORT Job에 모두 주입(stub의 `visible_scn` 조회는 정상); 그리고 **attempt 실행 중**(chunk 2 추출 중 apply lag를 threshold 초과로 재상승, ZERO_GAP·BEST_EFFORT 각 1건) | Guard(ZERO_GAP): `DATUM_TIME` stale → `SOURCE_LAG_EXCEEDED`, 모니터 세션 kill → `FENCE_UNAVAILABLE`, watermark 전진 0. Guard(BEST_EFFORT, `DATUM_TIME` stale 또는 lag 조회만 실패): 통과 + `contract.confidence = DEGRADED`·`confidence_reason` 기록, Outbox `degraded confidence`(11.3, 10.2 6번); 모니터 세션 kill은 등급 무관 `FENCE_UNAVAILABLE`. 실행 중: lag 재조회 없음 — CAS 정상, 상태 전이 없음(11.3, 13.4) | ZERO_GAP: `guard_result ∈ {SOURCE_LAG_EXCEEDED, FENCE_UNAVAILABLE}` ≥ 1 ∧ 그 contract `window_range` 비어 있음 ∧ `confidence=DEGRADED`인 ZERO_GAP 계약 0; BEST_EFFORT: `guard_result=OK` ∧ `confidence=DEGRADED`·`confidence_reason` 기록 ∧ ledger row `confidence=DEGRADED` 100% ∧ Outbox `degraded confidence` 1건(Source 집계); 실행 중 변형(각 1건): `chunks/{n}:commit` 거부 0 ∧ CAS = expected_chunk_count ∧ FINALIZED ∧ stub 쿼리 로그에서 Guard 이후 lag/DATUM_TIME 조회 0(양성 증거: 주입 API 응답으로 lag 상승 확인) ∧ 다음 회차 Guard는 ZERO_GAP `SOURCE_LAG_EXCEEDED` / BEST_EFFORT DEGRADED | watermark gap |
| FI-12 | Job A: ZERO_GAP, Source capability `max_commit_minus_watermark_seconds=1200`·`bound_kind=ENFORCED`·`fence_time_witness=HEARTBEAT_TABLE`·watermark 컬럼 `DB_TRIGGER`·`not_null`·`updated_on_every_change`·`delete_semantics ∉ {NONE_DECLARED, CDC_LATER}`(v1.2.2 7.2 5번 통과), overlap 30분(publish 통과). Job B: BEST_EFFORT, overlap 10분(`bound_kind=OBSERVED` Source). 두 Job 1h 주기, window high = 10:00 근방. stub late-commit API로 `UPDATE_DT = 09:48`, `commit_at = 10:05`인 row 100건 주입(commit − UPDATE_DT = 1020초 ≤ bound 1200초라 Job A Source의 stub ENFORCED 트리거 에뮬레이션이 허용; 10:00 Guard의 `visible_scn`에는 미포함; Job B overlap 10분의 extract_low = 09:50이 09:48을 덮지 못함). 대조군: Job A를 overlap 10분으로 publish 시도 | Job A: 11:00 회차 최초 chunk 1의 extract window `[10:00−30분, …)` anti-join이 100건 적재(12.2 extract window, FI-47) → 누락 0. Job B: 11:00 회차 overlap 10분이 09:40을 덮지 못해 누락 → Data Reconciliation Audit(12.3)이 탐지·repair MERGE | (a) Job A: 11:00 FINALIZED 후 Iceberg에 100건 존재, PK 중복 0, receipt `overlap_recovered_rows ≥ 100`; (b) Job B: 11:00 FINALIZED 직후 100건 부재(양성 증거), audit 1주기 후 100건 존재, audit ledger의 `repair_contract_id` FINALIZED, 탐지 지연 ≤ audit 주기; (c) 대조군: Job A overlap 10분 validate·publish 모두 `422 VALIDATION_FAILED` ∧ `violations[0].rule_id = OVERLAP_BELOW_MINIMUM`·`actual = 600`·`computed_minimum = 1200 + safety_lag + clock_skew`·`inputs`에 SourceCapability·Envelope id ∧ JobSpecVersion·release 미생성; Job B publish는 202 + `warnings[]`에 `OVERLAP_BELOW_MINIMUM`(`computed_minimum = null`) 1건(v1.2 17장) | watermark gap(ZERO_GAP만), 데이터 차이(Job B의 audit 전 gap은 설명된 차이) |
| FI-13 | stub role 전환(PHYSICAL STANDBY → PRIMARY) — (a) Guard 시점, (b) driver 추출 직전("다음 세션부터 PRIMARY"). (a)의 거부 계약에 **24h 주기 Job 1개**를 포함시키고 Hold를 10분 안에 release한다 | (a) Guard `SOURCE_ROLE_MISMATCH` → `VOID` + occurrence `REJECTED_AT_GUARD` + Source `HOLD_NEW`; (b) driver 재검증 실패 → `precheck_failure` → `attempt-failure` → `WRITER_FENCED` → `CANCELLED(SOURCE_ROLE_MISMATCH)`(11.3, 10.2); release 시 `REJECTED_AT_GUARD(SOURCE_ROLE_MISMATCH)` Job도 14.2 impacted 집합이므로 CATCHUP 생성(v1.2.1 14.2) | (a) stub 쿼리 로그에서 `role_at_query = PRIMARY`이고 SQL이 `V$DATABASE` 역할 검증이 아닌 건 0, 단 검증 쿼리 ≥ 1(양성 증거); (b) Guard 변형: `guard_result = SOURCE_ROLE_MISMATCH` 1, contract `VOID(SOURCE_ROLE_MISMATCH)`, SA 0; (c) driver 변형: Pipes `precheck_failure` 1 → `CANCELLED(SOURCE_ROLE_MISMATCH)`, Iceberg snapshot 0; (d) `maintenance_hold` row(scope=SOURCE, mode=HOLD_NEW, reason=SOURCE_ROLE_MISMATCH) 1건, Outbox `source role mismatch` 1건; (e) Hold 중 같은 Source의 다음 tick `SKIPPED_BY_HOLD` ≥ 1; (f) role 복귀 후 release: `REJECTED_AT_GUARD` 기록만 있는 24h Job에 `CATCHUP` occurrence 1건 ∧ 그 CATCHUP이 `FINALIZED*`(양성 증거: 그 Job의 Hold 구간 `SKIPPED_BY_HOLD` 0 — tick 없음) ∧ 다음 정규 NORMAL까지 해당 Job의 lateness ≤ `freshness_slo`(24h class) | Primary 자동 fallback, 중복/누락 |
| FI-14 | Polaris REST 5xx during commit — 프록시 변형 4종 × 5회: `forward_then_500`(기대 COMMIT), `drop_then_500`(기대 NO_COMMIT, 운영자 대기), `forward_then_504`(**(≥1.11) 단발 reconcile 성공 / (≤1.10) 즉시 실패 후 Adjudication** — pinned 버전을 §8.2에 기록), `strip_etl_keys`(기대 RECONCILIATION_REQUIRED) | `CommitStateUnknownException` → receipt `committed=null` → `attempt-failure {COMMIT_STATE_UNKNOWN}` → fencing → 구간 판정(13.2). 단 `forward_then_504`만은 **pinned 버전에 따라 갈린다**(v1.2.3 13.1) — ≥1.11.0은 `reconcileOnSimpleUpdate`가 1회 refresh로 조용히 성공 처리(update set이 `AddSnapshot` + 같은 snapshot의 `SetSnapshotRef`뿐일 때만)해 정상 `FINALIZED`로 끝나고, ≤1.10.x는 재시도 없이 즉시 실패해 Adjudication으로 간다 | 변형별 verdict가 Iceberg `snapshots` 실재와 20/20 일치, 새 attempt 자동 생성 0(attempt 수 = 1), `RECONCILIATION_REQUIRED` 오탐(정상 snapshot에 대해) 0, `adjudicate` 수동 트리거 1회 이상 사용 | Adjudication 없는 attempt |
| FI-15 | Iceberg commit 직후 driver 종료(receipt 미전송, `FAULT_EXIT_AFTER_COMMIT=<n>`) — (a) 마지막 chunk, (b) 중간 chunk; (b′) FI-22(a)의 전제 계약: (b) 절차를 별도 계약 1건(`FAULT_EXIT_AFTER_COMMIT=1`, expected_chunk_count ≥ 2)에 추가 수행하고 RETRY 없이 `expires_at`까지 방치 | `SPARK_TERMINATED_WITHOUT_RECEIPT` → fencing → S′ 구간 검색: (a) `COMMIT` → finalize 대행 → `FINALIZED`; (b) `PARTIAL_COMMIT` → 누락 chunk의 ledger·CAS 대행 → `ADJUDICATION_PENDING` 유지, 운영자 RETRY로 새 attempt가 `low = 마지막 CAS`부터 재개(13.2) | (a) `attempt-failure{SPARK_TERMINATED_WITHOUT_RECEIPT}` 1건 → verdict COMMIT → ledger row actor = ADJUDICATION → FINALIZED, watermark = window.high, Dagster run FAILURE 1건·재시도 run 0; (b) 자동 재시도 0, RETRY 후 접두 구간 일치, 이중 commit 0 | Adjudication 없는 attempt |
| FI-16 | (A) 정시 burst 직전 stub 계정 비밀번호 변경(ACTIVE CredentialRevision은 구 값 유지, `FAILED_LOGIN_ATTEMPTS=10`, `SourceCapability.failed_login_attempts=10`·`legacy_concurrent_sessions=2`·`password_rollover_registered=false` 등록); (B) 변형(별도 회차, A 회복 후): stub 계정을 `PASSWORD_GRACE_TIME` 경고 상태(ORA-28002, 만료 D-7)로 두고 정시 burst 1회; (C) **변형(v1.2.3, 별도 회차)**: driver precheck를 구 비밀번호로 **통과시킨 뒤**(precheck 세션 성립 확인) 곧바로 stub 계정 비밀번호를 회전시켜 executor fan-out(`numPartitions` ≥ 4인 Append-large 3계약)이 새 비밀번호 창에서 로그인하게 한다 | (A) 이미 로그인한 attempt는 완주; 로그인 전 attempt는 driver precheck 세션(executor보다 먼저)이 ORA-01017로 실패 → `precheck_failure{CREDENTIAL_FAILURE}` → `attempt-failure {reason: CREDENTIAL_FAILURE, ora_code, credential_revision_id}`(attempt당 1건) → 같은 트랜잭션에서 breaker(`credential_breaker_failures`=1) 개방 + Source `HOLD_NEW`(reason `CREDENTIAL_FAILURE`) + Outbox `source credential failure`(`event_id = hash(hold_id, ∅, 'SOURCE_CREDENTIAL_FAILURE')` — v1.2.2 16.4, Hold created는 별 event_type); 이후 tick `SKIPPED_BY_HOLD`, Guard 4번 `HOLD` → `VOID(SKIPPED_BY_HOLD)`(14.1); 실패 attempt는 `WRITER_FENCED` 후 verdict `NO_COMMIT(CREDENTIAL_FAILURE)`로 `ADJUDICATION_PENDING` 운영자 대기; breaker 개방 중 Control은 같은 revision으로 모니터 세션 재접속 안 함; 운영자 `POST /v1/holds/{id}/release` → `412 CREDENTIAL_REVOKED`; 새 revision VERIFIED→ACTIVE 전이 트랜잭션에서 breaker 닫힘 + Hold 자동 해제(actor = 승격 운영자) → CATCHUP occurrence(키 `job + hold_release_at`, 14.2) 제출; `ADJUDICATION_PENDING` 계약은 Hold 해제 후 운영자 RETRY로 Guard가 새 ACTIVE revision 선택(6.2, 10장 인터페이스, 16.4). (B) ORA-28002는 로그인 성공 경고 — breaker 입력 아님: driver `precheck_warning{CREDENTIAL_EXPIRING}` → 쓰기 계속 → Run Pod가 `chunks/{1}:commit` 본문 `credential_warning`으로 보고 → Outbox `credential expiring` Source·revision·일 단위 1건; Hold·breaker 변화 없음(v1.2.1 6.2·10장·16.4) | (a) stub 실패 로그인 수 ≤ 1 + 첫 실패 전 제출된 SA 수(attempt당 ≤ 1 — precheck 세션만 실패, executor 세션 0) + 모니터 세션 재접속 1, ORA-28000 0; (b) 첫 ORA-01017 → `maintenance_hold`(reason=CREDENTIAL_FAILURE, credential_revision_id=구 revision) 생성 ≤ 10초, 그 이후 SA 생성 0(양성 증거: Hold 중 `SKIPPED_BY_HOLD` ≥ 1); (c) Outbox `source credential failure` 1건(`event_id = hash(hold_id, ∅, 'SOURCE_CREDENTIAL_FAILURE')`, Source 집계 — 같은 hold_id의 `Hold created` 이벤트와 별개 row) ∧ Job 단위 failed 이벤트 0; (c′) 실패 attempt의 `attempt-failure.reason = CREDENTIAL_FAILURE` ∧ breaker 개방 중 모니터 세션 재접속 시도 0(stub 세션 로그); (c″) Hold 개방 중 `POST /v1/holds/{id}/release` 1회 → 412 `CREDENTIAL_REVOKED`(양성 증거) ∧ Hold 상태 불변; (d) 새 revision ACTIVE 전이와 Hold released가 동일 트랜잭션(동일 xid, actor = 승격 운영자) → CATCHUP = Hold 기록 Job 수, 전부 FINALIZED; `ADJUDICATION_PENDING(CREDENTIAL_FAILURE)` 계약은 RETRY 후 attempt 2의 `credential_revision_id` = 새 ACTIVE, FINALIZED; expected − (EXECUTED ∪ `SKIPPED_BY_HOLD` ∪ CATCHUP 흡수) = 0; (e) Source 저장 시 validator가 `credential_breaker_failures + 최대 총 JDBC connection weight(Σ numPartitions + driver_sessions) + 1 + legacy_concurrent_sessions > 9`인 설정(예: 1 + 5 + 1 + 2 = 9는 통과, weight 6은 422)을 422로 거부(양성 증거 1건 — v1.2.2 6.2: 항은 ETL Job 수가 아니라 JDBC connection weight); (e′) 같은 Source를 `password_rollover_registered = true`로 등록하면 항이 최대 동시 ETL Job으로 바뀌어 weight 6(Job 3 × (numPartitions 1 + driver 1))·Job 3 → 1 + 3 + 1 + 2 = 7 통과(양성 대조 1건 — 등록 플래그만 검증, stub 측 rollover 에뮬레이션 없음); (f) (B): burst 전 attempt 전부 FINALIZED ∧ `maintenance_hold` 생성 0 ∧ `source.credential_breaker.failures = 0` ∧ `attempt-failure {CREDENTIAL_FAILURE}` 0 ∧ Pipes `precheck_warning{CREDENTIAL_EXPIRING}` ≥ 1(양성 증거) ∧ Outbox `credential expiring` 정확히 1건(Source·revision·일 dedup, `expires_in_days = 7`) ∧ stub 실패 로그인 카운터 증가 0; (g) (C): 회차 전체의 stub 실패 로그인 수 ≤ Σ `numPartitions`(계약별 executor 세션 수 — 양성 증거: 세션별 첫 로그인 결과가 남은 stub 세션 로그) ∧ **ORA-28000(계정 잠금) 0** ∧ Spark task retry **0**(fail-fast) ∧ 실패 executor를 가진 attempt는 `attempt-failure {CREDENTIAL_FAILURE}` 1건(attempt당 1건 계수) ∧ `credential_breaker_failures` 증가분 = 실패 attempt 수 ∧ 그 계약의 Iceberg 부분 snapshot 0 | — |
| FI-17 | Source S(C=4)에 weight 2 Job 2개 실행 중 + 같은 Source에 PLANNED 계약 3개가 `LEASE_BUSY` backoff 중인 상태를 만든 뒤, 실행 중 attempt 1개의 driver pod에 NetworkPolicy로 Control API egress만 차단(Oracle 세션 유지, heartbeat 중단); 다른 실행 중 Job 1개는 정상 완주시킨다; stub `stub_session_linger_seconds = 0`(graceful 경로 — 잔존 경로는 FI-43) | lease TTL 만료 → `EXPIRING`(token 미반환) → Control이 SA 삭제 + driver/executor pod 부재 확인 → `RECLAIMED`(= `WRITER_FENCED`, Adjudication 진행) → 모니터 세션이 attempt 태그 세션 0건을 `lease_release_probe_interval_seconds` 간격 연속 `lease_release_zero_count`회 관측 → `RELEASED`(token 반환) → 대기 계약에 Guard 8번 grant 식(`observed + reserved_unrealized + requested_weight ≤ pool_cap`)으로 부여(v1.2.2 11.2); 정상 완주 Job의 `finalize`도 `RECLAIMED` → probe(`lease_release_zero_count = 1`) → `RELEASED`(v1.2.2 — finalize 트랜잭션 즉시 `RELEASED` 경로 없음); 계약은 `ADJUDICATION_PENDING(LEASE_EXPIRED)` 운영자 대기 | (a) `lease_state_history`: `EXPIRING` 진입 < `RECLAIMED` < `RELEASED` < 대기 계약 `to_state = GRANTED` row의 `at`, `RELEASED` 전 같은 Source의 GRANTED 중 이 token 재부여 0; (b) `RECLAIMED` 직전 AuditEvent가 "SA 삭제 + pod 부재 확인", 순서 역전 0, Adjudication(`attempt_state_history` FENCED → ADJUDICATED)은 `RECLAIMED` 이후·`RELEASED` 이전에 시작(양성 증거: `RELEASED`를 기다리지 않음); (c) stub 세션 로그: 해당 attempt 태그 세션이 0이 된 시각 ≤ `RELEASED` 시각 ≤ 그 시각 + `lease_release_probe_interval_seconds × lease_release_zero_count` + 5초; 정상 완주 계약은 `lease_state_history`에 `RECLAIMED`·`RELEASED` row 각 1개(finalize 트랜잭션에서 `RELEASED` 직행 0) ∧ `RECLAIMED`~`RELEASED` 간격 ≤ `lease_release_probe_interval_seconds` + 5초(zero_count 1 — v1.2.2); (d) 1초 샘플 전부 stub 동시 세션 ≤ C, 부여/회수 ±5초 밖 샘플에서 `GV$SESSION` 총수(태그 무관, 모니터 세션 제외) = pool 회계 + 잔존 세션 0; (e) 자동 attempt 0; (f) 대기 계약 GRANTED 직전 `guard_result.lease_grant`에 grant 식 평가값(`observed`, `reserved_unrealized`, `requested_weight`, `pool_cap`) 기록 ≥ 1 ∧ 그 row에서 `observed + reserved_unrealized + requested_weight ≤ pool_cap` 성립 ∧ 어떤 GRANTED 직전 row에서도 식 위반 0 | 절대 한도 초과, `RELEASED` 전 재부여 |
| FI-18 | Hold DRAIN mid-chunk / HOLD_NEW / FORCE_STOP(Hold 전 PLANNED 계약 준비, Hold 구간에 tick ≥ 1); `drain_timeout_seconds` 승격은 `FAULT_DELAY_FINALIZE = drain_timeout_seconds + 60`으로 유발 | DRAIN: chunk 경계 `stop` → SA 종료 확인 → `finalize {CANCELLED_AT_SAFEPOINT}`; HOLD_NEW: 진행 중 attempt 완주; FORCE_STOP: fencing → Adjudication → 부분 commit 반영(14.1) | Hold 구간 `SKIPPED_BY_HOLD` ≥ 1, Guard `HOLD` 거부 ≥ 1, K8s 감사 로그의 SA 생성 시각이 Hold [created_at, released_at) 안인 건 0; DRAIN `CANCELLED_AT_SAFEPOINT` 후 `window.low` = 마지막 CAS, 해제 후 CATCHUP이 그 값부터 재개; `drain_timeout_seconds` 초과 시 FORCE_STOP 자동 승격 + `CANCELLED(FORCE_STOP)` + 부분 CAS 반영 + 알림 1건(14.1 (5)) | Hold 중 SA 제출 |
| FI-19 | republish(`interface_changed=false`, manifest 확인) 직후 같은 논리 시각 수동 NORMAL / Gap Recovery 복원 | 같은 (job, logical_at) occurrence 1개, pinned release는 논리 시각 기준(9.2) | 수동 NORMAL API 응답 `contract_id` = 기존 값; Gap Recovery 후 `(job, logical_at)` row 1개; `contract.pinned_release_id` = `effective_from ≤ logical_at`인 **이전** release(새 release id와 불일치) | 중복/누락 |
| FI-20 | 동일 Job(Append 1h 주기)에 10:00 정시 tick, 10:00:05 수동 NORMAL(`POST /v1/jobs/{id}/runs {mode: NORMAL}`), 10:00:05 Dagster UI Materialize, 진행 중 계약에 RETRY, 10:00:10 BackfillPlan(window = 현재 열린 window와 겹치게) 동시 요청; 변형: 정시 tick 직후·Guard 전(`PLANNED`) 계약에 RETRY; 같은 절차를 Full Job 1개에도 수행 | schedule·수동 NORMAL·UI 실행은 같은 `(job, logical_at)` occurrence → 같은 contract. 수동 NORMAL API는 같은 계약을 반환하고 Run을 추가 제출하지 않음(`200 {launch_result: ALREADY_SUBMITTED, dagster_run_id}` — tick run이 non-terminal — 또는 `NOT_LAUNCHABLE` — 이미 `ATTEMPT_ACTIVE`, 9.2); UI Materialize Run은 Guard binding CAS에서 `ATTEMPT_ALREADY_BOUND` SUCCESS; RETRY는 진행 중 계약이므로 `409 ATTEMPT_IN_PROGRESS` + `required_action`, `PLANNED` 변형은 `409 CONTRACT_NOT_STARTED {required_action: RESUBMIT}`(14.3); Backfill은 별도 contract이며 Incremental은 409 `OVERLAPS_OPEN_RANGE`(Run 미생성), Full은 Guard `OPEN_WINDOW` → `VOID(COALESCED)`(13.4, 14.3) | (a) occurrence 1개, attempt 1개, SA 1개; (b) binding run 1개 외 Run은 Dagster UI run 정확히 1개이고 tag `guard_result=attempt_already_bound` SUCCESS; 수동 NORMAL API 응답 200 `{launch_result: ALREADY_SUBMITTED \| NOT_LAUNCHABLE, contract_id = 기존}` ∧ 그 호출로 생성된 Dagster run 0(`resubmit_no ≥ 1` run 0); (c) RETRY 응답 409 `ATTEMPT_IN_PROGRESS` ∧ 본문 `current_attempt=1`·`bound_dagster_run_id` = binding run ∧ `retry_authorization` 미발급·계약 상태 불변; `PLANNED` 변형 409 `CONTRACT_NOT_STARTED`; (d) BackfillPlan start 409 `OVERLAPS_OPEN_RANGE`(Incremental) / backfill contract `VOID(COALESCED)`(Full), SA 0; (e) 실패 알림 0; (f) 대조군: 닫힌 구간 backfill은 정상 실행돼 SA 1개 추가 — SA 총 2개가 **정상**(§5.1 (2)는 `(job, lane)`별 판정이므로 backfill SA(repair/backfill lane)와 NORMAL SA(ingest lane)의 SA 존재 구간 겹침은 오탐이 아니다, v1.2.2; 같은 target의 (c) lease 구간 겹침 0·(3)(b) logical window 쌍별 겹침 0은 그대로 요구) | 중복 |
| FI-21 | Full 0 row(`load.on_empty_source` = `FAIL` / `RETAIN_PREVIOUS`, v1.2.1 12.1) → RETRY → `accept-empty` → `RERUN_LATEST {replace_with_empty: true}` / Append·Merge 0 row / (d) v1.2.2: Full `RETAIN_PREVIOUS` Job 1개를 Source 변경 0으로 3회차 연속 실행 + 4회차에 변경 주입 | 12.1·12.2·13.2 규칙 | Full(`FAIL`): snapshot 불변 + verdict `NO_COMMIT(EMPTY_FULL)` + `ADJUDICATION_PENDING`, 자동 attempt 증가 0, `adjudication_pending_alert_after` 알림 1건; RETRY → 새 attempt가 같은 receipt(0 row)로 같은 verdict 반복(attempt_no=2, snapshot 불변); `accept-empty {reason}` → Spark 제출 0·`FINALIZED_NO_DATA`·window/target lease/source token 해제(같은 트랜잭션), 승인자 기록, target 이전 snapshot 유지; `RERUN_LATEST {replace_with_empty: true}` → 새 계약에서만 빈 snapshot commit 1개(`etl.*` 키 포함); Full(`RETAIN_PREVIOUS`): `FINALIZED_NO_DATA` + 이전 snapshot 유지(`current-snapshot-id` 불변); Append/Merge: ledger row(`committed_snapshot_id` NULL, `cas_applied = true`) + CAS = `window.high`, `FINALIZED_NO_DATA`; (d) 12.1·16.4 v1.2.2: 3회차 모두 `FINALIZED_NO_DATA ∧ target_unchanged = true` → 3회차 finalize 트랜잭션에서 `target unchanged` 1건, `freshness breach` 0(orchestration freshness), 4회차 FINALIZED로 streak 리셋; Full(`RETAIN_PREVIOUS`)·Append/Merge 0 row: Run materialization `{rows: 0, no_data: true}` 1건; Full(`FAIL`): Run FAILURE 1건(`attempt-failure {EMPTY_FULL}`, 12.1)·materialization 0, `accept-empty` 후 Run 생성 0, RETRY attempt 2도 FAILURE 1건; 어느 값에서도 `replace_with_empty` 없이 빈 snapshot이 commit된 건 0(양성 증거: RERUN_LATEST의 빈 commit 1); (d) Outbox `target unchanged` 정확히 1(`event_id = hash(job_id, 1회차 contract_id, 'TARGET_UNCHANGED')`) ∧ `freshness breach` 0 ∧ contract `target_unchanged = true` 3건 ∧ read model target publication age = 1회차 이전 마지막 `cas_at` 기준, 4회차 뒤 리셋 | 데이터 차이 |
| FI-22 | (a) verdict `PARTIAL_COMMIT`·`WRITER_FENCED` 확정 상태의 `ADJUDICATION_PENDING` 계약(FI-15(b′)로 생성 — chunk 1 CAS 완료·chunk 2 미commit)을 `expires_at`까지 방치 → 다음 회차 → 그 계약에 RETRY; (a′) **순서 변형(v1.2.2 14.3·10.2 1번)**: (a)와 같은 계약을 하나 더 만들어 RETRY가 스캐너보다 먼저 도달하게 함 — 이 회차에 한해 `planned_scan_interval`을 300초로 올리고(§7 파라미터, 코드 변경 없음) `expires_at` + 5초에 RETRY 호출, 그 뒤 스캐너 실행; (b) 별도의 `ADJUDICATION_PENDING(PARTIAL_COMMIT)` 계약에 `watermark:seed`로 watermark 이동 후 RETRY; (c) SA 생존(`RUN_WORKER_LOST`) 계약이 `expires_at`에 도달(Run Pod kill 시각 = `expires_at` − 120초, `run_retries` 비활성) | (a) verdict 확정 계약만 만료 대상(v1.2.1 9.3 — verdict NULL은 FI-28 변형) → `CANCELLED(EXPIRED)`(부분 commit 보존, window·lease 해제) → 다음 회차 `[current_watermark, fence)`; RETRY 409 `CONTRACT_CLOSED`; (a′) RETRY 트랜잭션 (a)가 contract row lock 안에서 inline 만료 → `CANCELLED(EXPIRED)`(actor `EXPIRY`) → 409 `CONTRACT_CLOSED`, `retry_authorization` 미발급·attempt 2 없음·구 fence 실행 없음; 이어 도는 스캐너는 변경 0 — 스캐너 선행 (a)와 결과 동일; (b) RETRY 409 `STALE_WINDOW` + `CANCELLED(STALE_WINDOW)`; (c) `reattach_deadline`까지 대기 → fencing → Adjudication(verdict 확정) → `CANCELLED(EXPIRED)`(9.3, 13.4) | (a) `attempt_state_history`에 `FENCED → ADJUDICATED`(reason = verdict `PARTIAL_COMMIT`) row가 `contract_state_history`의 `CANCELLED(EXPIRED)`(actor = `EXPIRY`) row보다 앞선 순서(`WRITER_FENCED`는 attempt 이력이지 contract 이력이 아니다 — 6.2), ledger chunk 1 row(`cas_applied = true`) 보존, 다음 회차 `window.low` = chunk 1 high, RETRY 409 `CONTRACT_CLOSED`; (a′) 409 `CONTRACT_CLOSED` 1건 ∧ `contract_state_history` `CANCELLED(EXPIRED)`(actor `EXPIRY`) row가 RETRY AuditEvent와 같은 트랜잭션(동일 xid — 양성 증거: 스캐너 다음 실행 AuditEvent보다 앞섬) ∧ attempt 수 1 ∧ `retry_authorization` NULL ∧ SA 0 ∧ 다음 회차 `window.low` = chunk 1 high ∧ 후속 NORMAL `COALESCED_INTO` 0; (b) 409 `STALE_WINDOW`, 계약 `CANCELLED(STALE_WINDOW)`, SA 0; (c) `reattach_deadline` 전 만료 0 ∧ deadline 후 `attempt_state_history` FENCED → ADJUDICATED → `contract_state_history` `CANCELLED(EXPIRED)` 1건(양성 증거); 모두 같은 `(contract_id, attempt_no, chunk_no)` snapshot 2개 0 ∧ §5.1 (3)(b) window 쌍별 겹침 0 | Append 이중 commit |
| FI-23 | (a) Append/Merge: `chunks/{n}:commit`에 검사 1 summary 불일치 주입(Polaris 프록시 `strip_etl_keys` 아님 — `added-records`를 +1 변조) → RETRY 시도 → 다음 정시 tick 1회 대기 → repair REPLAY; (b) Full-large: Source row를 이전 snapshot `total-records`의 40%로 줄여 검사 5 row 급감(−50%) 유발 → RETRY 시도 → `abort` → `RERUN_LATEST {accept_row_drop: true}`; (b′) 대조군: 60%(−40%)로 줄여 정상 통과 | (a) `DQ_FAILED` 전이(CAS 없이 ledger row만, Outbox `DQ_FAILED` payload `main_exposed: true`·`committed_snapshot_id`), Run Pod `stop` → `finalize {outcome: DQ_FAILED}`로 source token만 반환(window·target lease 유지), RETRY 409 `CONTRACT_CLOSED`; repair REPLAY Guard 7번이 parent window 인수 → repair FINALIZED 트랜잭션에서 parent `RESOLVED(REPAIR_CONTRACT)` + watermark `window.high` 전진(13.1, 13.4, 14.3); (b) v1.2.1 13.1 검사 5 = driver pre-commit: `chunk_receipt{committed: false, dq_failed: true, dq.row_drop: false}` → `attempt-failure {CHUNK_DQ_FAILED}` → fencing → verdict `NO_COMMIT(CHUNK_DQ_FAILED)` → `ADJUDICATION_PENDING` 운영자 대기, **main snapshot 불변**; RETRY → attempt 2가 pinned JobSpec으로 같은 실패 반복; `abort` → `ABORTED_NO_COMMIT`; `RERUN_LATEST {accept_row_drop: true}` → 새 계약이 `dq_overrides`로 검사 5를 건너뛰고 commit, receipt `dq.row_drop = true`·ledger `dq_result = ROW_DROP_OVERRIDDEN`, FINALIZED(12.1, 14.3); (b′) 정상 FINALIZED | (a) RETRY 409 `CONTRACT_CLOSED` 응답 로그 ∧ 수락 0 ∧ Outbox `DQ_FAILED` 1건에 `main_exposed = true`·`committed_snapshot_id` = 노출 snapshot(양성 증거: 그 snapshot이 main의 current ancestor) ∧ 후속 occurrence `COALESCED_INTO(X)` ∧ repair FINALIZED와 parent RESOLVED·watermark 전진이 동일 트랜잭션(동일 xid) ∧ 이후 새 window 예약 성공 ∧ source token 반환 시각 = finalize 시각; (b) (a)와 달리 `DQ_FAILED` 전이 0 ∧ Iceberg snapshot 0(main current snapshot = 이전 값, 노출 구간 0초) ∧ `attempt-failure.reason = CHUNK_DQ_FAILED`·receipt `dq.row_drop = false` ∧ RETRY 202 후 attempt 2도 같은 reason(attempt 수 = 2, snapshot 0) ∧ `abort` → `ABORTED_NO_COMMIT` + window·`EXCLUSIVE_TABLE` lease 해제(같은 트랜잭션) ∧ `RERUN_LATEST {accept_row_drop: true}` 계약 FINALIZED·snapshot 1개·ledger `dq_result = ROW_DROP_OVERRIDDEN`·AuditEvent(사유·승인자) 1건; (b′) `dq.row_drop = true`·FINALIZED·snapshot 1개(양성 증거: 임계 경계) ∧ 알림 0 | DQ_FAILED RETRY 수락 (Phase 2 게이트), 데이터 차이 |
| FI-23c | Append-large 4 chunk, chunk 2의 `chunks/{2}:commit`에 검사 1 summary 불일치 주입 → `DQ_FAILED`(chunk 1 CAS 완료, chunk 2 commit 존재·ledger `dq_result=FAILED`·CAS 없음, attempt 1 계획의 chunk 3·4 미실행) → `finalize {DQ_FAILED}` 확인 후 순서대로 (c1) `resolve {resolution: OPERATOR_ACCEPT}`, (c2) RETRY, (c3) `dq:accept {reason}`, (c4) RETRY → attempt 2 → FINALIZED; 대조군: `finalize {DQ_FAILED}` 수신 전(`FAULT_HOLD_FINALIZE=1`) `dq:accept` 1회 | (c1) 409 `RESOLUTION_NOT_ALLOWED {required_action}` — 상태·window·watermark 불변(generic accept가 chunk 2 CAS 없이 window를 풀면 다음 회차가 chunk 2를 재append, 14.3); (c2) 409 `CONTRACT_CLOSED`; (c3) 같은 트랜잭션에서 ledger chunk 2 `dq_result=ACCEPTED`(actor `OPERATOR`, 13.1) + CAS(`high_2`) → k = 2 < expected = 4이므로 `ADJUDICATION_PENDING`(verdict `PARTIAL_COMMIT`, reason `DQ_ACCEPTED`), window·target lease 유지, watermark = `high_2`(≠ `window.high` — 미실행 구간은 승인하지 않음); attempt 전이는 `finalize {DQ_FAILED}` → `TERMINAL_OBSERVED → FENCED`, `dq:accept` → `FENCED → ADJUDICATED(PARTIAL_COMMIT, DQ_ACCEPTED)`(COMPLETED 경유 없음, `DQ_SEALED` 신설 없음 — v1.2.2 6.2); (c4) RETRY 202 → Guard 3.4가 `retry_authorization` 소비·attempt 2 생성(`window.low = high_2`, 같은 fence) → **attempt 2 chunk 1·2**(원 구간 3·4 — attempt마다 chunk를 `low`부터 재번호, `expected_chunk_count = 2`; 13.4·§5.1 (3)(a)) → FINALIZED(6.2 표·13.1·14.3); 대조군: 409 `ATTEMPT_IN_PROGRESS` | (c1) 409 1건 ∧ `contract_state_history` row 0 ∧ watermark 불변 ∧ `lease_state_history` RELEASED 0; (c2) 409 `CONTRACT_CLOSED` 1건; (c3) 직후 watermark = `high_2` ∧ `window.high > high_2`(양성 증거) ∧ ledger chunk 2 `dq_result=ACCEPTED`·`actor = OPERATOR` ∧ `attempt_state_history` attempt 1: `TERMINAL_OBSERVED → FENCED` 1건(finalize 뒤) < `FENCED → ADJUDICATED`(`PARTIAL_COMMIT`, `DQ_ACCEPTED`) 1건(dq:accept 시각) ∧ attempt 1 `COMPLETED` 전이 0 ∧ `window_range` 비어 있지 않음; (c4) RETRY 202 1건((c2)의 409와 대비) ∧ attempt 2 Guard 응답 `window.low = high_2` ∧ attempt 2 ledger row 정확히 2개 — `(contract_id, 2, 1)`·`(contract_id, 2, 2)`, `expected_chunk_count = 2`, chunk 1 `window_low = high_2`, chunk 2 `window_high = window.high`, actor RUNPOD ∧ attempt 2 snapshot summary `etl.attempt_no = 2`·`etl.chunk_no ∈ {1, 2}`·`etl.expected_chunk_count = 2` ∧ attempt 1 chunk 2 snapshot 정확히 1개 ∧ 같은 `(contract_id, attempt_no, chunk_no)` snapshot 2개 0 ∧ 같은 job ingest snapshot `[etl.logical_window_low, high)` 쌍별 겹침 0(§5.1 (3)(b)) ∧ 최종 FINALIZED ∧ ledger `cas_at` 순 `window_low = prev.window_high` 연속(attempt 경계 포함 — contract-wide 연속은 이 식으로만 증명, §5.1 (4)); 대조군 409 `ATTEMPT_IN_PROGRESS` 1건 ∧ 상태 불변 | DQ_FAILED RETRY 수락(Phase 2 게이트), Watermark gap, Append 이중 commit |
| FI-24a | (R) Template canary 실패 → `POST /v1/releases/{id}/rollback {reason}`: 문제 release를 pin한 PLANNED 40건(`interface_changed` true 30 + false 10) + 직전 release를 pin한 PLANNED ≥ 10건(logical_at < 문제 release `effective_from`) 사전 준비; 이어서 같은 `{id}`에 2회차 rollback 호출, 이미 ROLLED_BACK인 id에 3회차 호출. (I) 인터페이스 변경 release: 영향 Job PLANNED 40건 + `interface_changed=false` Job PLANNED ≥ 10건 | (R) 202 `{operation_id, release_id}` → 새 release(`rollback_of = id`, VALIDATED 시작) DEPLOYED→VERIFIED→ACTIVE; ACTIVE 트랜잭션에서 문제 release `ROLLED_BACK` + 문제 release를 pin한 PLANNED 40건 전부(`interface_changed` 무관) `VOID(SUPERSEDED_BY_RELEASE)` + `SUPERSEDED_BY(RELEASE)` + Outbox `Definition release rolled back`, `effective_from = now()`; 2회차는 진행 중 rollback release를 재개하는 202(새 release 0), 3회차는 `409 RELEASE_NOT_ACTIVE`(6.2, 8.2, 17). (I) OPEN_CONTRACT_CHECK 통과 → `interface_changed=true` Job의 PLANNED 40건만 VOID, false Job은 불변, `effective_from` 확정 | (R) 202 → 새 release ACTIVE ∧ `bundle_digest` = 직전 ACTIVE 값 ∧ `rollback_of = id` ∧ 문제 release `ROLLED_BACK`, 가시화 ≤ §5 p95; VOID 40건(`contract_state_history.actor = RELEASE_CHECK`) ∧ 직전 release를 pin한 PLANNED ≥ 10건 상태 변화 0 ∧ Outbox 1건; 다음 tick이 새 release로 실행(`pinned_release_id` = rollback release); 2회차 202·release row 증가 0, 3회차 409 `RELEASE_NOT_ACTIVE`. (I) VOID 40건 ∧ `interface_changed=false` Job의 PLANNED 계약 상태 변화 0 | Template rollback 불능 (Phase 2 게이트) |
| FI-24b | 인터페이스 변경 release, 영향 Job 계약 PLANNED 40 + ATTEMPT_ACTIVE 3; **+ 정시 burst 중 `interface_changed=true` Job 100개의 Guard와 release ACTIVE 승격을 동시에 실행(lock 경합)**; **+ split 변형**: (s1) 포인터 교체·reload 직후 ACTIVE 단계 OPEN_CONTRACT_CHECK 실패(위 3건), (s2) rollback operation의 ACTIVE 재검사 실패 — 두 경우 모두 포인터 복귀 전 split 구간에 같은 shard Job 20개의 Guard를 도달시킨다 | `412 OPEN_CONTRACTS_RUNNING` + `{contract_id, state, required_action}` 3건, 트랜잭션 rollback — PLANNED 40건 상태 변경 없음, release VERIFIED 유지, **같은 operation이 shard 포인터를 Control ACTIVE bundle로 복귀 + reload**(6.2 ACTIVE 실패 분기, 17장 rollback (4)). 경합: row lock 직렬화 — Guard가 먼저면 412 목록에 해당 contract_id 포함, ACTIVE가 먼저면 Guard `CONTRACT_CLOSED` SUCCESS. split 구간 Guard: `loaded_bundle_digest ≠ ACTIVE bundle_digest` ∧ 더 새 ACTIVE 없음 → `INTERFACE_MISMATCH` split 분기 = `PLANNED` 유지 + backoff(200, SUCCESS), 복귀 후 통과(10.2 5번 2분기) | 실행 중 계약 자동 마감 0, PLANNED VOID 0, 412 본문에 3개 contract_id·required_action; DRAIN Hold로 3건 종결 후 재시도 시 FI-24a 결과; 경합: `VOID(SUPERSEDED_BY_RELEASE)` 계약에 attempt row 0, `ATTEMPT_ACTIVE` 계약이 VOID로 바뀐 건 0, deadlock/serialization 오류 0, 두 결과의 합 = 100; split 변형 (s1)(s2) 각각: 포인터 복귀·reload 이벤트가 412 이후 operation 로그에 1건, 복귀 후 code location manifest digest = Control ACTIVE `bundle_digest`; split 구간 Guard 20건 중 `guard_result = INTERFACE_MISMATCH` ≥ 1(양성 증거) ∧ 그 계약 `VOID` 0 ∧ Run FAILURE 0 ∧ attempt row의 `loaded_bundle_digest` 기록 100% ∧ 복귀 후 stale 루프 재제출로 20건 전부 `ATTEMPT_ACTIVE` 또는 설명된 disposition; split 구간에 pinned plan과 다른 plan으로 생성된 SA 0(SA spec의 plan digest = contract pinned digest 전수) | Template rollback 불능 (Phase 2 게이트) |
| FI-24c | **Phase 2 게이트(§3.5 7번, FI-24a/b와 같은 회차 — activation boundary 축소판).** (a) 1h 주기 Job 50개의 cron을 `:00 → :30`으로 옮기고 그중 10개의 schedule을 삭제하는 release R2(현 ACTIVE = R1)를 정각 3분 전에 DEPLOYED까지 진행시킨 뒤, `interface_changed=true` Job 3개를 `ATTEMPT_ACTIVE`로 두어 ACTIVE 단계 `412 OPEN_CONTRACTS_RUNNING` → 포인터 복귀를 유도한다(정각 tick은 split 구간에 떨어진다); 복귀 뒤 3건을 DRAIN으로 종결하고, 자동 Gap Recovery가 launch한 정각 계약 50건이 전부 종결(`FINALIZED*`)된 것을 확인한 뒤 같은 release를 재승격(정상 ACTIVE — 그 전 재승격은 `interface_changed = true`인 50 Job의 `PLANNED` VOID 또는 `412 OPEN_CONTRACTS_RUNNING`이므로, 6.2 OPEN_CONTRACT_CHECK); (b) `POST /v1/jobs/{id}/publish` 본문에 `effective_from = now() + 1d`를 실은 요청 1회; (c) **변형(v1.2.3)**: (a)의 재승격을 두 지점에서 crash시킨다 — (c-1) ACTIVE 포인터 UPDATE **직전**(operation `completed_step = DEPLOYED`), (c-2) 포인터 UPDATE commit **직후·자동 Gap Recovery 기동 전**(`completed_step = ACTIVE`). 두 지점 각 1회, 복구는 프로세스 재기동 | v1.2.2 6.2 ACTIVE 행·8.2 7번·9.3·17장: `effective_from = now()` 고정(미래 시각 금지 — 요청은 422 `VALIDATION_FAILED {rule_id: EFFECTIVE_FROM_NOT_ALLOWED}`); release operation 말미(ACTIVE commit 직후 또는 412 포인터 복귀 완료 직후)에 같은 operation이 Gap Recovery를 `scope={shard}, range=[deployed_at − 1 period, now()]`, actor `SYSTEM`으로 자동 기동 → split 구간에 놓친 정각 tick을 9.2 NORMAL 규칙(`effective_from ≤ logical_at`인 최신 = R1)으로 create-or-get·launch; R2 pin은 ACTIVE 시각 이후 tick만 | (a) 412 복귀 뒤와 정상 ACTIVE 뒤 각각 Outbox `schedule gap recovery completed`(actor `SYSTEM`, scope = shard) 1건 ∧ 정각 expected tick 50개 전부 occurrence row 존재(`pinned_release_id = R1`) ∧ `expected occurrence missing` 0 ∧ `VOID(SUPERSEDED_BY_RELEASE)` 0(재승격 시점에 50 Job 계약 전부 종결 — 양성 증거: 종결 전 재승격 1회를 추가로 시도해 412 또는 VOID ≥ 1 확인 후 원복) ∧ §5.1 (1) 중복 0; ACTIVE 뒤 첫 `:30` tick은 R2 pin, 삭제된 10개 Job은 ACTIVE 이후 expected 집합에서 제외되고 그 전 tick은 R1 cron으로 설명됨; 양성 증거: split 구간 `guard_result = INTERFACE_MISMATCH` ≥ 1 또는 자동 Gap Recovery operation의 launch 건수 ≥ 1; (b) 422 `EFFECTIVE_FROM_NOT_ALLOWED` 1 ∧ release row 생성 0 ∧ 모든 ACTIVE release의 `effective_from` = release history의 ACTIVE 전이 `at`(차이 0); (c) 두 지점 모두: 재기동 뒤 같은 `operation_id`가 `completed_step` **다음 단계부터** 재개(이전 단계 재실행 0) ∧ ACTIVE release 정확히 1개(c-1은 R1 유지, c-2는 R2) ∧ `deployed_at` 선기록 row 존재 ∧ 자동 Gap Recovery 기동 **정확히 1회**(Outbox `schedule gap recovery completed` 1건, `event_id` 중복 0) ∧ 정각 expected tick 50개의 occurrence 중복 0·누락 0 | 중복/누락, Template rollback 불능 (Phase 2 게이트) |
| FI-25 | (a) Executor OOM/쓰기 전 예외(driver 생존, `FAULT_EXIT_BEFORE_WRITE`), (b) Driver OOM. 두 계약 모두 period/2 경과·알림 1건 확인 **후** (a) 계약에 `abort {reason}` 호출. (c) **`dagster/max_runtime` 경계 변형**: 같은 Job(Append-large, expected_chunk_count ≥ 3)의 `run_monitoring.max_runtime_seconds`를 정상 실행 p99 **직하**(p99 − 60초, 계약 5건)와 **직상**(p99 + 60초, 계약 5건)으로 RunRequest tag에 지정(22장 22번 규칙) | (a) receipt `committed=false` + `exception_class` → `attempt-failure {SPARK_FAILED}` → fencing → verdict `NO_COMMIT(SPARK_FAILED)`; (b) receipt 없이 SA FAILED → `attempt-failure {SPARK_TERMINATED_WITHOUT_RECEIPT}` → fencing → 구간 판정. 둘 다 `ADJUDICATION_PENDING` 운영자 대기, 자동 재시도 0(10.2); (c) 직하: Dagster run_monitoring이 Run을 CANCELED로 종료 → 반입 표의 CANCELED 원인 `MAX_RUNTIME_EXCEEDED`(v1.2.1 10.2 — `OPERATOR_CANCELLED`·`PLATFORM_TERMINATE`와 분리, 자동 재시도 비대상) → 재결합 대기 없이(`reattach_deadline` 기록 없음, v1.2.1 10.2 반입 표 CANCELED 행) 즉시 SA 삭제 + pod 부재 확인(fencing) → Adjudication `PARTIAL_COMMIT`(접두 chunk CAS) → 운영자 RETRY 대기; 직상: 종료 0, FINALIZED | attempt 수 = 1, Dagster run 중 `dagster/retry_number ≥ 1` run 0, run 결과 FAILURE 1건, verdict reason이 (a)(b)대로 기록, (b)에서 S′ = ∅ 확인, alert는 period/2 경과 후 Outbox "ADJUDICATION_PENDING 장기화" 정확히 1건(중복 0); abort 후 (a) 계약 `ABORTED_NO_COMMIT`, window·target lease 해제(같은 트랜잭션), source token 잔존 0, AuditEvent(actor·idempotency_key) 1건 — §6 운영 절차 체크리스트의 abort 증거; (c) 직하 5/5 Dagster CANCELED ∧ `attempt_state_history` reason = `MAX_RUNTIME_EXCEEDED`(`RUN_WORKER_LOST` 0) ∧ `reattach_deadline IS NULL` 5/5 ∧ 자동 attempt 0 ∧ 접두 chunk CAS 보존·이중 commit 0 ∧ `max_auto_attempts` 소비 0; 직상 5/5 FINALIZED ∧ CANCELED 0(양성 증거: 두 집합의 실측 runtime이 경계 양쪽); `credential_grace_seconds` = 이 값 전제(§7) 확인 | — |
| FI-26 | Oracle DR outage 15분 — (A) Guard 시점, (B) 추출 중 (stub 응답 중단 API, primary 리스너는 정상) | (A) `guard_result ∈ {FENCE_UNAVAILABLE, SOURCE_LAG_EXCEEDED}`, SA 0, backoff; (B) driver receipt `committed=false, exception_class=ORA-03113/…` → `ADJUDICATION_PENDING(SPARK_FAILED)`, 접두 chunk만 CAS. 두 경우 primary 접속 0 | (A) 거부 ≥ 1·SA 0, 회복 후 FI-10(a) 방식으로 누락 0; (B) 접두 chunk CAS, 자동 attempt 0; 공통: stub 접속 로그에서 primary 리스너 접속 0 ∧ standby 접속 시도 ≥ 1(양성 증거) | Primary 자동 fallback |
| FI-27 | TNS/RAC endpoint 전환: 새 ConnectionRevision ACTIVE(구 SUPERSEDED) 상태에서 attempt A 실행 중 (a) `POST /v1/sources/{id}/connection-revisions/{rid}/revoke {reason}`(force 없음), (b) 별도 계약에서 `force: true`, (c) ACTIVE revision 없이 REVOKED만 남긴 뒤 Guard; (d) **변형(v1.2.3)**: 새 ConnectionRevision이 ACTIVE로 전이한 **직후 첫 Guard**를 대상으로, 모니터 세션이 트랜잭션 **밖**에서 새 descriptor로 선수립되고 트랜잭션 **안**에서는 포인터만 교체되는지(fence provenance) 관측한다 | (a) `412 OPEN_CONTRACTS_RUNNING` + `{contract_id, attempt_no, state, required_action: DRAIN Hold}`, 상태 변경 없음, A 완주; 새 계약(다음 occurrence)은 ACTIVE descriptor, 같은 계약의 RETRY attempt는 pinned(SUPERSEDED) descriptor 유지; (b) 같은 트랜잭션에서 REVOKED + attempt별 FORCE_STOP 프로토콜 → `WRITER_FENCED` → Adjudication → `CANCELLED(CONNECTION_REVOKED)` 또는 `ABORTED_NO_COMMIT`; 이후 pinned REVOKED 계약의 새 attempt는 Guard가 ACTIVE로 재해석해 attempt에 기록(`attempt.connection_revision_id`, v1.2.2 6.1 — contract pin은 불변); (c) Guard `CONNECTION_REVOKED`(모니터 세션 조회 전 판정) → `PLANNED` 유지 + backoff, 새 ACTIVE 등록 시 대기 계약 `next_eligible_at=now()` 리셋(6.2, 7.1, 10.2 표) | (a) 412 본문에 A의 contract_id·attempt_no ∧ revision 상태 SUPERSEDED 유지 ∧ A의 stub 세션 endpoint = 구 descriptor ∧ FINALIZED ∧ attempt A의 fence를 읽은 모니터 세션과 driver·executor 세션의 stub 로그 `db_identity` tuple 6항(§2.3, v1.2.2 — `pdb_guid` 포함) = contract pinned `db_identity` ∧ Guard가 pin한 descriptor hash = fence를 읽은 revision(v1.2.1 7.1); 다음 계약 Guard 응답 `descriptor_hash` = 새 ACTIVE; (b) `attempt_state_history` FENCED→ADJUDICATED 후 `CANCELLED(CONNECTION_REVOKED)`(부분 commit은 CAS 반영) ∧ SA 삭제 AuditEvent ∧ 구 descriptor 세션 0(stub 세션 로그) ∧ 재해석 attempt의 `connection_revision_id` = 새 ACTIVE ∧ contract pinned descriptor hash·`db_identity` 불변; (c) `guard_result=CONNECTION_REVOKED` ≥ 1 ∧ stub 모니터 세션 접속 시도 0 ∧ 새 ACTIVE 등록 후 `planned_stale_after + planned_scan_interval × 2` 내 재제출 100%; (d) ACTIVE 전이 직후 첫 Guard가 읽은 fence의 provenance: 그 fence를 실은 stub 세션의 endpoint·`db_identity` tuple = **새 ACTIVE descriptor**(구 descriptor 세션이 실은 fence 0 — 양성 증거: 세션 수명 로그의 수립 시각 < Guard 트랜잭션 시작) ∧ 계약의 `fence_ts`·`visible_scn`이 그 세션 관측값과 일치 ∧ Guard 트랜잭션 **안**에서의 새 세션 로그인 0(포인터 교체 1회만) | Adjudication 없는 attempt (Phase 2 게이트) |
| FI-28 | (a) Polaris catalog 장애 5분, (b) AIStor 장애 5분 — 각각 extract-once 경로 Job 2개 포함, Guard 시점·실행 중 attempt 양쪽; 변형: Guard 직후(SA 생성 전) Polaris 차단; **변형 (c) outage > `expires_at`**: 1h 주기 Append Job의 실행 중 attempt(chunk 1 CAS 완료·chunk 2 commit 직전)에서 Polaris를 차단(차단 시각 = `expires_at` − 5분)해 driver `committed=null` receipt → `attempt-failure {COMMIT_STATE_UNKNOWN}` → SA terminal(`WRITER_FENCED`)까지 확인되지만 Adjudication이 Polaris를 읽지 못하는 상태를 `expires_at` + 10분까지 유지한 뒤 회복 | Guard 5번 target health check(`target_health_timeout_seconds` 5초) 실패 → 200 `{result: TARGET_UNAVAILABLE}`, `PLANNED` 유지 + backoff, Source 미조회(6번 앞); breaker key별 연속 `platform_breaker_failures`(3)회에 같은 Guard 트랜잭션에서 자동 `HOLD_NEW`(reason `PLATFORM_BREAKER(key)`, scope: (a) 그 catalog의 Job 목록 / (b) Global)와 그 계약의 `VOID(SKIPPED_BY_HOLD)`, 이후 계약은 Guard 4번 `HOLD`·tick `SKIPPED_BY_HOLD`; 실행 중 attempt는 끝까지 수행하되 Polaris 오류 receipt(`committed=null`) → `attempt-failure {COMMIT_STATE_UNKNOWN}` → Adjudication(Polaris 미조회 시 `adjudication_retry_backoff_initial_seconds` backoff 재시도, `RECONCILIATION_REQUIRED` 아님), staging 보존; 변형: Guard 뒤 `base_snapshot_id` 조회 실패 → `attempt-failure {TARGET_UNAVAILABLE}` → SA 미생성 확인 = `WRITER_FENCED` → 구간 판정 없이 `NO_COMMIT(TARGET_UNAVAILABLE)` 운영자 대기; 회복 후 운영자 Hold release → CATCHUP(5.4, 10.2 표, 14.1); (c): fenced ∧ verdict NULL 계약은 `expires_at`이 지나도 `ADJUDICATION_PENDING`·window·target lease 유지, `CANCELLED(EXPIRED)` 없음(9.3 만료 전제 = verdict 확정), 후속 NORMAL은 `COALESCED_INTO`, `adjudication_pending_alert_after` 알림; 회복 후 verdict 정확히 1개 — `COMMIT`이면 finalize 대행 `FINALIZED`, `PARTIAL_COMMIT`/`NO_COMMIT`이면 CAS 반영 뒤 `CANCELLED(EXPIRED)`(6.2 표·13.4) | 장애 중 `guard_result = TARGET_UNAVAILABLE` ≥ 1 ∧ 그 Guard의 stub 모니터 세션 조회 0 ∧ SA 0; 3회 연속 후 `maintenance_hold`(mode=HOLD_NEW, reason=`PLATFORM_BREAKER(key)`) 1건 — scope가 (a) Job 목록 / (b) Global ∧ Outbox `target unavailable` key 단위 1건 + Hold created 1건; Hold 중 `SKIPPED_BY_HOLD` ≥ 1; 실행 중 attempt: `attempt-failure.reason = COMMIT_STATE_UNKNOWN` ∧ 회복 전 verdict 0·`RECONCILIATION_REQUIRED` 전이 0 ∧ 회복 후 verdict 확정; 변형: reason `TARGET_UNAVAILABLE` ∧ `attempt_state_history` FENCED→ADJUDICATED(verdict NO_COMMIT) ∧ Polaris 구간 조회 0 ∧ 자동 attempt 0; staging manifest 존재 ∧ RETRY 후 새 attempt의 stub 쿼리 0(Source 재조회 0); Hold release 후 CATCHUP = Hold 기록 Job 수; (c): `expires_at` 경과 후 회복 전 창에서 `contract_state_history` `CANCELLED(EXPIRED)` 0 ∧ 양성 증거(EXPIRY 스캐너가 그 계약을 평가한 AuditEvent ≥ 1 ∧ 후속 occurrence `COALESCED_INTO` ≥ 1) ∧ 새 attempt 0·SA 0 ∧ `lease_state_history` TARGET lease `RELEASED` 0 ∧ `window_range` 비어 있지 않음 ∧ Adjudication 재시도 AuditEvent ≥ 3·간격 단조 증가(상한 300초); 회복 후 `attempt_state_history` FENCED → ADJUDICATED 정확히 1건 ∧ ledger chunk 2 row ≤ 1 ∧ 같은 `(contract_id, chunk_no)` snapshot 2개 0 ∧ 다음 회차 `window.low` = 마지막 CAS 값 | Hold 중 SA 제출; (c) Append 이중 commit |
| FI-29 | Kafka 브로커 전부 10분 정지(정시 burst 포함), 시험 consumer는 `event_id` 기준 idempotent sink(PoC 전용 테이블) 사용 | Outbox 보존·outage 후 재전송, consumer가 `event_id`로 dedup(16.4) | (a) outage 창 포함 전후 30분의 `NotificationOutbox.event_id` 집합 = consumer sink `event_id` 집합(차집합 0); (b) 와이어 중복은 허용하되 sink의 처리 중복(같은 event_id 2회 반영) 0; (c) 회복 후 backlog 소진 ≤ 5분; (d) outage 중 Control 트랜잭션 실패(Outbox insert 실패로 상태 전이가 막힌 건) 0 | Outbox 유실 |
| FI-30 | Dagster UI 직접 Materialize(Critical / 비Critical / 종결 계약 / **Hold 중 비Critical**) | Critical `412 DIRECT_LAUNCH_FORBIDDEN` FAILURE; 비Critical NORMAL 계약 create-or-get(origin=DAGSTER_UI) 후 Guard 통과; Hold 중이면 create-or-get 단계가 occurrence `SKIPPED_BY_HOLD`/contract `VOID(SKIPPED_BY_HOLD)`를 반환하고 Guard는 `CONTRACT_CLOSED` SUCCESS(9.1·14.1 — 어느 단계에서 막히는지 기록); 종결 계약 `CONTRACT_CLOSED`(10.2, 14.3) | Critical run FAILURE ∧ Dagster 로그 `DIRECT_LAUNCH_FORBIDDEN` 1건 ∧ occurrence 미생성; 비Critical occurrence `origin=DAGSTER_UI` 1건 ∧ (schedule occurrence 기존 시 같은 contract_id); 종결 계약 `guard_result = CONTRACT_CLOSED`; Hold 중 UI 실행은 `SKIPPED_BY_HOLD` + SA 0; Guard 우회 Spark 제출 0 | Hold 중 SA 제출 |
| FI-31 | Append-large 계약이 chunk 2 진행 중(attempt ACTIVE, base_snapshot 기록됨)일 때 같은 테이블에 `kubectl apply`로 SA를 직접 생성해 `etl.*` 키 없는 append snapshot 1개 commit. 5회. 변형(b): 진행 중 계약이 없는 테이블. 음성 대조군(c): 유효한 `PARTITION_OR_FILESET` lease 아래의 compaction snapshot이 chunk 사이에 끼는 경우(SC-11) | (a) Run Pod가 chunk 2 receipt의 lineage(base(2)→committed_snapshot_id ancestor)를 `chunks/{2}:commit`에 실음 → Control이 lease 기록 없는 개입 snapshot을 확인해 ledger row(`committed_snapshot_id` 포함, `dq_result=EXTERNAL_SNAPSHOT`) 기록·CAS 없이 `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 전이 + Outbox 즉시 1건; Run Pod `stop` → SA terminal 확인 → `finalize {outcome: RECONCILIATION_REQUIRED, sa_status}`(source token만 반환, window·target lease 유지) → Run FAILURE; Adjudication·`WRITER_FENCED`·verdict 없음, RETRY는 409 `CONTRACT_CLOSED`, 복구는 repair REPLAY 또는 `resolve`(13.1, 6.2, 10.2). chunk 2 commit 자체가 OCC로 실패한 회차는 `attempt-failure {SPARK_FAILED}` → 13.2 3번으로 같은 `RECONCILIATION_REQUIRED` — 경로를 기록하되 둘 다 합격; (b) 탐지 경로 없음 — 미탐지가 **기대 결과**, Polaris principal/K8s RBAC·admission으로 제출 자체를 막는 방안은 22장 13·14번 결정 입력; (c) 전이 0·CAS 정상 | (a) 5/5 `RECONCILIATION_REQUIRED`(경로별 건수 기록) ∧ 외부 snapshot을 자기 chunk로 CAS한 건 0 ∧ 13.1 경로 회차는 ledger row `dq_result=EXTERNAL_SNAPSHOT`·CAS 없음·`attempt_state_history`에 FENCED/ADJUDICATED row 0·`finalize` outcome=`RECONCILIATION_REQUIRED` AuditEvent 1·source token 반환 시각 = finalize 시각·window_range 유지 ∧ Run FAILURE 1건·`run_retries` run 0 ∧ Outbox `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 1건 ∧ RETRY 1회 → 409 `CONTRACT_CLOSED` ∧ `resolve` 후 같은 job 새 window 예약 성공; (b) 결과 기록만, 판정 제외; (c) `RECONCILIATION_REQUIRED` 0 ∧ CAS = expected_chunk_count | Adjudication 없는 attempt(자동 attempt 0), Append 이중 commit |
| FI-32 | schema drift(컬럼 삭제·타입 축소) at Guard — 대상에 1h 주기 Job 1개와 **24h 주기 Job 1개**(tick 직후 drift 주입, Hold 10분 뒤 schema 승인으로 해제) | `ALL_TAB_COLUMNS` digest 불일치 → `VOID(SCHEMA_DRIFT)` + occurrence `REJECTED_AT_GUARD` + Job `HOLD_NEW`(10.2); 승인·해제 시 `REJECTED_AT_GUARD(SCHEMA_DRIFT)` Job은 Hold 구간 tick이 없어도 14.2 impacted 집합이므로 CATCHUP 생성(v1.2.1 14.2) | `guard_result = SCHEMA_DRIFT` ≥ 1 ∧ stub 쿼리 로그에서 해당 contract MODULE의 본문 SQL 0 ∧ `ALL_TAB_COLUMNS` 조회 ≥ 1 ∧ Job Hold 1건(Job당) ∧ 1h Job 다음 tick `SKIPPED_BY_HOLD` ∧ 알림 1건; 해제 후 24h Job: `CATCHUP` occurrence 1건 ∧ `FINALIZED*`(양성 증거: 그 Job의 Hold 구간 `SKIPPED_BY_HOLD` 0) ∧ expected − 설명된 누락 = 0 ∧ 24h Job lateness ≤ `freshness_slo`(24h class) | 중복/누락 (Phase 2 게이트) |
| FI-33 | CredentialRevision 교체: 새 revision ACTIVE(구 SUPERSEDED) 상태에서 attempt A 실행 중, `credential_grace_seconds` 경과 전 새 attempt B 생성, 경과 후 REVOKED 전이 / ACTIVE revision 없이 REVOKED만 남긴 뒤 Guard | grace 동안 A 완주(`attempt.credential_revision_id` = 구 revision), B는 ACTIVE 선택; REVOKED 시 실행 중 attempt는 FORCE_STOP 프로토콜 → `CANCELLED(CREDENTIAL_REVOKED)`; ACTIVE 없으면 Guard `CREDENTIAL_REVOKED`로 `PLANNED` 유지 + backoff, 새 ACTIVE 등록 시 해당 Source 대기 계약 `next_eligible_at = now()` 리셋(6.2, 10.2 표) | A의 ledger chunk 접두 구간 보존·CAS 일치, B의 `credential_revision_id` = ACTIVE, REVOKED 후 `CANCELLED(CREDENTIAL_REVOKED)` 전이 전 `WRITER_FENCED` 기록 존재, ACTIVE 등록 후 `planned_stale_after + planned_scan_interval × 2` 내 재제출 100%(9.3 stale 루프는 `planned_stale_after` 경과 계약만 대상), 구 revision으로 열린 Oracle 세션 0(`GV$SESSION` CLIENT_IDENTIFIER) | Adjudication 없는 attempt |
| FI-34 | Control API 불가 3분: (a) 정시 tick 직전부터(batch create-or-get 5xx/timeout), (b) Run Pod Guard 호출 중, (c) `chunks/{n}:commit` 호출 중; (c′) **network partition 변형** — Control API는 정상이되 NetworkPolicy로 해당 Run Pod → Control API egress만 3분 차단(driver ↔ Run Pod Pipes·driver → Polaris 정상), `chunks/{n}:commit` 호출 중; (c″) (c′)와 같되 4분 차단으로 `chunk_proceed_timeout_seconds`를 넘기는 계약 1개(해당 회차만 §7 값을 180초로 축소) | (a) tick fail-closed: RunRequest 0, tick FAILURE → `max_tick_retries` 재시도로 회복(9.1); (b) Run Pod가 `guard_retry_budget_seconds` 동안 재시도 후 tag `guard_result=CONTROL_API_UNAVAILABLE` SUCCESS 종료, 계약 `PLANNED` 유지 → stale 루프 재제출(10.2 표); (c)(c′) Run Pod 재시도, driver는 `proceed` 대기(`chunk_proceed_timeout_seconds` 480초 > 차단 180초 내 회복) — (c′)에서 다른 Run Pod·Control은 무영향; (c″) driver가 `proceed_timeout{last_chunk_no}` 후 exit 1 → SA FAILED → Run Pod 회복 후 `attempt-failure {SPARK_TERMINATED_WITHOUT_RECEIPT}` 또는 terminal 반입 → fencing → 접두 구간 `PARTIAL_COMMIT`, `max_auto_attempts` 비대상(`RUN_WORKER_LOST` 아님) → 운영자 RETRY 대기(10.1·10.2) | (a) 복구 후 occurrence 중복 0·누락 0, `max_tick_retries` 소진 tick 0(양성 증거: FAILURE tick ≥ 1); (b) tag `guard_result=CONTROL_API_UNAVAILABLE` Run ≥ 1(양성 증거) ∧ 실패 알림 0, `planned_stale_after + planned_scan_interval` 내 재제출 100%, 장애 중 SA 0; (c) 같은 chunk ledger row 1개·CAS 1회, `proceed_timeout` 0; (c′) 같은 chunk ledger row 1개·CAS 1회 ∧ `proceed_timeout` 0 ∧ Run Pod 재시도 로그 ≥ 1(양성 증거) ∧ 차단 해제 후 ≤ 60초 내 commit 호출 성공 ∧ 같은 시간대 다른 Run Pod의 `chunks/{n}:commit` p99 불변(§2.5 대조); (c″) Pipes `proceed_timeout` 1 ∧ 같은 `(contract, attempt, chunk)` snapshot 2개 0 ∧ receipt가 있던 chunk의 ledger row 1개(`actor = ADJUDICATION`) ∧ 자동 attempt 0 ∧ RETRY 후 `low = 마지막 CAS` 재개 | 중복/누락, 30분 초과 복구, Append 이중 commit |
| FI-35 | PLANNED stale 루프: (a) batch create-or-get commit 직후 daemon kill(RunRequest 미발행 20건), (b) burst 중 queue 대기가 `planned_stale_after`를 넘는 계약 50건(의도적 `max_concurrent_runs` 축소), (c) stale 루프와 정규 RunRequest가 같은 계약을 동시에 제출; (e) **poll 채번 뒤 응답 반환 전 Control crash(v1.2.3.1 A 9.1·9.3)**: `submissions:poll`이 `resubmit_no` 채번 + `submission_in_flight` set을 commit한 직후·응답 반환 전 Control 프로세스 kill(훅 `FAULT_KILL_AFTER_POLL_COMMIT=1`, PoC 빌드 한정 — 신설) 10건 + 대조 5건은 poll 응답이 sensor에 닿기 전 프록시 drop 후 kill; 재기동 뒤 stale 루프·Gap Recovery·수동 NORMAL 세 경로가 같은 계약을 동시에 만나게 함 | (a) `planned_stale_after` 경과 후 `planned_scan_interval` 내 stale 루프가 계약을 **제출 대상(eligible)으로 표시**(in-flight 미변경) → 다음 shard sensor 평가의 `submissions:poll`이 계약 row `FOR UPDATE` 아래에서 **최초 `resubmit_no = 0`**(A 9.3 채번 규칙)을 채번하고 같은 트랜잭션에서 `submission_in_flight {resubmit_no: 0, requested_at}`를 set한 뒤 항목 반환 → sensor가 `RunRequest(run_key = "{contract_id}:0")`로 제출(v1.2.3.1 A 9.1·9.3 — `launchRun`·`executionMetadata.runId`는 Dagster GraphQL에 없는 입력이라 철회됐고 제출 채널은 shard sensor 하나다); (b) 재기동 뒤 poll이 tag `dagster/run_key = "{contract_id}:0"`인 run 존재를 확인하고 **다시 싣지 않음**(존재하지 않으면 `resubmit_no`를 올리지 않고 **같은 `resubmit_no = 0`·같은 `run_key`로 다시 반환** — 그때도 run storage의 `dagster/run_key` 태그가 중복을 흡수해 run 1개); (c) Guard binding CAS로 attempt 1개, 나머지 `ATTEMPT_ALREADY_BOUND` SUCCESS(10.2 2번); (e) 재기동 뒤 `submission_in_flight`가 non-null인 계약은 **poll이** 새로 채번하지 않고(v1.2.3.1 — set 주체는 poll, NULL 해제 주체는 run 확정 경로다) `submission_recheck_after` 경과 후 tag `dagster/run_key = \"{contract_id}:{resubmit_no}\"`인 run을 `runsOrError`로 조회(v1.2.3 9.3 — `submission_in_flight.run_id`는 철회된 필드다) — 10건: run 존재 → `last_submitted_run_id` 확정·in-flight NULL·재제출 0; 대조 5건: run 없음 → poll이 `resubmit_no`를 올리지 않고 **같은 `run_key`로 다시 반환** → sensor `RunRequest` 1회(중복은 run storage의 `dagster/run_key` 태그가 흡수) | (a) 20건 전부 `resubmit_no=0` run 1개씩(tag `dagster/run_key = \"{contract_id}:0\"`인 run 정확히 1개 — `launchRun` 호출 0·run id 지정 없음, v1.2.3.1 A 9.3), Spark 제출 1건/계약; (b) Adapter 'non-terminal run 존재' 확인 AuditEvent ≥ 50 ∧ `resubmit_no ≥ 1` run 0건; (c) 계약당 attempt 1개, `ATTEMPT_ALREADY_BOUND` Run의 실패 알림 0, SA 1개; (e) 15건 전부 tag `contract_id` run 정확히 1개 ∧ 전 시점 non-terminal run ≤ 1(§5.1 (2)) ∧ 15건 전부 `launchRun` 호출 0(AuditEvent — 제출 채널은 shard sensor `RunRequest` 하나다)·대조 5건은 같은 `run_key`로 sensor 제출 정확히 1회 ∧ 세 경로 중 하나만 `runOrError` 확정 AuditEvent를 남기고 나머지는 `submission_in_flight` 확인 후 무동작 ∧ 양성 증거: crash 시점 `submission_in_flight` non-null 스냅샷 15건·복구 후 NULL 15건·`last_submitted_run_id` = 그 계약의 `run_key = \"{contract_id}:{resubmit_no}\"`로 확정된 run id | 중복/누락 |
| FI-36 | RECONCILIATION_REQUIRED 유도 3종 — (a) commit-wrapper 밖 SQL로 `etl.*` 키 없는 snapshot 생성, (b) 같은 chunk_no의 snapshot 2개 수동 commit, (c) chunk 2 누락·chunk 3 존재 — 후 `attempt-failure` 트리거. 이어서 (d) 다음 NORMAL tick, (e) `dagster-terminal-event` 재수신, (f) `resolve {resolution: WATERMARK_SEED}` + `watermark:seed`, (g) 별도 계약에서 repair REPLAY(`parent_contract_id`)로 RESOLVED | (a)(b)(c) verdict 불가 → `RECONCILIATION_REQUIRED`(13.2), window·target lease 유지(6.2); (d) `COALESCED_INTO` + `OPEN_WINDOW` SUCCESS; (e) 감사 로그만; (f) `RESOLVED(WATERMARK_SEED)` + window·lease 해제; (g) Guard 7번에서 parent window 인수 → repair FINALIZED 트랜잭션에서 parent `RESOLVED(REPAIR_CONTRACT)` + watermark 전진(13.4) | 자동 retry 0(attempt 수 불변), (d) `guard_result = OPEN_WINDOW` 1건 ∧ `VOID(COALESCED)` ∧ SA 0(§2.5의 exclusion constraint 충돌 수는 0이 정상 — 충돌이 기록되면 Guard 7번 결함으로 별도 보고), (f)(g) 후 같은 job 새 window 예약 성공, watermark 단조성 유지, H-05 오탐률 산정 입력 확보 | Adjudication 없는 attempt, 중복 |
| FI-37 | Append-large 4 chunk 중 chunk 2·4를 0 row로 주입; 변형: chunk 2(0 row) receipt 직후 Run Pod OOM | chunk 2: ledger row(`committed_snapshot_id` NULL) + CAS `high_2` 전진, 상태 `COMMIT_OBSERVED` 유지(6.2); chunk 4: 같은 처리 후 `finalize {FINALIZED}`(committed snapshot이 있으므로 NO_DATA 아님, 12.2 6번); 변형: 재결합 Run Pod가 Pipes 재생으로 chunk 2 ledger row+CAS 대행 후 chunk 3 `proceed`, 또는 fencing 후 Adjudication이 {1 snapshot, 2 receipt} = 접두 2로 `PARTIAL_COMMIT` 판정(13.2) | ledger row 4개(snapshot 2개·NULL 2개), watermark = `window.high`, 최종 FINALIZED, 변형에서 `RECONCILIATION_REQUIRED` 0·재개 `window.low` = `high_2` | watermark gap, Append 이중 commit, 데이터 차이 |
| FI-37a | Append-large 4 chunk 중 chunk 1을 0 row로 주입, receipt 1 직후 Run Pod kill(`FAULT_KILL_AFTER_RECEIPT=1`) + 재결합 차단(FI-07 본 케이스와 동일) → `reattach_deadline` → Control fencing | 13.2(v1.2.1): S′ = ∅이지만 0-row 집합 {1}이 1..4를 덮지 않으므로 `FINALIZED_NO_DATA`가 아니라 chunk 1 ledger row + CAS(`high_1`) 대행 후 verdict `PARTIAL_COMMIT`, `ADJUDICATION_PENDING` 유지 → `RUN_WORKER_LOST` 자동 attempt 2가 `window.low = high_1`부터 chunk 2~4 재개(13.4 — watermark는 `high_k`로만 전진) | `FINALIZED_NO_DATA` 전이 0 ∧ 양성 증거: `attempt_state_history` ADJUDICATED(verdict `PARTIAL_COMMIT`) 1건; Adjudication 직후 watermark = `high_1` ∧ ≠ `window.high`; attempt 2 Guard 응답 `window.low = high_1`; `{rows: 0, no_data: true}` materialization 0; 최종 ledger row 4개(chunk 1 NULL) ∧ FINALIZED ∧ ledger `window_low = prev.window_high` 연속; Control DB에 ledger `UNIQUE (contract_id, attempt_no, chunk_no)` 제약 존재(`pg_constraint` 1건) | watermark gap, 데이터 차이 |
| FI-37b | FI-37a와 같되 chunk 1·2를 0 row로 주입하고 receipt 2 직후 kill(`FAULT_KILL_AFTER_RECEIPT=2`) + 재결합 차단 | 0-row 집합 {1, 2}는 접두부이나 1..4를 덮지 않음 → chunk 1·2 ledger row + CAS(`high_2`) 대행 후 `PARTIAL_COMMIT`, attempt 2가 `low = high_2`부터 chunk 3·4 재개(13.2·13.4) | `FINALIZED_NO_DATA` 0 ∧ ADJUDICATED(`PARTIAL_COMMIT`) 1건(양성 증거); Adjudication 직후 watermark = `high_2` ∧ ≠ `window.high`; attempt 2 `window.low = high_2`; 최종 ledger row 4개(NULL 2개) ∧ FINALIZED ∧ chunk 3·4 snapshot 각 정확히 1개 | watermark gap, Append 이중 commit |
| FI-37c | Append-large 4 chunk 전부 0 row(정상 경로); 변형: receipt 4 직후·`finalize` 전 Run Pod kill + 재결합 차단 → fencing | 정상: chunk마다 ledger row(NULL) + CAS `high_1..high_4` 순차 전진, `ATTEMPT_ACTIVE` 유지, `finalize {FINALIZED_NO_DATA}`(12.2 6번 — committed snapshot 없음); 변형: Adjudication이 0-row 집합 = 1..4(완비)임을 확인해 빠진 ledger row·CAS 대행 후 `FINALIZED_NO_DATA` 대행(13.2 v1.2.1). 두 경우 모두 전이 트랜잭션은 watermark를 옮기지 않는다(13.4) | ledger row 4개(전부 NULL) ∧ CAS 4건의 `cas_at` 단조 증가 ∧ `window_low = prev.window_high`; watermark = `high_4` = `window.high`이고 마지막 watermark 변경 시각 = chunk 4 CAS 시각 ≤ `FINALIZED_NO_DATA` 전이 시각(전이 트랜잭션의 watermark UPDATE 0); 정상: materialization `{rows: 0, no_data: true}` 1건·`FINALIZED_NO_DATA` 1건; 변형: `attempt_state_history` FENCED → ADJUDICATED 1건 ∧ `FINALIZED_NO_DATA` 1건 ∧ `PARTIAL_COMMIT` 0 ∧ `RECONCILIATION_REQUIRED` 0 ∧ 다음 회차 `window.low` = `high_4` | watermark gap, 데이터 차이 |
| FI-38 | (a) 새 `(cron, tz)` 조합 Job 10개를 포함한 release publish, (b) write webserver에서 grouped schedule 1개를 STOP(수천 Job 포함 shard) 후 2 tick 경과 | (a) VERIFIED 단계에서 기대 schedule 집합이 instance에서 RUNNING으로 대조, `default_status=RUNNING`(9.1·6.2); (b) Gap Recovery 주기 검사가 STOPPED schedule 알림 1건, 누락 tick은 disposition 없음(설명되지 않은 누락)으로 §5 "중복/누락" 쿼리에 잡힘, "expected occurrence missing" 알림 | (a) 새 schedule 10개 전부 RUNNING, 첫 tick에 occurrence 생성; (b) 첫 누락 tick T에 대해 `expected occurrence missing`이 `(schedule, T)` 1건(Job 수 = shard Job 수 동봉, Job 단위 이벤트 0)으로 T + `planned_stale_after` + `planned_scan_interval` 안에 도착 ∧ Gap Recovery 주기 검사의 STOPPED 알림 1건; 운영자가 RUNNING 복원 + `POST /v1/schedule-gap-recoveries {scope: {shard}}` 후 누락 집계가 최신 tick 1건 복구·중간 tick `COALESCED_INTO`(9.3)로 닫힘 | 중복/누락 |
| FI-39 | read-only webserver에서 GraphQL mutation(schedule stop·run terminate·run delete·reload) 시도 / write 인스턴스에서 같은 mutation | read-only 인스턴스는 mutation 전부 거부, write 인스턴스는 SSO proxy access log에 사용자·mutation 기록(5.1) | read-only 거부율 100%, write 경로 감사 로그 누락 0, run delete 후에도 contract의 `dagster_run_id`·ledger 불변 | — |
| FI-40 | stub 가시성 모델 자가검증(§2.3 'SCN 기반 가시성' 행 — v1.2.2 system tick 포함, **§3.5 1번 — soak 전**): idle 모드(직전 table commit 후 10분 이상 **table commit 0**, system tick 3초는 계속)와 burst 모드(초당 ≥ 2,000 commit) 각 1회, 각각 `injected_lag` ∈ {0, 12분}으로 ZERO_GAP Append Job 1개의 Guard를 통과시킴(계약 4건). 동시에 stub 주입 API로 커서 이후 row N ≥ 1,000건(`commit_ts > stub_now − injected_lag`)을 준비 | Guard 6번이 읽은 contract `visible_scn`·`fence_ts`(11.3)가 stub 커서와 일치하고, driver의 `AS OF SCN visible_scn` 추출이 커서 이후 row를 0건 반환(11.3·11.4). idle·burst에서 같은 lag가 같은 시각을 가리킨다(단위 오류 모델 `primary_scn − lag`와의 차이); idle에서 커서는 system tick을 따라 전진하므로 apply lag ≈ `injected_lag`이지 idle 경과 시간이 아니다(source idleness ≠ apply lag — §2.3 v1.2.2) | (a) 4개 계약 전부 `contract.visible_scn` = 주입 API 응답 커서 `visible_scn` ∧ `SCN_TO_TIMESTAMP(visible_scn)` − 응답 `ts` ∈ [−3초, 0]; (b) `fence_ts − SCN_TO_TIMESTAMP(visible_scn)` ∈ [`injected_lag`, `injected_lag` + `datum_stale_seconds`](idle·burst 동일 — 양성 증거: burst 회차 stub commit 카운터 ≥ 2,000/초; idle 회차 table commit 0 ∧ 응답 `tick_count` 진행 ≥ 180(10분/3초) ∧ idle 회차 `visible_scn`이 tick SCN(row 없는 항목)); (c) 커서 이후 row N건 중 Iceberg 적재 0 ∧ 커서 이내 row 전부 적재(stub 생성 truth 대비 §5.1 (4) 차집합 0); (d) stub 쿼리 로그의 `V$DATAGUARD_STATS` 응답 apply lag = `injected_lag` ± 3초(idle 회차 포함 — idle 경과 시간(≈10분)으로 읽히면 불합격) ∧ `DATUM_TIME` age ≤ `datum_stale_seconds` | — (stub 유효성 전제 — 불합격 시 stub 수정 후 재실행, FI-10~13·26·H-03 결과 무효, soak 미착수) |
| FI-41 | **§3.5 3번(soak 4~5일차, FI-12 직후·같은 Source).** (a) `bound_kind=ENFORCED`·`DB_TRIGGER`·`not_null`·`updated_on_every_change`·`fence_time_witness=HEARTBEAT_TABLE`(v1.2.2) Source의 ZERO_GAP Job(overlap = 1200초 + safety_lag + clock_skew): stub late-commit API로 `commit_at − UPDATE_DT` ∈ {1199초, 1200초}(bound 내, **eligible**)인 row 100건과 bound 위반 row(`commit_at − UPDATE_DT` = 1260초) 10건 주입 — ENFORCED Source의 stub 트리거 에뮬레이션은 위반 commit을 거부해야 한다(주입 API 응답 `REJECTED_BY_BOUND`); (b) 같은 JobSpec을 `bound_kind=OBSERVED` Source에 ZERO_GAP으로 publish 시도; (c) `updated_on_every_change=false` 컬럼·`not_null=false` 컬럼·`timestamp_origin=APP_SUPPLIED` 컬럼·`delete_semantics=NONE_DECLARED` 각 1건으로 ZERO_GAP publish 시도; (d) OBSERVED Source의 BEST_EFFORT Job에 위반 row 10건 주입(stub 허용) | (a) eligible row는 다음 회차 최초 chunk의 overlap(12.2 extract window)에서 전부 적재, 위반 row는 Source에 존재하지 않음(v1.2.1 7.2 5번·11.3); (b)(c) `422 VALIDATION_FAILED` rule `ZERO_GAP_REQUIRES_ENFORCED_BOUND`(17장), BEST_EFFORT로는 publish 통과(경고만); (d) 위반 row는 다음 회차에 누락 → Data Reconciliation Audit(12.3)이 탐지·repair | (a) 주입 eligible PK 집합 − target PK 집합 = ∅(차집합 0 — §5.1 ‘주입 truth vs target’ 층과 같은 쿼리) ∧ 적재 100건·PK 중복 0 ∧ ledger `next.window_low = prev.window_high` ∧ 양성 증거: receipt `overlap_recovered_rows ≥ 100`, stub 거부 응답 10건; (b)(c) 422 응답 6건 — 5건 `violations[0].rule_id = ZERO_GAP_REQUIRES_ENFORCED_BOUND`, 1건(`zero_gap_verified = false` Source) `ZERO_GAP_REQUIRES_VERIFIED_SOURCE` ∧ `violations[].field`에 미달 항목 ∧ `inputs`에 SourceCapability id ∧ JobSpecVersion·release 미생성 ∧ BEST_EFFORT publish 202 + 경고; (d) 위반 10건 FINALIZED 직후 부재(양성 증거) → Audit 1주기 내 존재 ∧ `repair_contract_id` FINALIZED | Watermark gap(ZERO_GAP) — ledger 인접성만으로는 검출 불가하므로 이 행의 PK 차집합이 H-03 ZERO_GAP 판정의 필수 증거, 데이터 차이 |
| FI-41e | **§3.5 3번(FI-41 직후·같은 Source, v1.2.2).** `fence_time_witness = HEARTBEAT_TABLE` Source의 ZERO_GAP Job A(FI-41(a) 설정)에 (a) Guard 직전 stub heartbeat 갱신 정지 60초(`heartbeat_delay_seconds = 60` — `AS OF SCN :visible_scn`으로 보이는 `ts`가 `commit_ts(visible_scn)`보다 60초 이른 값) 후 정시 tick, 같은 창에 FI-41(a)와 같은 bound 내 late-commit row 100건 주입; (b) Guard 직전 `ETL_HEARTBEAT` 조회 오류 주입(권한 없음 에뮬레이션) 후 tick; (c) 같은 JobSpec을 `fence_time_witness = SCN_TO_TIMESTAMP` Source(그 외 조건은 전부 ENFORCED·DB_TRIGGER 충족)에 ZERO_GAP으로 publish 시도 | (a) 11.3 v1.2.2: `T_lb` = 보이는 heartbeat `ts`(60초 이른 값) → `high = min(T_lb, SYSTIMESTAMP_standby) − safety_lag`가 60초 보수화될 뿐 `T_lb ≤ commit_ts(visible_scn)` 유지 — 그 회차 window가 짧아지고 다음 회차가 `[high, …)`를 이어 덮어 누락 0(12.2 overlap 불변); (b) Guard 6번 `FENCE_UNAVAILABLE`(fail-closed, window 예약 0, PLANNED + backoff) → 회복 후 stale 루프 재제출; (c) `422 VALIDATION_FAILED` rule `ZERO_GAP_REQUIRES_ENFORCED_BOUND`(`violations[].field = fence_time_witness`), BEST_EFFORT publish 202 + 경고 | (a) contract `high` = stub 커서 `commit_ts(visible_scn)` − 60초 − `safety_lag` ± 5초(양성 증거: 주입 API 응답의 heartbeat 정지 시각) ∧ 모든 회차에서 `high + safety_lag ≤ commit_ts(visible_scn)`(보수 방향만, 초과 0) ∧ 주입 100건 + 커서 이내 row 전부 두 회차 안에 적재(§5.1 (4) 차집합 0, PK 중복 0) ∧ ledger `next.window_low = prev.window_high`; (b) `guard_result = FENCE_UNAVAILABLE` ≥ 1 ∧ 그 contract `window_range` 비어 있음·`visible_scn IS NULL` ∧ 회복 후 `planned_stale_after + planned_scan_interval × 2` 내 재제출·FINALIZED; (c) 422 1건(`rule_id = ZERO_GAP_REQUIRES_ENFORCED_BOUND`, `field = fence_time_witness`) ∧ JobSpecVersion·release 미생성 ∧ BEST_EFFORT 202 1건 | Watermark gap(ZERO_GAP), 데이터 차이 |
| FI-41f | **§3.5 3번(FI-41e 직후·같은 Source, v1.2.2 P0-08-B).** (a) `delete_semantics = CDC_LATER` JobSpec을 ZERO_GAP으로 publish 시도(그 외 조건 전부 충족); (b) `PK_RECONCILE(interval = 1h)` ZERO_GAP Merge Job에 hard delete 20건 주입 후 2회차 경과; (c) `SOFT_DELETE(target_action=DELETE)` ZERO_GAP Job에 soft delete 20건 주입 후 1회차 | (a) 7.2 5번·17장 v1.2.2: `422 VALIDATION_FAILED` rule `ZERO_GAP_REQUIRES_ENFORCED_BOUND`(`field = delete_semantics`), BEST_EFFORT 202; (b) 11.3 v1.2.2 ‘delete는 선언 경로의 bounded lag’: `interval` 이내 PK_RECONCILE Asset이 반영; (c) watermark와 동기 반영 | (a) 422 1건 ∧ release 미생성 ∧ 데이터 계약 노출 항목에 `delete_semantics`·`PK_RECONCILE.interval` 존재; (b) §3.2 비교 B(`interval` 경과 후) target − Source 차집합 0 ∧ 경과 전 차집합 = 20(설명된 차이, 양성 증거) ∧ PK_RECONCILE Asset `lease_state_history` GRANTED ≥ 1; (c) 회차 FINALIZED 직후 양방향 차집합 0 | Watermark gap(ZERO_GAP), 데이터 차이 |
| FI-41g | **§3.5 3번(FI-41f 직후·같은 Source, v1.2.3) — G2 항목**(실 DG 시험 인스턴스에서 수행, stub 회차는 대조용). **v1.2.3.1 — 이 행은 `bound_evidence.mechanism = SYNC_COMMIT_GUARD` Source 전용이다**(A 11.3: ENFORCED 근거는 이 mechanism 하나뿐). `TXN_AGE_KILL_JOB` Source의 kill miss 재현은 **FI-41h로 분리**했다 — 그 Source는 A 11.3대로 `upsert_consistency = BEST_EFFORT`라 ZERO_GAP 인증 증거가 될 수 없기 때문이다. `bound_kind = ENFORCED` ∧ `mechanism = SYNC_COMMIT_GUARD`(bound를 넘는 commit 자체를 **동기 fail-closed로 거부**하는 장치, `threshold_seconds`) ZERO_GAP Source에서 long-open transaction을 연다: (a) 나이가 bound 안(`threshold_seconds − 60초`)일 때 commit, (b) 나이 `threshold_seconds + 60초`에서 commit **시도**, 각 회차 row 50건 | 7.2·11.3 C1~C4·검토서 v3.1 §7.7(v1.2.3.1 재작성): (a) commit은 허용되고 다음 회차 overlap(`O ≥ threshold_seconds + 1초 + safety_lag + clock_skew`) 안에 들어와 전부 회수된다. (b) guard가 **commit 자체를 거부**하므로 bound 초과 commit이 애초에 존재하지 않는다 — 그 트랜잭션은 실패로 끝나고 **Source row 자체가 생기지 않는다**(‘누락’이 아니라 미존재이며, 이것이 `SYNC_COMMIT_GUARD`만 ENFORCED 근거인 이유다). 따라서 이후 회차의 watermark gap은 0이고 그 사실이 이 Source의 **ZERO_GAP 인증 증거**(§8.3 G2)다. C4 상시 반증(관측 상한 초과 1건 → `zero_gap_verified := false`)은 이 시험에서 **발동하지 않아야** 한다 — 발동하면 guard가 fail-closed가 아니라는 뜻이므로 FAIL이다 | (a) 주입 PK 50건 전부 target 존재(§5.1 (4) `AS OF SCN` 양방향 차집합 0) ∧ receipt `overlap_recovered_rows ≥ 50`; (b) **commit 거부 1건**(양성 증거: 주입 API가 받은 Oracle 오류 코드·SQLSTATE와 거부 시각) ∧ 그 트랜잭션의 row 0건이 Source에 존재(`AS OF SCN` 조회 결과 0건) ∧ 뒤이은 2회차 연속 §5.1 (4) 양방향 차집합 0(= gap 0) ∧ `zero_gap_verified` false 전이 **0건** ∧ Outbox `zero gap verification invalidated` **0건** ∧ `HOLD_NEW(ZERO_GAP_INVALIDATED)` **0건**; 공통: 관측한 최대 트랜잭션 나이를 §8.2에 기록해 7.2 13번 overlap 부등식의 입력으로 올린다(SLO가 아니라 **측정량**) | Watermark gap(ZERO_GAP), 데이터 차이 |
| FI-41h | **§3.5 3번(FI-41g 직후·다른 Source, v1.2.3.1 분리) — G2 항목이 아니다**(BEST_EFFORT 안전망 시험). `bound_evidence.mechanism = TXN_AGE_KILL_JOB`(`threshold_seconds = T_threshold`, `repeat_interval_seconds = P_kill`)만 등록된 Source — A 11.3대로 이 Source의 Job은 `upsert_consistency = BEST_EFFORT`이고 ZERO_GAP publish는 애초에 불가다. kill job을 1회 skip시켜 나이 `T_threshold + 60초` 트랜잭션의 commit을 **성공**시킨다(kill miss 재현), row 50건. 이 회차의 목적은 인증이 아니라 `late_commit_handling = DETECT_AND_REPAIR` 안전망의 실증이다 | A 11.3 `TXN_AGE_KILL_JOB` 규정 + 11.3 C1~C4: 주기(`P_kill`)와 kill 지연(`(Q+L)`) 때문에 회피 창이 남으므로 bound 초과 commit이 실제로 착지하고, overlap(`O ≥ T_threshold + 1초 + P_kill + (Q+L)ₘₐₓ,ₒᵦₛ + safety_lag + clock_skew`) 밖이면 **누락**이 생긴다. Data Reconciliation Audit(12.3)이 그 차이를 탐지·repair한다. **이 회차의 통과는 BEST_EFFORT 안전망의 증거이지 ZERO_GAP 인증 증거가 아니며 §8.3 G2 목록에 넣지 않는다** | 누락 ≥ 1 재현(양성 증거: 주입 API의 commit 나이 응답 ∧ kill job 마지막 성공 실행 시각) ∧ Audit 1주기 내 그 PK 탐지 ∧ repair 계약 FINALIZED 후 §3.2 비교 A 차집합 0 ∧ 그 Source의 ZERO_GAP publish 시도는 422 `ZERO_GAP_REQUIRES_ENFORCED_BOUND`(`violations[].field = bound_evidence.mechanism`) 1건 ∧ `zero_gap_verified` 전이 0건(등록 시점부터 false) ∧ 관측한 kill 지연(`Q+L`) 분포를 §8.2에 기록해 7.2 13번 `computed_minimum`의 입력으로 올린다(측정량) | 데이터 차이 |
| FI-41i | **§3.5 6번 말미(실 DR 시간대의 G1·G2 증거 수집을 모두 마친 뒤 수행, v1.2.3.1 신설) — G0 무효화 전파 드릴.** 이 드릴은 `g0_pass := false`로 **그 시점 이후의 G1·G2 판정을 미판정으로 만들므로**(§8.3) 다른 회차보다 뒤에 두고, 드릴 종료 후 재실증(새 `g0_report_id`, 이전 id는 `supersedes`)까지를 한 회차로 §8.2에 기록한다. G0 무효화 입력 1종을 바꾼다(`NLS_NCHAR_CHARACTERSET` 변경 또는 `ETL_CANON` 재배포로 함수 source digest 변경 — §8.1 무효화 목록) | `g0_pass := false` + Outbox `g0 invalidated` 1건, 그 `g0_report_id`에 의존하는 **모든** Source의 `zero_gap_verified := false`(A 6.1) + Source별 `zero gap verification invalidated` 1건, 각 Source의 ACTIVE `ZERO_GAP` Job에 자동 `HOLD_NEW(ZERO_GAP_INVALIDATED)` insert-or-get | 의존 Source 수만큼 false 전이·Outbox 각 1건 ∧ Hold 정확히 1건/Source(제약 (7)) ∧ 그 Hold 아래 신규 SA 0(`SKIPPED_BY_HOLD` ≥ 1) ∧ 진행 중 계약 중단 0 ∧ 이후 `ZERO_GAP` publish 시도 422 `ZERO_GAP_REQUIRES_VERIFIED_SOURCE` ∧ **과거 `g2_report_id`·`g0_evidence` 보고서 보존**(`supersedes`로 연결) ∧ 재실증 후 새 `g0_report_id` 위 G2로만 `true` 복귀 | 확정(ZERO_GAP 인증 무효화 전파) |
| FI-42 | DB identity 상이(§3.5 3번, FI-13과 같은 창): §2.3 identity 상이 endpoint B(같은 descriptor 이름·role `PHYSICAL STANDBY`, `cdb_dbid`·`db_unique_name` 상이)로 (a) 새 ConnectionRevision 연결 테스트, (b) ACTIVE revision의 ADDRESS failover로 Guard 모니터 세션이 B에 접속, (c) Guard 통과 뒤 driver precheck만 B로, (d) executor connection만 B로(FI-45(c) 절차); (e) v1.2.2: §2.3 endpoint C(같은 CDB·다른 PDB — `cdb_dbid`·`db_unique_name`·`resetlogs_change_no` 동일, `pdb_guid`·`pdb_dbid`·`pdb_con_uid`·`CON_NAME` 상이, 같은 schema/table)로 (a)~(d)를 반복 + non-CDB 에뮬레이션 Source(`pdb_identity = NOT_APPLICABLE`)의 연결 테스트 1건; (f) **변형(v1.2.3)**: (b) 경로를 stub switchover 창에 맞추고 같은 Source의 Guard **20건을 동시에** 도달시켜(20개 계약을 같은 정시에 배치) 자동 Hold 생성이 동시에 시도되게 한다 | (a) `422 VALIDATION_FAILED` rule `DB_IDENTITY_MISMATCH`(`actual`·기대값 동봉 — 6항 tuple), VERIFIED 전이 0(7.1); (b) Guard 6번 `SOURCE_IDENTITY_MISMATCH` → `VOID(reason)`/`REJECTED_AT_GUARD(reason)` + Source `HOLD_NEW` + `source role mismatch`(`mismatch_kind = IDENTITY`), SA 0; (c)(d) `sessionInitStatement` ORA-20902 → `attempt-failure {SOURCE_IDENTITY_MISMATCH}` → fencing → `CANCELLED(SOURCE_IDENTITY_MISMATCH)`/`ABORTED_NO_COMMIT` + Hold(10.2 표·11.3·12.3); (e) B와 동일 결과 — `V$DATABASE` 3항이 전부 같아도 `V$CONTAINERS` tuple 불일치로 (a)~(d) 각 경로가 막히고 Template SQL의 `CON_NAME` 술어가 두 번째 겹(12.3 v1.2.2); non-CDB는 `NOT_APPLICABLE` 양쪽 일치로 통과 | (a) 422 1건 ∧ VERIFIED row 0 ∧ `actual`에 6항 전부; (b) `guard_result = SOURCE_IDENTITY_MISMATCH` ≥ 1 ∧ B endpoint data SELECT 0 ∧ `maintenance_hold(reason=SOURCE_IDENTITY_MISMATCH)` 1건 ∧ Outbox `mismatch_kind = IDENTITY` 1건; (c)(d) ORA-20902 ≥ 1 ∧ Iceberg snapshot 0 ∧ `attempt-failure.reason = SOURCE_IDENTITY_MISMATCH` ∧ Template SQL 텍스트에 `SYS_CONTEXT('USERENV','CON_NAME')` 술어 존재(양성 증거); 전 경로에서 B의 `AS OF SCN` 본문 SQL 0; (e) C endpoint에서 (a) 422 1건(`actual.pdb_guid ≠ 기대값` ∧ `actual.cdb_dbid = 기대값` — 양성 증거: `V$DATABASE` 3항 일치 로그) ∧ (b) `guard_result = SOURCE_IDENTITY_MISMATCH` ≥ 1 ∧ (c)(d) ORA-20902 ≥ 1 ∧ C의 data SELECT 0 ∧ Iceberg snapshot 0 ∧ stub 쿼리 로그에 `V$CONTAINERS` 조회 ≥ 1(연결 테스트·init 블록 각각) ∧ non-CDB Source 연결 테스트 VERIFIED 1 ∧ 저장된 `db_identity.pdb_identity = NOT_APPLICABLE`(null 0); (f) 20 Guard 동시 도달: **deadlock 0 ∧ serialization 실패 0**(PostgreSQL 로그로 양성 확인) ∧ `maintenance_hold` row 정확히 1건(제약 (7) insert-or-get — 나머지 19건은 기존 row를 get) ∧ Outbox `source role mismatch` 1건(`event_id` 중복 0) ∧ Source row `FOR UPDATE` 획득은 credential breaker 경로에서만 관측(role·identity 자동 Hold 경로의 Source `FOR UPDATE` 0) ∧ 20건 전부 `VOID(SOURCE_IDENTITY_MISMATCH)` 또는 `REJECTED_AT_GUARD` ∧ SA 0 | Primary 자동 fallback(identity 포함), 데이터 차이 |
| FI-43 | **§3.5 2번(soak 1~3일차, FI-17 직후·같은 Source S, C=4).** (a) stub `stub_session_linger_seconds = 900`(TCP 단절 후 세션 잔존)으로 FI-17 절차 반복: driver pod를 `kubectl delete pod --grace-period=0`으로 제거(FIN 미전달 모사) → lease 만료 → `RECLAIMED`; 대기 PLANNED 계약 3개(weight 2)는 backoff 중; `lease_release_max_wait_seconds` = 120으로 축소; 600초 후 stub 세션 강제 종료 API로 잔존 세션 kill; (b) 변형: `RECLAIMED` 직후 stub `GV$SESSION` 뷰 응답을 `monitor_query_timeout_seconds` 초과로 지연(관측 실패) 상태에서 대기 계약 Guard 도달 | (a) `RECLAIMED` 후 잔존 세션 2개가 남아 probe가 0건을 관측하지 못함 → `RELEASED` 보류, token은 pool 회계에 잔류; 대기 계약 Guard 8번은 `observed(2 잔존 + 실행 중 2) + reserved_unrealized(0) + 2 > 4`로 `LEASE_BUSY`(v1.2.2 11.2 grant 식 — `RECLAIMED`만 보고 부여하던 v1.2 경로 차단); 120초 경과 시 Outbox `source session lingering` 1건(Source 집계), probe 계속; 세션 kill 후 다음 probe 연속 3회 0건 → `RELEASED` → 대기 계약 GRANTED; Adjudication은 `RECLAIMED` 직후 수행(`RELEASED` 무관); (b) 관측 실패는 `LEASE_BUSY`가 아니라 `FENCE_UNAVAILABLE`(fail-closed, 부여 0, PLANNED + backoff, Source 집계 알림) | (a) `lease_state_history`에 `RECLAIMED` row 있고 `RELEASED` row는 세션 kill 이후 ≤ `probe_interval × zero_count` + 5초; `RECLAIMED`~`RELEASED` 구간에 같은 Source GRANTED 0(양성 증거: 그 구간 `guard_result = LEASE_BUSY` ≥ 3, `lease_grant.observed = 4`) ∧ 1초 샘플 전부 stub 동시 세션 ≤ C(잔존 2 + 실행 2 = 4, 초과 0) ∧ Outbox `source session lingering` 정확히 1건(`RECLAIMED` + 120초 ± `probe_interval`) ∧ `attempt_state_history` FENCED → ADJUDICATED 시각 < `RELEASED` 시각 ∧ `RELEASED` 후 `planned_scan_interval × 2` 내 대기 계약 GRANTED; (b) `guard_result = FENCE_UNAVAILABLE` ≥ 1 ∧ 그 구간 GRANTED 0 ∧ `LEASE_BUSY`로 기록된 건 0(양성 증거: stub 뷰 지연 주입 API 응답) ∧ 회복 후 stale 루프 재제출 100% | 절대 한도 초과, `RELEASED` 전 재부여 |
| FI-43c | **§3.5 2번(FI-43 직후·같은 Source S, C=4, v1.2.2).** (a) reservation gap: stub JDBC open 지연 옵션(`stub_connect_delay_seconds = 60`, 신설 — 연결 수립만 60초 지연)으로 weight 2 계약 A가 Guard 8번 GRANTED 뒤 세션을 아직 열지 못한 상태에서 같은 Source의 weight 2 계약 B·C를 Guard 8번에 연속 도달시킴(비태그 legacy 세션 2개가 stub에 상주, `legacy_concurrent_sessions = 2`); (b) linger 조합: (a)와 같되 FI-43(a)의 잔존 세션 2개(`stub_session_linger_seconds = 900`, `RECLAIMED` 상태 token weight 2)가 남아 있는 동안 수행 | (a) A의 token은 미실현(`tagged_observed_A = 0`)이므로 B의 Guard 8번은 `observed(legacy 2) + reserved_unrealized(2) + 2 = 6 > 4`로 `LEASE_BUSY`(v1.2.2 11.2 grant 식 — v1.2.1의 `observed + requested`만으로는 4 ≤ 4 통과), A의 세션이 열린 뒤에는 `observed = 4`, `reserved_unrealized = 0`으로 같은 결과; (b) `RECLAIMED` token의 잔존 세션은 `observed`에, 미실현 weight는 `reserved_unrealized`에 각각 1회만 잡혀 B·C `LEASE_BUSY`, 잔존 세션 kill + A 완료 뒤 순차 GRANTED | (a) B·C `guard_result = LEASE_BUSY` ≥ 2 ∧ 그 row의 `lease_grant.reserved_unrealized = 2`·`observed = 2`(양성 증거) ∧ A 세션 open 전 구간 같은 Source GRANTED 0 ∧ 1초 샘플 전부 stub 동시 세션 ≤ C(초과 0) ∧ A 완료 후 B·C 순차 GRANTED·FINALIZED; (b) `lease_grant` row 전부에서 `observed + reserved_unrealized + requested_weight > pool_cap`인 GRANTED 0 ∧ 잔존 세션 존재 구간 GRANTED 0 ∧ 동시 세션 ≤ C ∧ `lease_state_history`에 정상 finalize 계약 A의 `RECLAIMED` → `RELEASED` row 2개(간격 ≤ `lease_release_probe_interval_seconds` + 5초) | 절대 한도 초과, `RELEASED` 전 재부여 |
| FI-44 | **§3.5 3번(soak 4~5일차).** Append-large(expected_chunk_count ≥ 4, `undo_retention_seconds = 1200`, `retention_guarantee = false`) 계약에 (a) chunk 3 추출 중 stub undo 만료 API(`undo_expire_before_scn = visible_scn + 1` — 이후 `AS OF visible_scn` 조회는 ORA-01555) 주입; (b) 별도 계약: chunk 2 CAS 후 stub DDL 주입(ORA-01466); (c) 별도 계약: chunk 소요를 stub 지연으로 4분으로 만들고 chunk 3 `chunks:begin` 시각 + 예상 chunk 소요(240초)가 `fence_ts + 600초`를 넘게 함(10.2 undo deadline 식; chunk 2는 480 ≤ 600으로 통과, chunk 3은 720 > 600으로 초과 — 전후 경계); (d) INITIAL_LOAD 2건: `fence_mode = PER_CHUNK_FENCE`(`retention_guarantee = true` Source)와 `EXTRACT_ONCE`, 총 적재 시간 > 1200초 × 0.5 | (a)(b) receipt `committed=false, exception_class ∈ {ORA-01555, ORA-01466}` → `attempt-failure {SPARK_FAILED}` → fencing → `PARTIAL_COMMIT`(chunk 1·2 CAS 보존) → `FENCE_EXPIRED`와 같은 처리: `resubmit_blocked = true`, `retry_authorization` 무효, RETRY 409 `FENCE_EXPIRED`, 자동 attempt 0, 운영자 `abort` → 다음 회차 `[current_watermark, fence_new)`(v1.2.1 11.4); (c) chunk 3 `chunks:begin` → `FENCE_EXPIRED` → `stop` → `finalize {CANCELLED_AT_SAFEPOINT, reason: FENCE_EXPIRED}` → `window.low = high_2`, window·lease 해제, 다음 회차 재개, ORA-01555 0(10.2); (d) PER_CHUNK_FENCE: data chunk마다 `visible_scn_k` 단조 증가·ORA-01555 0, 목록 끝 final sweep chunk 1개(`expected_chunk_count = n + 1`, fence는 chunk n 재사용, 논리 window `[wm, wm)`, `extract_window_low = T_lb_1 − overlap` — v1.2.3 12.2(`fence_ts`는 wall-clock이라 하한에 쓰지 않는다); 경계 late commit은 FI-44e); EXTRACT_ONCE: Source 재독 0, staging 재사용(12.2·11.5); `EXTRACT_ONCE`가 아닌 INITIAL_LOAD를 `retention_guarantee = false` Source에 publish 시도 → 422 | (a)(b) `attempt-failure.reason = SPARK_FAILED` ∧ `exception_class` 기록 ∧ ledger chunk 1·2 CAS 보존·chunk 3 row 0 ∧ RETRY 1회 → 409 `FENCE_EXPIRED` ∧ attempt 수 = 1 ∧ stub 쿼리 로그에 같은 `visible_scn` 재조회 0(양성 증거: abort 후 다음 회차는 새 `visible_scn`으로 `[high_2, fence_new)` 적재, 누락 0 — FI-10(a) 방식) ∧ 같은 `(contract, chunk)` snapshot 2개 0; (c) `chunks:begin(3)` 응답 `FENCE_EXPIRED` 1건 ∧ `chunks:begin(2)` OK(경계 양성 증거) ∧ `contract_state_history` `CANCELLED_AT_SAFEPOINT`(reason `FENCE_EXPIRED`) 1건 ∧ stub 오류 로그 ORA-01555 0 ∧ 다음 회차 `window.low = high_2` ∧ 누락 0; (d) PER_CHUNK_FENCE 계약의 ledger `summary['etl.source_scn']`(= 12.2 `visible_scn_k`)이 data chunk마다 증가 ∧ sweep row(`chunk_no = n + 1`)의 `etl.source_scn = visible_scn_n` ∧ `window_low = window_high = wm` ∧ `extract_window_low = fence_ts_1 − overlap` ∧ ORA-01555 0 ∧ FINALIZED; EXTRACT_ONCE 계약 stub 쿼리 수 = chunk 수(재독 0) ∧ FINALIZED; validator 422 `violations[0].rule_id = EXTRACT_EXCEEDS_UNDO_BUDGET` 또는 fence_mode 제약 위반 1건(양성 증거) ∧ `resubmit_blocked = true` ∧ stale 루프 재제출 0 | Append 이중 commit, Watermark gap, 데이터 차이(§3.2 미비교 0) |
| FI-44e | **§3.5 3번(FI-44 직후, v1.2.2).** `PER_CHUNK_FENCE` INITIAL_LOAD(Append ZERO_GAP, ENFORCED Source, bound 1200초, `safety_lag` 60초, overlap 1263초, `initial_load.chunk = PT5M`, data chunk n = 10, `cooldown` 0; stub 지연으로 chunk 1~8 각 30초, chunk 9 300초)에서 chunk 8 CAS 직후(T_8 ≈ `fence_ts_1` + 240초, ledger `cas_at` 기준) stub late-commit API로 `commit_at = T_8 + 10초`, `UPDATE_DT = commit_at − 1150초`(chunk 8 논리 구간 `[wm_1 − 900초, wm_1 − 600초)` 안, `wm_1 = fence_ts_1 − 60초`)인 row 50건(bound 내, stub ENFORCED 허용) + bound 위반 대조 5건(1260초, `REJECTED_BY_BOUND`) 주입; 대조군: 같은 절차를 `EXTRACT_ONCE` INITIAL_LOAD에; (b) **변형(v1.2.3)**: 같은 `PER_CHUNK_FENCE` INITIAL_LOAD의 chunk 5~8 적재 중 Source에서 **hard delete 20건**(chunk 1~4가 이미 적재한 PK)을 수행한다 — `delete_semantics = SOFT_DELETE` 1회, `PK_RECONCILE {interval}` 1회; (c) **변형(v1.2.3)**: heartbeat witness를 60초 지연시켜 final sweep chunk의 extract window 하한이 `T_lb_1` 규칙(12.2)보다 뒤로 밀리지 않는지 대조한다 | chunk 9·10은 자기 구간만 읽어 50건을 보지 않고, 다음 NORMAL overlap `[wm − 1263초, wm)`(wm = `safe_cutoff(fence_10)` ≈ `fence_ts_1` + 480초)도 `UPDATE_DT` ≈ `fence_ts_1` − 900초를 덮지 못한다 → v1.2.3 12.2 final sweep chunk(chunk 11, `AS OF visible_scn_10`, extract window `[T_lb_1 − 1263초, wm)`, 논리 window `[wm, wm)`)가 anti-join 뒤 50건 적재(`overlap_recovered_rows = 50`), CAS 값 불변, FINALIZED 직후 final-fence source↔target 양방향 diff 0; 대조군 `EXTRACT_ONCE`는 단일 fence라 wm = `wm_1`이고 첫 NORMAL overlap `[wm_1 − 1263초, wm_1)`이 50건을 회수(INITIAL_LOAD 직후 부재는 설명된 상태) | sweep 계약: ledger `chunk_no = 11` row 1개(`window_low = window_high = wm`, `extract_window_low = T_lb_1 − 1263초`, `summary['etl.source_scn'] = visible_scn_10`, `cas_applied = true`, `overlap_recovered_rows = 50` — 양성 증거) ∧ watermark = wm(sweep CAS 전후 동일) ∧ FINALIZED 직후 §5.1 (4) 'Source `AS OF visible_scn_10`·`UPDATE_DT < wm` ↔ target' 양방향 PK 차집합 0(주입 50건 전부 존재, PK 중복 0) ∧ stub 거부 5건 ∧ ORA-01555 0 ∧ `next.window_low = prev.window_high` 연속(sweep row 포함) ∧ chunk 9·10 receipt에 주입 PK 0(양성 증거: 주입 시각 > chunk 8 `cas_at`); 대조군: INITIAL_LOAD FINALIZED 직후 50건 부재 → 첫 NORMAL `overlap_recovered_rows ≥ 50` ∧ 그 뒤 차집합 0; `PER_CHUNK_FENCE` + `delete_semantics = NONE_DECLARED` ZERO_GAP publish 시도 1건 → 422 `violations[].rule_id`에 `PER_CHUNK_FENCE_REQUIRES_SWEEP`과 `ZERO_GAP_REQUIRES_ENFORCED_BOUND`(`field = delete_semantics`) 둘 다 존재(양성 증거 — 두 rule 동시 위반, 7.2 5·6번); (b) 적재 중 hard delete 20건: INITIAL_LOAD 구간의 `delete_consistency` 표시가 선언값과 무관하게 **`BOUNDED_LAG`**로 상향(파생값 `initial_load_in_progress`와 함께 노출 — 11.3) ∧ `SOFT_DELETE` 회차는 final sweep(chunk 11) 이후 target 잔여 20건 = 0 ∧ `PK_RECONCILE` 회차는 `interval + freshness_slo` 이내 0이고 그 전 차이는 §3.2 비교 B의 **설명된 차이**(PK별 `비교 시각 − 마지막 Source 변경 관측 시각 ≤ delete_lag_slo_seconds`)로 분류 ∧ 설명되지 않은 차이 0; (c) heartbeat 60초 지연: sweep chunk의 `extract_window_low` = `T_lb_1 − overlap`(= `T_lb_1 − 1263초`, 12.2 v1.2.3) ∧ `T_lb_1 ≤ fence_ts_1`(양성 증거: `chunks:begin(1)`이 attempt에 기록한 `T_lb_1`과 `fence_ts_1` 동시 기록) ∧ 하한이 무지연 회차보다 **뒤로 밀린 회차 0**(앞으로 당겨지는 것은 보수적 방향이므로 합격) ∧ `fence_ts_1`을 하한으로 계산한 회차 0 ∧ 주입 50건 회수 불변 ∧ ORA-01555 0 | Watermark gap(ZERO_GAP), 데이터 차이 |
| FI-45 | **§3.5 3번(soak 4~5일차, FI-13 직후).** FI-13(b)의 executor 변형 — `numPartitions=2` Append-large에서 stub role 전환을 “N번째 이후 세션부터 PRIMARY”로 설정하되 N = driver precheck 세션 + 1(driver precheck는 standby에서 통과, 첫 executor connection부터 primary 리스너로 failover); 변형 (b): chunk 2 추출 중 executor task 1개를 kill해 task retry 재접속이 primary로 가게 함; 변형 (c): 같은 절차를 identity 상이 endpoint(§2.3, endpoint B)로 — FI-42(c)와 같은 결과를 executor 경로에서 재확인 | executor connection의 `sessionInitStatement` PL/SQL 블록이 `DATABASE_ROLE ≠ PHYSICAL STANDBY`로 ORA-20901(identity는 ORA-20902)을 내어 connection 자체가 열리지 않음 → task fail-fast(retry 0) → receipt `committed=false, exception_class = ORA-20901` → `attempt-failure {SOURCE_ROLE_MISMATCH}` → fencing → `CANCELLED(SOURCE_ROLE_MISMATCH)` 또는 `ABORTED_NO_COMMIT` + Source `HOLD_NEW` + `source role mismatch`(`mismatch_kind = ROLE`); (c)는 `SOURCE_IDENTITY_MISMATCH`·`mismatch_kind = IDENTITY`(v1.2.1 11.3, 10.1, 10.2 표); init 성공 뒤 전환은 FI-45d | (a)(b) stub 쿼리 로그: primary endpoint에서 `sessionInitStatement` 블록 실행 ≥ 1(양성 증거) ∧ primary endpoint의 data SELECT 0 ∧ Iceberg snapshot 0(commit 0) ∧ Spark event log의 해당 stage task retry 0 ∧ `attempt-failure.reason = SOURCE_ROLE_MISMATCH`·`exception_class = ORA-20901` ∧ `contract_state_history` `CANCELLED(SOURCE_ROLE_MISMATCH)` 또는 `ABORTED_NO_COMMIT` 1건 ∧ `maintenance_hold`(scope=SOURCE, reason=SOURCE_ROLE_MISMATCH) 1건 ∧ Outbox `source role mismatch` 1건(`mismatch_kind = ROLE`); (c) 같은 조건에 ORA-20902·reason `SOURCE_IDENTITY_MISMATCH`·`mismatch_kind = IDENTITY`; 대조군: driver precheck만 primary인 FI-13(b)와 결과 동일 | Primary 자동 fallback(executor 경로 포함) |
| FI-45d | **§3.5 3번(FI-45 직후, v1.2.2).** `numPartitions=2` Append-large(chunk 1 = 비어 있지 않은 구간)에서 stub role 전환을 '세션별 `sessionInitStatement` 완료 신호 뒤·첫 data SELECT 직전'으로 설정(stub 장애 주입 API의 세션 단계 조건 — init 블록은 standby에서 통과, 같은 connection의 SELECT는 PRIMARY role로 평가; 기존 read-only 세션 유지 에뮬레이션); 대조군: 예외 발생식을 제거한 Template(boolean 술어만, PoC 빌드 한정)으로 같은 절차 1회; **대조군 2(v1.2.3)**: role 전환 없이 **데이터가 유발한 ORA-01722**(NUMBER 캐스팅 대상 컬럼에 비수치 문자열 1건 주입)를 같은 Append-large에 일으켜 role mismatch 오분류가 일어나지 않는지 본다 | 12.3 v1.2.2 예외 발생식: data SELECT가 `CASE … THEN TO_NUMBER('SOURCE_ROLE_MISMATCH')`에서 ORA-01722(또는 `etl_assert_standby()` ORA-20901) → task fail-fast(retry 0) → receipt `committed=false, exception_class ∈ {ORA-01722, ORA-20901}` → `attempt-failure {SOURCE_ROLE_MISMATCH}` → fencing → `CANCELLED(SOURCE_ROLE_MISMATCH)`/`ABORTED_NO_COMMIT` + Source `HOLD_NEW`(`mismatch_kind`는 모니터 세션 재판정); 대조군은 boolean 술어가 false라 0 row → 0-row CAS(silent loss 재현 — 결함의 양성 대조, 운영 Template 금지 근거) | 본 케이스: stub 쿼리 로그에 init 블록 성공 ≥ 1 ∧ 같은 세션의 data SELECT가 오류로 끝남(ORA-01722 또는 ORA-20901 ≥ 1, 양성 증거) ∧ `attempt-failure.reason = SOURCE_ROLE_MISMATCH` ∧ 해당 chunk ledger `cas_applied` row 0 ∧ `FINALIZED_NO_DATA` 0 ∧ Iceberg snapshot 0 ∧ `maintenance_hold(reason=SOURCE_ROLE_MISMATCH)` 1건 ∧ Outbox `source role mismatch` 1건; 대조군: 같은 주입에서 0-row CAS 1건(결함 재현 — 시험 기록용); 대조군 2(데이터 유발 ORA-01722): 모니터 세션 재판정이 role 일치를 확인하므로 `attempt-failure.reason = SPARK_FAILED` **정확히 1건** ∧ `SOURCE_ROLE_MISMATCH` 0 ∧ `maintenance_hold` 생성 **0**(양성 증거: 같은 창의 Outbox `source role mismatch` 0) ∧ Iceberg snapshot 0 ∧ Source Hold 없이 RETRY 경로로 남아 다음 회차 정상 진행 | Primary 자동 fallback(init 뒤 전환), Watermark gap |
| FI-47 | **§3.5 3번(soak 4~5일차, FI-12 직후·Job A).** FI-12 Job A(ZERO_GAP, overlap 30분, expected_chunk_count ≥ 3, 11:00 회차 `original_logical_low = 10:00`)에 (a) 경계 row 두 묶음 — (a1) extract_low 경계(기존 row, `commit_at = 09:50`·10:00 회차가 이미 적재): `UPDATE_DT` ∈ {09:30:00.000000(= `extract_low`, SQL low literal 포함 경계 — 11:00 회차가 읽되 anti-join으로 제외), 09:29:59.999999(SQL 제외 경계 — 11:00 회차가 읽지 않음)} 각 1건; (a2) late-commit row(`commit_at = 10:05`, bound 내): `UPDATE_DT` ∈ {09:46:00.000000(overlap 내부, 1140초), 10:00:00.000000(논리 window 시작, 300초)} 각 1건 + bound 위반 대조 1건 `UPDATE_DT = 09:44:59`(1201초 — stub ENFORCED 거부 `REJECTED_BY_BOUND` 확인); (b) 재개 attempt 변형: 같은 회차에 `FAULT_KILL_AFTER_RECEIPT=1`(FI-07 본 케이스) → `PARTIAL_COMMIT` → 자동 새 attempt; attempt 1의 chunk 1 commit 뒤 `[09:30, 10:00)` 구간에 late-commit row 5건을 추가 주입(commit_scn > `visible_scn`) | (a) 11:00 회차의 `window.low == original_logical_low`인 chunk 1만 `extract_window_low = 09:30`(12.2 extract window — Guard 응답·`proceed(1).extract_low`에 Control이 계산해 실음, v1.2.2)으로 읽어 09:46:00·10:00:00 row 적재, 09:30:00 row는 읽되 anti-join 제외, 09:29:59.999999 row는 SQL low literal 밖이라 읽지 않음(stub 쿼리 로그 row count로 확인), 09:44:59 row는 stub `REJECTED_BY_BOUND`, `anti_join_dropped_rows = 1`, `overlap_recovered_rows = 1`(09:46:00 — 10:00:00은 논리 window); chunk ≥ 2는 `extract_window_low = low_k`; (b) attempt 2 chunk 1은 `low = high_1`, `extract_window_low = high_1`(`window.low = high_1 > original_logical_low`라 overlap 0 — v1.2.2 12.2 조건; 첫 CAS 전 실패 경로는 FI-47c), 추가 주입 5건은 `commit_scn > visible_scn`이라 attempt 2에 보이지 않고 12:00 회차 overlap(`[11:00−30분, …)`에 UPDATE_DT가 없으므로 회수 대상 아님 → **설명된 누락이 아니라 bound 위반**이어야 하므로 stub ENFORCED가 거부; 거부되지 않는 BEST_EFFORT 대조 Job에서는 Audit이 회수) — v1.2.2 12.2·13.1 | (a) receipt·ledger: chunk 1 `extract_window_low = 09:30:00` ∧ Guard 응답 `extract_window_low = 09:30:00`(Control 계산 양성 증거) ∧ `overlap_recovered_rows = 1` ∧ `anti_join_dropped_rows = 1` ∧ `written_rows == added-records`(13.1 검사 1 통과, `extracted_rows = written_rows + 1`) ∧ stub 거부 응답 1건(양성 증거) ∧ chunk ≥ 2 `extract_window_low = low_k` ∧ stub 쿼리 로그의 chunk 1 SQL low literal = 09:30:00·chunk 2 low literal = `high_1`; §3.2 비교 A overlap 행 차집합 0 ∧ PK 중복 0; (b) attempt 2 Guard 재개 응답·chunk 1 receipt `extract_window_low = high_1` ∧ `overlap_recovered_rows = 0` ∧ 접두 구간 일치·이중 commit 0 ∧ 추가 주입 5건: ENFORCED Source에서 stub 거부 5건(양성 증거) / BEST_EFFORT 대조 Job에서는 FINALIZED 직후 부재 → Audit 1주기 내 존재 | Watermark gap(ZERO_GAP), Append 이중 commit, 데이터 차이 |
| FI-47c | **§3.5 3번(FI-47 직후·Job A, v1.2.2).** FI-47 Job A 11:00 회차(`original_logical_low = 10:00`, overlap 30분)에 FI-47(a2)와 같은 bound 내 late-commit row 5건을 `[09:30, 10:00)` 구간에 주입한 뒤 attempt 1을 chunk 1 CAS **전** 두 지점에서 끝낸다 — (a) overlap extract 시작 전: Guard 성공 직후·`chunks:begin(1)` 전 Run Pod를 `kubectl delete pod --grace-period=0` + 재결합 차단(FI-07 방식) → 반입 시 SA 미생성 확인(UID 없음 — 재결합 대상 없음, `reattach_deadline` 기록 없이 즉시 `WRITER_FENCED`, 10.2 FAILURE 행·13.2 1번) → verdict `NO_COMMIT(RUN_WORKER_LOST)` → 자동 attempt 2; (b) overlap 구간 extract 뒤·chunk 1 commit 전: driver `FAULT_EXIT_BEFORE_WRITE`(쓰기 전 예외) → receipt `committed=false, exception_class` → `attempt-failure {SPARK_FAILED}` → fencing → `NO_COMMIT` → 운영자 RETRY로 attempt 2; 대조군 (c) FI-47(b)(chunk 1 CAS 뒤 kill) | v1.2.2 12.2: (a)(b) 모두 `window.low == original_logical_low`(= 10:00, `cas_applied` row 0)이므로 attempt 2의 Guard 재개 응답 `extract_window_low = 09:30`(Control 계산), `proceed(1).extract_low = 09:30`, chunk 1이 overlap 구간을 다시 읽어 5건을 anti-join 뒤 적재; (c)는 `low = high_1 > 10:00`이라 `extract_window_low = high_1`(overlap 0). attempt 2 chunk 1 CAS 뒤에는 같은 계약의 어떤 attempt도 overlap을 재적용하지 않는다 | (a)(b) attempt 1 ledger `cas_applied` row 0 ∧ `attempt_state_history` ADJUDICATED(verdict `NO_COMMIT`) 1건(양성 증거) ∧ attempt 2 Guard 응답·`proceed(1)`·receipt·ledger의 `extract_window_low = 09:30:00` ∧ stub 쿼리 로그의 attempt 2 chunk 1 SQL low literal = 09:30:00 ∧ 주입 5건 PK 집합 − target PK 집합 = ∅(§5.1 '주입 truth vs target') ∧ `overlap_recovered_rows = 5` ∧ PK 중복 0 ∧ 같은 `(contract, chunk)` snapshot 2개 0 ∧ §3.2 비교 A overlap 행 차집합 0; (c) attempt 2 `extract_window_low = high_1` ∧ `overlap_recovered_rows = 0`(대조 양성 증거); 세 경우 모두 `next.window_low = prev.window_high` ∧ FINALIZED | Watermark gap(ZERO_GAP), Append 이중 commit, 데이터 차이 |
| FI-48 | **Phase 3 진입 게이트**(Phase 2 shadow 뒤, 기존 Job 이관 전 — §3.5 8번; Phase 1·2 판정 범위 밖): stub Source에서 Airflow 역할의 old writer(`etl.*` 키 없는 직접 commit)를 둔 Append 2개·Merge 1개에 22장 16번 cutover runbook(v1.2.2 순서)을 수행하며 주입 — (a) runbook (2) 세션 0 확인 뒤 old writer가 late commit 1건 수행, (b) old writer 정지 확인 전 새 경로 첫 NORMAL 동시 시작 시도, (c) `watermark:seed` 후 첫 계약의 chunk 2 CAS 직전 Run Pod SIGKILL(`FAULT_KILL_AFTER_RECEIPT=2`), (d) 경계 reconcile 중 ORA-01555 주입, (e) **runbook (1) DRAIN Hold 생성 뒤·(2) old writer 정지 전** old writer가 정상 commit 1건(old watermark 전진분 포함) 수행 — v1.2.2 추가 | 22장 16번 runbook 7단계 순서(v1.2.2 재배열): (1) DRAIN Hold → (2) Airflow writer 정지 + `GV$SESSION` old writer 세션 0 → (3) **권위 old watermark + target head(`current-snapshot-id`) 캡처** → (4) `watermark:seed` → (5) 경계 reconcile → (6) lease 이전 → (7) 해제; (a) 13.1 lineage → `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` → repair REPLAY; (b) Hold 중 Guard `HOLD` 거부 또는 old writer 세션 > 0이면 runbook (2) 실패로 진행 금지; (c) 10.2 재결합 또는 Adjudication `PARTIAL_COMMIT` 재개; (d) 11.4 fail-fast → `SPARK_FAILED` → 운영자 RETRY(같은 SCN 재독 금지 시 `abort` 후 다음 회차); (e) 캡처가 세션 0 **뒤**이므로 (e)의 commit이 캡처 watermark·target head에 포함되고 seed는 stale이 아니다 — 첫 NORMAL window `low` = 캡처 wm, (e) 구간 재추출 없음 | (a) `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 1 ∧ repair REPLAY 후 §3.2 비교 A 차이 0 ∧ 자동 RETRY 0; (b) runbook (2) 전 새 경로 SA 생성 0 ∧ `guard_result = HOLD` ≥ 1(양성 증거) ∧ stub `GV$SESSION` old writer 세션 0 샘플이 (3) 캡처 이전에 존재; (c) 같은 `(contract, chunk)` snapshot 2개 0 ∧ `low = 마지막 CAS` 재개 ∧ 최종 `FINALIZED`; (d) ORA-01555 1(의도된 fail-fast) ∧ Iceberg 쓰기 0 ∧ RETRY/abort 후 경계 reconcile 완료; (e) stub commit 로그에서 (e) commit 시각이 Hold 생성 < commit < writer 정지 구간에 있음(양성 증거) ∧ `watermark:seed` 값 ≥ (e) commit 이후 old watermark ∧ 첫 NORMAL 계약 `original_logical_low` = seed 값 ∧ (e) 구간 row의 Append 이중 적재 0(§5.1 (3)(b) 쌍별 겹침 0·비교 A 차이 0); 전체: `watermark:seed` AuditEvent 1건(§5.1 gap 예외 지점), 캡처 AuditEvent(old wm·target head snapshot id)가 세션 0 샘플 뒤, seed 전후 ledger `window_low/high` 연속, 3 Job 모두 cutover 후 §3.2 비교 A·B 차이 0, 롤백 runbook 1회 drill(첫 신규 commit 전 = 포인터 복귀, 후 = forward repair만) 증거 | 데이터 차이, Append 이중 commit, Watermark gap (Phase 3 게이트) |
| FI-49 | Hold 겹침(soak 6일차, §3.5 4번 — SC-06 이전, §3.4 겹침 금지 규칙의 명시 예외; v1.2.2 단계 순서 교정): stub Source S1(Job 60개, 그중 24h Job 5개·Append-large chunk 진행 중 2개)에서 **H1 생성 전에** Job 1개(뒤의 H2 밖)에 `SCHEMA_DRIFT` 유도 → 같은 Job의 PLANNED 계약 2개(NORMAL + 수동 NORMAL 재제출 전 `cancel`한 뒤 REPLAY 1개)를 drift 상태에서 Guard 6번에 동시 도달시킴(자동 Job Hold H3 동시 생성 경합) → H3 생성 확인 → 운영자 Source scope `DRAIN` Hold H1 생성 → 5분 뒤 같은 Source의 Job 10개에 Job 목록 scope `HOLD_NEW` H2 생성 → 해제 순서 H2 → H3(schema 승인) → H1 | v1.2.1 14.1 겹침 의미: held = 덮는 open Hold ≥ 1, effective mode = max(FORCE_STOP > DRAIN > HOLD_NEW), `chunks:begin`은 하나라도 DRAIN이면 `DRAIN`, 자동 Hold `(scope, reason, key)` open unique(기존 hold_id 반환); 해제마다 14.2 CATCHUP, 남은 Hold에 덮인 CATCHUP은 Guard 4번 `VOID(SKIPPED_BY_HOLD)`(남은 hold_id)로 impacted 기록, 마지막 해제가 coverage 회수. 경합 시험이 H1 앞에 오는 이유: H1 아래 Job의 PLANNED는 Guard 4번 `HOLD`에서 닫혀 6번에 도달하지 못한다(10.2) | (a) H1+H2가 겹친 Append-large attempt의 `chunks:begin` 응답 `DRAIN` ≥ 1 → `CANCELLED_AT_SAFEPOINT`(H2가 HOLD_NEW여도 DRAIN 우선), 그 구간 SA 신규 생성 0 ∧ tick `SKIPPED_BY_HOLD` ≥ 1; (b) 경합 Guard 2건(H1 생성 전): `guard_result = SCHEMA_DRIFT` 2(둘 다 6번 도달 — 양성 증거: stub `ALL_TAB_COLUMNS` 조회 2) ∧ `maintenance_hold`(scope=JOB, reason=SCHEMA_DRIFT, key=job_id) open row 정확히 1 ∧ 두 계약의 `VOID(SCHEMA_DRIFT)`가 참조하는 `hold_id` 동일 ∧ unique 위반·deadlock 0; 그 뒤 같은 Job의 다음 tick Guard는 `guard_result = HOLD`(6번 미도달 — stub `ALL_TAB_COLUMNS` 조회 증가 0, hold_id = H3 또는 H1); (c) H2 해제: H2 Job 10개에 CATCHUP 10건 생성 ∧ 전부 Guard `HOLD` → `VOID(SKIPPED_BY_HOLD)`(hold_id = H1) ∧ SA 0; (d) H3 해제도 (c)와 같이 H1에 기록; (e) H1 해제: S1의 impacted Job 전부(`SKIPPED_BY_HOLD`·`CANCELLED_AT_SAFEPOINT`·`REJECTED_AT_GUARD`·(c)(d)의 VOID 포함, 24h Job 5개 포함)에 CATCHUP 1건씩 ∧ expected − 설명된 누락 = 0 ∧ Job당 `PLANNED` ≤ 2 ∧ 동시 token ≤ C; (f) 202 응답의 예상 소요·`expires_before_completion`(14.2 Source별 식)과 실제 `EXPIRED_UNLAUNCHED` 발생 일치 | Hold 중 SA 제출, 중복/누락 |
| FI-50 | **§3.5 3번(soak 4~5일차, FI-14 직후·같은 Polaris 프록시, Append-large `expected_chunk_count ≥ 3` 계약 X).** late-apply commit(v1.2.3 13.2 2번·13.1 Append OCC·10.2 base 연속성 검사): 프록시 변형 `hold_then_apply(release_anchor)` — client에는 즉시 504를 돌려주고, 보관한 `updateTable`을 **`release_anchor`** 시점에 Polaris로 전달한다. 앵커는 **클라이언트 신원**을 **attempt 단위**로 분류해(Run Pod IP — Template이 REST catalog `header.*`로 붙인 `X-Contract-Id` 헤더만으로는 **같은 계약의 attempt 1·2를 가르지 못하므로** 프록시 로그에 Run Pod IP를 반드시 남긴다, v1.2.3.1) 그 client의 카탈로그 호출로 정의한다. X의 attempt 1 chunk 1 `updateTable`(= A)을 보관한 채 다섯 앵커에 착지시킨다 — **(i)** attempt 2(= B)의 **Guard 완료(A 10.2 9번 응답 수신) 후 ∧ B의 Run Pod가 보내는 첫 base `loadTable` 이전**(프록시 앵커 `before_first_load_table(B)`, 3회 — v1.2.3.1 정정: A 10.2 chunk 루프대로 Run Pod는 base `loadTable` → `chunks:begin(1)`을 **SparkApplication 생성 전**에 수행하므로 6차의 'SA 생성 K8s 이벤트 직후 ∧ 첫 카탈로그 호출 이전'은 **도달 불가 앵커**였다: SA는 두 호출보다 뒤에 만들어진다. 기대 동작은 6차와 같다 — 이 앵커라야 B가 읽는 base가 H1′가 되어 `chunks:begin(1)`의 base 연속성 검사가 개입 snapshot을 보고 **SA 생성 전에** 차단한다); **(ii)** attempt 2(= B)의 `chunks:begin(1)` 200 후 ∧ B의 마지막 `loadTable`로부터 `t_gap` 경과(= B의 commit-전 `loadTable` 이전), `commit.retry.num-retries` 설정 2종(기본 4 / `0`) × 3회; **(iii)** 프록시가 그 pod의 `updateTable`을 붙잡은 상태(B의 commit-전 `loadTable` 후 ∧ B의 `updateTable` 도착 전), 설정 2종 × 3회; **(iv)** B의 `updateTable` 200 후(3회 — **대조군**); **(v)** ambiguous — `forward_then_504`(FI-14와 같은 훅, 응답만 504) 3회. 합계 **21회차**. 착지 시점은 의도이지 보장이 아니므로 **사후에 프록시 로그 순서(`loadTable`·`updateTable`의 도착·전달·응답 시각)로 case 귀속을 확정**하고, 기대와 다른 case를 밟았으면 **실제로 밟은 case로 재분류**해 그 case의 합격식으로 판정한다(재분류 건수·사유를 §8.2에 기록) | 전제(v1.2.3 13.1, `versions.lock` 고정 필수): T1 = `SnapshotProducer.apply()`가 **매 시도(첫 시도 포함) `refresh()`**, T2 = `AssertRefSnapshotID`는 **writer가 refresh한 head**를 assert, T3 = `num-retries` 기본 4 ∧ `CommitFailedException`만 재시도. attempt 1 공통: receipt `committed=null` → `attempt-failure {COMMIT_STATE_UNKNOWN}` → fencing → 13.2 2번 head-settle(`target_health_timeout_seconds` 간격 2회 연속 같은 `current-snapshot-id`) → `adjudicated_head_snapshot_id` 기록 → verdict `NO_COMMIT`(착지 전) → 운영자 RETRY. **(i)** A가 H0 → H1′로 착지 → B가 읽는 base = H1′ ≠ Guard 응답 `last_committed_snapshot_id`(= attempt 1 `adjudicated_head_snapshot_id`) → `chunks:begin(1)`이 **SA 쓰기 전** `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)`, attempt `BOUND → FENCED`·`adjudicated_head = base`, source token 즉시 `RELEASED`, Run FAILURE → repair REPLAY. **(ii)** T1에 의해 B가 H1′를 head로 보고 **첫 시도로 append 성공** → **409도 재시도도 없다** → 같은 논리 window의 ingest snapshot 2개. `chunks/{1}:commit`의 lineage 검사가 late snapshot(`etl.execution_contract_id = X`·`attempt_no = 1`) 개입을 탐지 → 13.1 (3) `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)`(ledger row `dq_result=EXTERNAL_SNAPSHOT`, CAS 없음) → `finalize {RECONCILIATION_REQUIRED}` → repair REPLAY(parent window 인수, partition replace). **(iii)** B의 assert가 불일치해 **409 발생**. ⑴ 기본(4): refresh 후 재시도가 성공 → 최종 상태는 (ii)와 같다(중복 2개). ⑵ `0`: `CommitFailedException` → `SPARK_FAILED` → Adjudication → late snapshot 1개만 남고 attempt 2 snapshot 0. **(iv)** T2에 의해 보관 요청의 assert가 현재 head와 불일치 → **Polaris가 409로 기각, 착지 실패**. v1.2.2의 옛 (iii)("attempt 2 `FINALIZED` 뒤 착지")이 **재현 불가**임을 증명하는 대조군이다. **(v)** pinned 버전 의존 — ≤1.10.x: `CommitStateUnknownException` 즉시 실패(`cleanAll` 미실행 → manifest 잔존) → receipt `committed=null` → Adjudication; ≥1.11.0: `reconcileOnSimpleUpdate`가 **1회 refresh로 조용히 성공 처리**(update set이 `AddSnapshot` + 같은 snapshot의 `SetSnapshotRef`뿐일 때만). (i)~(iv)에서는 **다른 attempt가 만든 개입 snapshot**을 `committed_snapshot_id`로 갖는 `cas_applied = true` row가 없다. (v)는 예외가 아니라 다른 사건이다(v1.2.3.1) — 착지한 snapshot이 **자기 attempt의 것**이므로 ≤1.10 분기에서는 13.2 3번대로 Adjudication이 그 snapshot에 대해 ledger row와 watermark CAS를 정확히 1회 대행한다(`EXTERNAL_SNAPSHOT`이 아니다) | **21회차 전부**를 사후 확정한 case의 식으로 판정한다(재분류된 회차는 재분류된 case의 식으로). 공통: repair·REPLAY 뒤 §3.2 비교 A 차이 0 ∧ PK 중복 0 ∧ §5.1 (3)(b) 이중 commit 4-불변식 위반 0 ∧ 자동 RETRY 0. **(i)** attempt 2 snapshot **0** ∧ 그 window의 `cas_applied = true` row 0 ∧ `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 전이 **정확히 1**(`contract_state_history`) ∧ 전이 시각 < attempt 2 SA 쓰기(K8s 감사 로그 SA 0) ∧ `lease_state_history` source token `GRANTED → RELEASED`(`RECLAIMED` row 없음) ∧ `attempt_state_history` `BOUND → FENCED` 1건 ∧ repair REPLAY `FINALIZED` 뒤 비교 A 차이 0. **(ii)** **`num-retries` 값과 무관하게** 409 **0** ∧ 재시도 **0** ∧ 같은 window ingest snapshot 정확히 **2** ∧ **둘 다 미채택** ∧ `RECONCILIATION_REQUIRED` 1 ∧ watermark 전진 0 ∧ repair 뒤 비교 A 차이 0. **(iii)** ⑴ 409 정확히 1 ∧ 재시도 1 ∧ 이후 (ii)의 술어 전부; ⑵ 409 1 ∧ 재시도 **0** ∧ attempt 2 snapshot **0** ∧ `RECONCILIATION_REQUIRED` 1 — 양쪽 모두 repair 뒤 비교 A 차이 0. **(iv)** 409 정확히 1 ∧ **late A snapshot 0**(보관 요청이 새 snapshot을 만들지 못했다) ∧ **B의 chunk 1 snapshot 정확히 1** ∧ **`snapshots`·`refs` 목록이 apply 전후로 불변**(보관분 전달 직전과 409 응답 직후 두 시점의 `loadTable` 결과가 동일) ∧ `RECONCILIATION_REQUIRED` **0**(오탐 0) ∧ 계약 정상 `FINALIZED`. v1.2.3.1 정정 — 이 앵커는 B가 이미 commit에 성공한 뒤이므로 6차의 '새 snapshot **0**'은 성립할 수 없다(B의 chunk 1 snapshot이 그 사이에 생긴다). 이 대조군이 세우는 명제는 '보관 요청이 카탈로그 상태를 아무것도 바꾸지 못했다'이며 그 술어가 위 3항이다. **(v)** **버전을 명시하지 않은 합격식은 쓰지 않는다** — (≥1.11) 단발 reconcile 성공 후 정상 `FINALIZED` ∧ `RECONCILIATION_REQUIRED` 0, (≤1.10) 즉시 실패(receipt `committed=null` → `attempt-failure {COMMIT_STATE_UNKNOWN}` → fencing) 후 **Adjudication이 head-settle로 이미 전달·착지한 그 snapshot을 S′에서 찾아 A 13.2 3번으로 판정**하는 것까지가 합격 조건이다 — verdict는 접두 규칙대로이며(이 fixture는 `expected_chunk_count ≥ 3`이고 A는 chunk 1이므로 `PARTIAL_COMMIT`, 접두가 1..expected를 모두 덮은 회차라면 `COMMIT`) ∧ 그 snapshot에 대한 chunk 1 ledger row **정확히 1** ∧ watermark CAS(`high_1`) **정확히 1** ∧ verdict `COMMIT` 회차면 finalize 대행 **정확히 1**(`FINALIZED`) ∧ 같은 snapshot에 대한 두 번째 ledger row·두 번째 CAS **0** ∧ `RECONCILIATION_REQUIRED` **0**(이 snapshot은 자기 attempt의 것이므로 EXTERNAL_SNAPSHOT이 아니다) ∧ 후속 attempt는 `window.low = 마지막 CAS 값`부터 재개 ∧ 중복 채택 0이며, **pinned 버전과 실제로 밟은 분기를 §8.2에 기록**한다. 양성 증거(**case별로 갈린다** — v1.2.3.1 정정: late snapshot 실재를 21회차 전부에 요구하면 (iv)의 '착지 실패' 합격식과 정면으로 모순된다). **21회차 공통**: 프록시 보관·전달 로그 21건(앵커별 도착·전달·응답 시각과 전달 응답의 상태·본문) ∧ 사후 case 귀속표(기대 case → 실제 case, 재분류 건수·사유). **(i)(ii)(iii)**: Polaris `snapshots`에 late snapshot **실재**(`etl.execution_contract_id = X`·`attempt_no = 1`·`chunk_no = 1`) ∧ 그 `sequence_number` > attempt 1 `adjudicated_head` ∧ attempt 1 `adjudicated_head_snapshot_id` non-null ∧ Adjudication AuditEvent에 head 읽기 ≥ 2회·마지막 2회 동일·간격 ≥ `target_health_timeout_seconds`. **(iv)**: late snapshot **부재**가 곧 양성 증거이며, 부재만으로는 '주입이 아예 안 됐다'와 구분되지 않으므로 **프록시가 받은 409 응답의 상태줄·본문**(`CommitFailedException` 계열 payload, 전달 시각 포함)을 §8.2에 기록하는 것이 이 case의 필수 증거다 ∧ attempt 1 `adjudicated_head_snapshot_id` non-null. **(v)**: 분기별 — (≥1.11) late snapshot 실재 ∧ 그것이 계약의 정상 chunk 1 snapshot이고 attempt 1이 정상 성공했으므로 `adjudicated_head_snapshot_id`는 **null이 정상**(Adjudication 자체가 없다), (≤1.10) late snapshot 실재 ∧ `adjudicated_head_snapshot_id` non-null ∧ 그 head가 late snapshot의 descendant-or-self ∧ Adjudication AuditEvent head 읽기 ≥ 2회·마지막 2회 동일. **측정 항목**(합격식과 별도로 §8.2 기록): ⑴ 탐지 지연 = late snapshot `committed_at` → `RECONCILIATION_REQUIRED` 전이, **case별 p50/p99**(경로마다 차수가 달라 단일 임계값을 두지 않고 v1.2.3 SLO 선언의 입력으로 올린다); ⑵ 노출 기간 = late `committed_at` → repair REPLAY `finalized_at`, p99를 선언 SLO와 대조; ⑶ 노출 중 §3.2 비교 A **1회 추가 실행**(관측값 — 합격식 아님); ⑷ case별 409 수·재시도 수 | Append 이중 commit, 데이터 차이, Adjudication 없는 attempt |
| FI-51 | cross-type target lease 경합(soak 1~3일차, §3.5 2번 — FI-17 직후, 같은 target table): 같은 table에 대해 3쌍 × 100회, 각 회차 두 요청을 Control API replica 2개에 동시 투입 — (i) `EXCLUSIVE_TABLE`(Full static replace 계약 Guard 8번) vs `APPEND`(Append 계약 Guard 8번), (ii) `EXCLUSIVE_TABLE` vs `PARTITION_OR_FILESET` **fileset 표현**(maintenance data compaction — gist 제약 밖), (iii) `PARTITION_OR_FILESET` fileset vs fileset(정규화 fileset key 교집합 ≠ ∅ — data file 1개 공유); 추가 회차 10회는 교집합 ∅인 fileset 쌍; (iv) **변형(v1.2.3)**: Control 프로세스를 거치지 않고 **raw PostgreSQL session에서 직접 DML**로 lease row를 밀어 넣는다 — (iv-a) 애플리케이션 role로 `target_lease` INSERT 1회, (iv-b) 권한 있는 소유자 role로 이미 GRANTED인 구간과 충돌하는 INSERT 1회 | v1.2.2 6.1 lock 순서 `… → watermark → target_table(table_id) row FOR UPDATE → target lease → source token`, 13.3 conflict matrix(`EXCLUSIVE_TABLE`은 모든 non-RELEASED와 충돌, fileset끼리는 정규화 key 교집합, `APPEND`는 `EXCLUSIVE_TABLE`과만) — 검사와 lease row insert가 `target_table` lock 아래 한 트랜잭션이므로 write skew 없음; 진 쪽은 bounded try-lock 실패 → 역순 해제 → `LEASE_BUSY`(200, `PLANNED` 유지 + backoff); 6.1 (9) DB 제약은 이중 방어 | 3쌍 × 100회 모두 `lease_state_history` `to_state = GRANTED`(non-RELEASED 겹침 구간) 정확히 1 ∧ `guard_result = LEASE_BUSY` 정확히 1(양성 증거 — 그 row의 `lease_grant` non-null ∧ Guard AuditEvent에 13.3 conflict matrix가 거부한 상대 `lease_id` 기록) ∧ §5.1 (2) 같은 target `EXCLUSIVE_TABLE` 구간 쌍별 겹침 0 ∧ deadlock·serialization 오류 0 ∧ unique/EXCLUDE 제약 위반 로그 0(제약에 닿기 전에 matrix가 거부); 교집합 ∅ fileset 쌍 10회는 둘 다 GRANTED(오탐 0); 진 쪽 역순 해제 — window 예약 row 잔존 0; (iv) raw session 직접 DML: (iv-a) 권한 거부 1건(`42501 insufficient_privilege` — 애플리케이션 role에 `target_lease` INSERT/UPDATE 권한 없음, 양성 증거: 같은 세션의 SELECT는 성공), (iv-b) 권한 있는 role에서도 행 트리거가 13.3 conflict matrix를 재검사해 `RAISE`로 거부 1건(오류 메시지에 충돌 `lease_id`) ∧ 두 경우 모두 `target_lease` row 증가 0 ∧ `lease_state_history` row 증가 0 ∧ 진행 중 계약 영향 0 | 중복/누락(§5.1 (2) lease 구간 겹침) |

### 3.5 시험 순서

1. SC-01(shard·전달 방식 결정) → FI-40(stub 가시성 자가검증 — v1.2.2 system tick 모델 포함: idle = table commit 0·tick 계속; 불합격 시 stub 수정 후 재실행, soak 미착수) → SC-02(burst) → SC-02b(poison item — SC-02 재실행 회차; §5.1 (1) 설명된 누락 `ITEM_REJECTED` 건수 = 주입 건 대조) → SC-02d(shard sensor 단일 제출 채널 지연 — `num_submit_workers` 1/4/16 각 3회차, soak 착수 전에 상한 선언값과 최소값을 §7에 확정) → SC-03 soak 시작(7일 연속, 이 기간에는 부하 모델을 바꾸지 않는다)
2. soak 1~3일차: FI-01~FI-04, FI-06(FI-06b cancel·max_runtime 변형 포함)~FI-09(Dagster·Run Pod 복구), FI-09c(terminal event·late evidence 순서 역전 — FI-09 직후), FI-17~FI-19, FI-51(cross-type target lease 경합 — FI-17 직후 같은 target, EXCLUSIVE_TABLE↔APPEND·EXCLUSIVE_TABLE↔PARTITION_OR_FILESET·PARTITION_OR_FILESET 범위 교차 3쌍 × 100회 동시 grant, v1.2.2 6.1·13.3), FI-43(잔존 Oracle 세션·관측 실패 fail-closed — FI-17 직후 같은 Source), FI-43c(미실현 reservation + 비태그 세션 조합·정상 finalize의 `RECLAIMED → probe → RELEASED` — FI-43 직후 같은 Source S, stub JDBC open 지연 옵션, v1.2.2 11.2 grant 식 `reserved_unrealized`), FI-33, FI-35(lease·Hold·멱등성·credential·stale 루프; (e) `submission_in_flight {resubmit_no, requested_at}` commit 직후·poll 응답 반환 전 Control crash 10건 + Dagster 미도달 대조 5건 → 재기동 시 poll이 `submission_recheck_after` 경과 뒤 tag `dagster/run_key = \"{contract_id}:{resubmit_no}\"`를 `runsOrError`로 확정하고, run이 없으면 `resubmit_no`를 올리지 않고 같은 `run_key`로 다시 반환(poll 채번·set 뒤 응답 전 crash) — v1.2.3.1 A 9.1·9.3, 5.4 Control scheduler 단일 프로세스 전제)
3. soak 4~5일차: FI-10(cancel 변형 포함)~FI-15(b′ 포함), FI-50(Polaris late apply — FI-14 직후 같은 프록시·Append-large expected_chunk_count ≥ 3: 프록시 hold_then_apply(release_anchor)로 attempt 1 chunk 1 updateTable을 보관해 (i) attempt 2 Guard 완료(9번 응답) 후·그 pod의 첫 base loadTable 전 3회(프록시 앵커 before_first_load_table(B) — A 10.2대로 base loadTable·chunks:begin(1)이 SA 생성보다 먼저라 6차의 'SA 생성 직후' 앵커는 도달 불가였다; 이 앵커라야 base 연속성 검사가 SA 생성 전에 차단한다), (ii) chunks:begin(1) 200 후 ∧ 마지막 loadTable로부터 t_gap 경과 시점에서 num-retries 4/0 각 3회, (iii) 프록시가 B의 updateTable을 붙잡은 상태에서 num-retries 4/0 각 3회, (iv) B의 updateTable 200 후 3회(대조군 — v1.2.2 옛 (iii) 착지가 재현 불가임을 증명; 합격 = late A snapshot 0 ∧ B의 chunk 1 snapshot 정확히 1 ∧ snapshots·refs 목록이 apply 전후로 불변 — B가 이미 commit했으므로 '새 snapshot 0'은 쓰지 않는다), (v) ambiguous forward_then_504 3회 = 합계 21회차; 앵커는 클라이언트 신원을 attempt 단위(Run Pod IP 필수 — X-Contract-Id만으로는 attempt를 못 가른다)로 분류하고 사후에 프록시 로그 순서로 case 귀속을 확정·재분류(§8.2) — 합격 = case별 식(409 수·재시도 수 포함) 충족 ∧ (ii)(iii)⑴에서 생긴 중복 snapshot 양쪽 다 미채택 ∧ (iv)는 late A snapshot 0·snapshots 불변 ∧ (v) ≤1.10 분기는 Adjudication이 착지 snapshot을 S′에서 찾아 ledger row·watermark CAS를 정확히 1회 대행(접두가 1..expected를 모두 덮은 회차면 verdict COMMIT + finalize 대행 1회)한 것까지 확인 ∧ repair 뒤 §3.2 비교 A 차이 0, 측정 4종(탐지 지연·노출 기간·노출 중 비교 A·409/재시도) 기록, v1.2.3 13.1·13.2·10.2), FI-16(정시 burst 직전; (B) ORA-28002 변형은 A 회복 후 별도 회차), FI-20 전체, FI-21(accept-empty까지·(d) target unchanged 3연속), FI-22(seed 분기 + (a′) 순서 변형: RETRY가 EXPIRY 스캐너보다 먼저 contract row lock에 도달해도 같은 `CANCELLED(EXPIRED)` + 409 `CONTRACT_CLOSED` — v1.2.2 10.2 1번·14.3 inline expiry), FI-25((c) `max_runtime` 경계 변형 포함), FI-26, FI-28(변형 (c) outage > `expires_at` 포함)~FI-31, FI-34((c′) partition 변형 포함), FI-37(a/b/c 포함), FI-38, FI-41(ENFORCED bound·eligible PK 차집합 — FI-12 직후 같은 Source), FI-41e(heartbeat witness 지연 주입 → `high = min(T_lb, SYSTIMESTAMP_standby) − safety_lag` 보수화만·누락 0, `SCN_TO_TIMESTAMP` 전용 Source는 BEST_EFFORT 422 — FI-41 직후 같은 Source, v1.2.2 11.3)·FI-41f(delete semantics 3건: `CDC_LATER` ZERO_GAP publish 422·`PK_RECONCILE` + hard delete interval 내 반영·`SOFT_DELETE` watermark 동기 — 같은 창, §3.2 양방향 차집합)·FI-41g(`SYNC_COMMIT_GUARD` Source — bound 내 commit 회수 / bound 초과 commit 거부·Source row 미생성 → 이후 회차 gap 0; 실 DG 시험 인스턴스 회차는 §3.5 6번 G2, 여기서는 stub 대조 회차, v1.2.3 §7.7 C1~C4)·FI-41h(`TXN_AGE_KILL_JOB` Source — kill miss로 bound 초과 commit 착지 → 누락 재현 → Audit 탐지·repair; BEST_EFFORT 안전망 시험이라 **G2 목록 밖**, v1.2.3.1 분리), FI-47(extract window 경계·재개 attempt — FI-12 직후 Job A), FI-47c(첫 CAS 전 실패(overlap extract 전 / extract 후 commit 전 두 지점 kill) 뒤 attempt 2가 overlap 재적용 — `contract.window.low == original_logical_low` 조건, Guard 응답 `extract_window_low = L − O`, overlap row 전부 회수 — FI-47 직후 Job A, v1.2.2 12.2), FI-42(DB identity 상이 — FI-13과 같은 창), FI-42(e)(같은 CDB·같은 `cdb_dbid`/`DB_UNIQUE_NAME`·다른 `CON_NAME`/`pdb_guid` endpoint — FI-42와 같은 창·같은 fixture, 11.3 init 블록 ORA-20902·12.3 `CON_NAME` 술어, v1.2.2), FI-45(executor 재접속·task retry·failover — FI-13 직후), FI-45d(`sessionInitStatement` 성공 뒤 첫 data SELECT 직전 role 전환 → 예외 발생식 술어 ORA-01722 ≥ 1·CAS 0·`FINALIZED_NO_DATA` 0 — FI-45 직후, v1.2.2 12.3), FI-44(undo churn·`chunks:begin` deadline·INITIAL_LOAD fence_mode), FI-44e(PER_CHUNK_FENCE final sweep chunk `[T_lb_1 − overlap, wm)` — sweep 구간 경계 late commit(bound 내) + final-fence source↔target 양방향 diff 0, sweep 불가 Source는 BEST_EFFORT 강등 422 — FI-44(d) 직후 같은 INITIAL_LOAD fixture, v1.2.2 12.2·11.4), FI-05b(5일차 정시 burst 직후 — Global Hold를 만들므로 §3.4 겹침 금지 규칙 적용, failover 직전 dump로 soak 기록 보존)
4. soak 6~7일차: Phase 1 잔여 FI, FI-36, FI-49(Hold 겹침 — SC-06 이전; v1.2.2 순서 교정: (b) 자동 Hold unique 경합 단계를 H1 생성 **전**에서 수행 → H3 확인 → H1·H2 생성 → 해제 H2 → H3 → H1, 기대값 (b)~(e) 유지), SC-06(Hold 해제 catch-up — soak 부하 위에서), SC-11(compaction 동시성 — v1.2.2 보강 fixture: maintenance lease 만료 시 SA 삭제·pod 부재 확인 회수와 그 뒤 도착한 maintenance snapshot의 `RECONCILIATION_REQUIRED`, `expire_snapshots`(만료 제외 집합 계약 1건 포함)·`remove_orphan_files(orphan_min_age)`가 진행 중 attempt 파일·staging을 지우지 않음, active attempt branch ref 보호, 하한 미만 `orphan_min_age` JobSpec 422 — §7 행과 일치), SC-08(증가율·UI p95·retention 비용 외삽)·SC-09·SC-10 집계, FI-39
5. **soak 종료 후(8~9일차)**: FI-05(Control PG PITR — conservative 기대집합: snapshot 없는 0-row CAS 제외·watermark 후퇴 방향만 허용(FI-05b (c)와 동일), soak 기록 보존을 위해 마지막), FI-05c(resync 단계 (1)~(4) 각각 직후 Control 재사망 → 재기동 시 Hold(RESYNC)·operation row의 완료 step부터 재개, 중복 fencing·중복 lease·CAS 전진 0 — FI-05 직후 같은 dump·fixture 재사용, v1.2.2 5.4), SC-04 stress → SC-04b(queue cap 실제 포화 — SC-04 직후, v1.2.2 backlog 5분해 식), SC-02c(500 Job 단일 Source 집중 — 기본안·precursor 비교안 각 1회), SC-05 Source 포화, SC-07
6. 실 DR 시간대: 24 Job matrix 정합성(§3.1·§3.2 — `ETL_CANON` canonical hash 양방향 비교(§3.2 v1.2.3 — 좁은 행 `STANDARD_HASH(RAW,'SHA256')` / 광폭·LOB `DBMS_CRYPTO.HASH(BLOB, HASH_SH256)` 두 경로), 닫힌 구간 backfill·WAP 경로 포함), H-03·H-06 측정(H-06 변형: Merge-large chunk 2 branch 진행 중 main에 lease 우회 commit 1회 주입(FI-36(a) 절차 — lease를 지키는 maintenance는 attempt 내내 EXCLUSIVE_TABLE에 막힘) → fast_forward 기각 → receipt FAST_FORWARD_REJECTED·attempt-failure {CHUNK_DQ_FAILED} → Adjudication이 13.2 3번 분류로 RECONCILIATION_REQUIRED(chunk 1 CAS 보존) ∧ main = 주입 snapshot ∧ ledger에 branch-only snapshot 0 — v1.2.2 13.1 WAP, §5.1 (3)(a) main-ancestry; 추가 변형 1건 — fast_forward 요청에만 forward_then_504 주입(FI-14와 같은 훅): SetSnapshotRef만인 commit이라 1.11 reconcile 대상이 아니므로 버전 무관 COMMIT_STATE_UNKNOWN → Adjudication 경로를 확인, v1.2.3 13.1); DG 시험 인스턴스(확보 시, §2.1): §8.3 ‘Oracle ZERO_GAP Go’ 주입 6종(long transaction·late commit·role transition·undo churn·executor reconnect·old timestamp — FI-12·13·41·44·45 절차의 실 Oracle 회차) + v1.2.2 추가 2항(heartbeat witness 실 Oracle 회차 — FI-41e 절차, `T_lb ≤` 실제 commit 시각 방향 보장 확인; role 전환 시 기존 read-only 세션 종료 여부 — FI-45d 절차, P1-08 전제 검증) + §5.1 (4) `AS OF SCN` truth 판정; 마지막으로 **FI-41i**(G0 무효화 전파 드릴 — `NLS_NCHAR_CHARACTERSET` 또는 `ETL_CANON` source digest 변경 → 의존 Source 전부 `zero_gap_verified := false` + Source별 `HOLD_NEW(ZERO_GAP_INVALIDATED)` 1건 → 재실증까지 한 회차, v1.2.3.1 신설: G0 무효화는 그 시점 이후 G1·G2를 미판정으로 만들므로 §3.5의 모든 G1·G2 증거 수집을 마친 뒤 수행한다)
7. Phase 2 게이트(Phase 2 구현 후, 24 Job shadow 비교 시작 전): FI-21 RERUN_LATEST 분기, FI-23(c 포함 — `dq:accept` chunk 단위 CAS·`resolve` 제한), FI-23c(attempt 2 = chunk 1·2 재번호, `dq:accept` 뒤 attempt `FENCED → ADJUDICATED` 전이 — v1.2.2), FI-24a/b(FI-24b split 변형 포함), FI-24c(release activation boundary — 미래 `effective_from` 요청 422, schedule 삭제/cron 이동 release를 정각 직전 DEPLOYED 후 ACTIVE 412 유도 → 복귀 후 release operation 말미의 자동 Gap Recovery(`scope={shard}, range=[deployed_at − 1 period, now()]`, actor SYSTEM)가 누락 tick을 R1으로 실행, `expected occurrence missing`·VOID 0; 412 복귀 경로는 FI-24b와 공유, v1.2.2 6.2·9.3), FI-27((a) identity 단언 포함), FI-32, SC-08 purge 행, §5 "표준 Job 신규 등록", §6 운영 절차 체크리스트의 Custom UI 측
8. **Phase 3 진입 게이트**(Phase 2 24 Job shadow 뒤, 기존 Job 이관 전 — Phase 1·2 판정 아님): FI-48(cutover runbook — v1.2.2 22장 16번 재배열 순서: DRAIN Hold → old writer 정지·세션 0 → 권위 old watermark + target head 캡처 → `watermark:seed` → reconcile → lease 이전 → 해제; 변형: 구 순서의 (1)~(3) 사이에 old writer commit 1건 주입 → 새 순서에서는 캡처 전 정지라 seed가 stale하지 않음, Append 이중 적재 0) — 22장 16번·20장 Phase 3
9. 판정 회의: §5 표 채우기(§8.3 ‘Scale/Control Go’·‘Oracle ZERO_GAP Go’ 각각), §4 가설 결론, §8 리포트

---

## 4. 가설 시험 (PoC에서 판정)

리뷰 §1.4 ②. 각 가설은 **측정 → 판정 기준 → 결과에 따른 v1.2 결정**으로 닫는다.

| ID | 가설 | 시험 | 측정 | 판정 기준 | 결과 → v1.2 결정 |
|---|---|---|---|---|---|
| H-01 | 500 occurrence tick(v1.2.3 9.1 — tick RunRequest 0)이 기본 gRPC timeout(60초) 안에 batch create-or-get 1회로 끝난다 | SC-02, 단일 shard 500 worst case | batch p99, tick wall time, `num_submit_workers` on/off | batch p99 ≤ 50초, `max_tick_retries` 소진 tick 0(재시도로 회복된 실패 tick은 집계만) | 충족: batch 유지, timeout 기본값. 미달: `DAGSTER_SCHEDULE_GRPC_TIMEOUT_SECONDS` 상향 + tick 분할(shard 세분화) 또는 precomputed occurrence + cursor sensor 승격(9.3) |
| H-02 | 1 주기 이상 daemon 장애 후 coalesced recovery가 중복·누락 없이 동작한다 | FI-02 | disposition 완전성, 복원 제출 수 | 중복/누락 0, 복구 직후 Source별 동시 token ≤ C | 미달: Gap Recovery 범위·우선순위 조정, cursor sensor 승격 |
| H-03 | SourceVisibilityFence + overlap ≥ `max_commit_minus_watermark_seconds + safety_lag + clock_skew`(`bound_kind=ENFORCED`·`DB_TRIGGER`·`not_null`·`updated_on_every_change`·`fence_time_witness=HEARTBEAT_TABLE`·`delete_semantics ∉ {NONE_DECLARED, CDC_LATER}` Source에만 ZERO_GAP 허용, v1.2.2 7.2 5번)로 DR lag·late commit 누락이 0이다 | FI-10~FI-12, FI-41, FI-47, 실 DR 24 Job 정합성(ZERO_GAP Job은 ENFORCED Source만) | ZERO_GAP 누락 수(FI-41 eligible PK 차집합 + §3.2 비교 A·B), BEST_EFFORT audit 탐지·repair, `SCN_TO_TIMESTAMP` 실측 오차(`clock_skew` 확정 입력) | ZERO_GAP 누락 0(FI-41 차집합 0 포함 — ledger 인접성 0건만으로는 불충분), BEST_EFFORT repair 100%·탐지 지연 ≤ audit 주기, `SCN_TO_TIMESTAMP` 실측 오차 ≤ `clock_skew` | 미달: `STANDBY_VISIBLE_SCN` cutoff 운영 승격 검토, overlap 계수·`clock_skew` 조정; ENFORCED Source가 0개면 ZERO_GAP 출시 자체를 보류(BEST_EFFORT 한정) |
| H-04 | Run Pod 사망 후 재결합 경로가 Spark 재실행 없이 이어진다 | FI-06~FI-09 | 재결합 성공률, Pipes 재생 정확도, `reattach_grace_seconds`·`chunk_proceed_timeout_seconds` 적정성 | 재결합 100%, 이중 commit 0 | 미달: `k8s_job_executor` 채택(Dagster resume 지원) 트레이드오프 재평가 |
| H-05 | Commit Adjudication이 REST 5xx·driver 사망·부분 chunk·외부 writer에서 정확히 판정한다 | FI-14, FI-15, FI-07, FI-36, FI-37 | 판정 정확도, `RECONCILIATION_REQUIRED` 오탐률(분모 = FI-36 주입 + 정상 commit), snapshot summary 키 누락률 | 정확도 100%, 오탐 0, summary 키 누락 0 | 미달: commit-wrapper 보강, WAP(H-06) 기본화 검토 |
| H-06 | Critical Merge/Full에 attempt branch → DQ → `fast_forward`가 사용 Iceberg/Polaris 버전에서 동작하고 비용이 감당된다 — 그리고 v1.2.2 13.1 WAP 규칙(ff 성공 뒤에만 `chunks/{n}:commit`·CAS, ledger = `published_main_snapshot_id`)이 concurrent main commit에서 오판하지 않는다 | 실 DR Merge-large 2개 + Full-large 1개(§3.5 6번); **변형**: Merge-large 1개의 chunk 2 branch 진행 중 main에 lease 우회 commit 1회 주입(FI-36(a) 절차 — `etl.*` 키 없음; lease를 지키는 maintenance는 attempt 내내 `EXCLUSIVE_TABLE`에 막혀 주입 불가) → ff 기각 → 운영자 `resolve`·repair 뒤 재실행; **변형 2(504 on fast_forward, 1건)**: `fast_forward` 요청에만 프록시 `forward_then_504`를 적용(FI-14와 같은 훅) — fast-forward commit은 update set이 `SetSnapshotRef`뿐이라 ≥1.11.0의 `reconcileOnSimpleUpdate` 대상이 아니므로 pinned 버전과 무관하게 `CommitStateUnknownException` 경로를 탄다(v1.2.3 13.1) | fast_forward 성공률, branch 메타데이터 증가, refs 정리, Polaris 호환; 변형: ff 기각 경로와 main 불변 | fast_forward 성공률 100%(변형 제외), branch 메타데이터(metadata.json·manifest list) 증가 ≤ main 대비 +20%/일, refs 정리 후 잔존 branch 0, Polaris에서 `write.wap.enabled`·branch ref 조회 오류 0; 정상 회차 ledger `committed_snapshot_id = published_main_snapshot_id = branch_head_snapshot_id` ∧ 전부 current main의 ancestor; **변형**: receipt `committed=false, exception_class = FAST_FORWARD_REJECTED` ∧ main current snapshot = 주입 snapshot(attempt branch snapshot은 main ancestor 아님) ∧ `attempt-failure.reason = CHUNK_DQ_FAILED` ∧ Adjudication이 주입 snapshot을 13.2 3번 분류로 `RECONCILIATION_REQUIRED`(chunk 1 CAS 보존) ∧ ledger에 branch-only snapshot을 `committed_snapshot_id`로 가진 row 0 ∧ 자동 RETRY 0 ∧ 양성 증거: attempt 내내 같은 table의 maintenance Job `guard_result = LEASE_BUSY` ≥ 1(`EXCLUSIVE_TABLE` 승격) ∧ repair 뒤 §3.2 비교 A 차이 0; **변형 2**: receipt `committed=null` ∧ `attempt-failure {COMMIT_STATE_UNKNOWN}` ∧ Adjudication이 main ancestry로 ff 착지 여부를 확정(`published_main_snapshot_id`가 main ancestor면 `COMMIT`, 부재면 `NO_COMMIT`) ∧ ledger CAS는 `COMMIT` 확정 회차에만 1건 ∧ branch-only snapshot을 `committed_snapshot_id`로 가진 row 0 ∧ 자동 RETRY 0 ∧ 재실행 뒤 §3.2 비교 A 차이 0 | 충족: Critical 테이블 등급에 WAP 채택(22장 6번). 미달·미결: WAP 미채택, fencing만으로 운영 |
| H-07 | `PARTITION_OR_FILESET` lease + Iceberg OCC로 compaction과 APPEND ingestion이 병행된다 | SC-11 | ingestion 지연, compaction 기아, conflict retry, `use-starting-sequence-number` 효과 | compaction 병행 중 Append 1h 주기 lateness p95 증가 ≤ +5분(H-12 대비), compaction 완료율 100%(기아 0), Iceberg conflict retry ≤ 3회/commit, `RECONCILIATION_REQUIRED` 0(maintenance snapshot이 S′에서 제외) | 미달: lease 단계 재조정, compaction offset 정책 |
| H-08 | Full 테이블 snapshot retention 등급별 용량이 예산 내다 | SC-10 + Full-large 4개 | retention 1·3·7일별 GB, ledger로 증거 보존 충분성 | 용량 모델(리뷰 B-6) 예산 내 | retention 등급 수치 확정(22장 7번) |
| H-09 | lease 3단계 회수(`EXPIRING → RECLAIMED → RELEASED`) + 관측 fence가 `V$SESSION` 실측과 일치한다 | FI-17, FI-43, SC-02 | `GV$SESSION` vs pool 회계 차이, driver_sessions 실측 weight | 부여/회수 이벤트 ±5초 밖 샘플에서 차이 0, 경계 샘플 차이 ≤ 1 token; weight 계수 확정 | weight 정의(11.2) 수치 확정, 가변 numPartitions 확대 전제 |
| H-10 | Run Pod-per-Run이 burst 500에서 자원·기동 시간 예산 내이고 Operator가 병목이 아니다 | SC-02, SC-04, SC-07 | Run Pod RSS·start, `t0→t4` p95(보호 지연 제외), Operator CR→RUNNING p95, 노드 용량 | `t0→t4` p95 ≤ 2분, 노드 예산 내 | 미달: 경량 이미지·pre-pull, 소형 Job multi-asset 묶음, fire-and-observe 패턴, Operator replica/worker 조정 |
| H-11 | Source별 용량 모델 ρ = Σ_i(D_i × w_i / period_i) / C_S와 publish Gate 임계(0.7/1.0)가 실측과 맞는다 | SC-05, SC-02c, soak ρ 기록 | 예측 ρ vs 실측 queue 깊이·lateness, 비실행 disposition 비율(`EXPIRED_UNLAUNCHED`·`COALESCED_INTO`), 단일 Source 집중 시 Guard p99·모니터 세션 대기 | 예측 오차 < 20% | 계수·임계 확정(22장 11번); SC-02c 기본안 Guard p99 미달 시 fence precursor 비교안 채택을 v1.2.2 입력으로 |
| H-12 | Job class별 freshness SLO 수치가 달성 가능하다 | soak lateness 분해 | 1h 주기 p95, 24h 주기 p95, 분해 비율, breach 이벤트 정확도 | 1h 주기 p95 ≤ 20분, 24h 주기 p95 ≤ 60분(잠정, Phase 0 baseline으로 보정) | `freshness_slo` 수치 확정(22장 22번 잠정값 보정; 임계값·계산 주체·dedup은 v1.2 16.4에서 확정) |
| H-13 | Bundle 전달(이미지 내장 vs AIStor pull)과 shard 수가 publish 가시화·cold load 기준을 만족한다 | SC-01 | publish→가시화 단계별 p95, cold load, reload 중 tick 손실, Control API 차단 중 cold start | publish p95 3분(단계별 예산: compile+validate ≤60초, Bundle 게시(DEPLOYED: AIStor put 또는 이미지 build·push) ≤20초, 승격 ≤10초, reload ≤60초, webserver 갱신 ≤30초 — 이미지 내장 방식이 게시 20초를 넘기면 p95 3분은 AIStor pull 방식 기준으로 판정하고 이미지 방식은 '사전 빌드 제외 시간'으로 별도 기록), cold load 10분, tick 손실 0, **두 방식 모두 Control API 차단 중 cold start 성공** | 전달 방식·shard 수 확정(22장 8번) |

---

## 5. 합격 기준 (SLO 표 확장)

v1.1 19.4(v1.2에서 이 문서로 이관)의 각 행에 **측정 방법·기준 유형(확정/가설/Phase 2)·No-Go 여부**를 붙였다.

| 항목 | PoC 기준 | 측정 방법 | 유형 | No-Go |
|---|---|---|---|---|
| 데이터 정합성 | 설명되지 않은 row/hash 차이 0, 미비교 0 | §3.2 비교 A·B | 확정 | ✔ |
| 정상 Run 중복/누락 | 7일 40,000/일에서 0 — 기대 집합 = cron 전개 ∪ CATCHUP 키 − 설명된 누락(`ITEM_REJECTED` Outbox row — 건수 = SC-02b 주입 건, v1.2.2) | §5.1 (1) expected 키당 occurrence 정확히 1개(설명된 누락 제외) + (2) 같은 `(job, lane)`의 writer·lease 구간 쌍별 겹침 0 | 확정 | ✔ |
| PK 중복 | 0 | Iceberg 대상 snapshot PK 유일성 쿼리(13.1 검사 2) | 확정 | ✔ |
| Watermark gap/regression | ZERO_GAP: 주입 truth vs target 차집합 0(§5.1 (4)) ∧ 보조 불변식 ledger gap 0·regression 0 / BEST_EFFORT: regression 0, audit 탐지 gap의 repair 완료율 100%, 탐지 지연 p95 ≤ audit 주기 | §5.1 (4) 비교 A(stub 생성 truth·실 DR `AS OF SCN`) + 보조 ledger 쿼리 + FI-12 주입 시각 → 탐지 시각 | 확정 | ✔ |
| Snapshot contract metadata | 대상 commit 100% — 분모에서 §8.2 기록 `snapshot_id` allowlist(FI-31·FI-36 주입 snapshot) 제외. **이 allowlist의 적용 범위는 이 행(메타데이터 부재 판정)뿐이며 §5.1 (3)(b) 이중 commit 판정과 분리된다**(v1.2.3 — 이중 commit 4-불변식에는 allowlist 예외가 없다) | §5.1 'Snapshot metadata 100%' | 확정 | — |
| Daemon/Code Location 복구 | 30분 이내, 내부 목표 5분 | FI-01(a), FI-03 | 확정 | ✔(반복 실패) |
| Definition cold load | 10분 이내 (shard당 cold load 시간 = tick 손실 창) | SC-01 | 가설(H-13) | — |
| publish 가시화 | p95 3분, p99 5분 (단계별 예산은 H-13) | SC-01 | 가설(H-13) | — |
| 500 occurrence batch create-or-get | p99 ≤ gRPC timeout − 10초, `max_tick_retries` 소진 tick 0 | SC-02 | 가설(H-01) | — |
| schedule tick (PoC 신규 행) | tick 소요 p95 30초, `max_tick_retries` 소진 tick 0, 재시도로 회복된 실패 tick은 집계만 | SC-02, soak | 가설(H-01) | — |
| Guard 거부 시 오탐 실패 알림 | 0 | 10.2 표에서 Run 결과가 SUCCESS로 규정된 거부 사유(`CONTRACT_CLOSED`·`ATTEMPT_ALREADY_BOUND`·`RETRY_REQUIRED`·`HOLD`·`OPEN_WINDOW`·`PINNED_RELEASE_INACTIVE`·`INTERFACE_MISMATCH` split 분기(더 새 ACTIVE 없음, v1.2.1 10.2 5번)·Guard 감지 `SOURCE_ROLE_MISMATCH`·`SCHEMA_DRIFT`·`SOURCE_LAG_EXCEEDED`·`FENCE_UNAVAILABLE`·`LEASE_BUSY`·`CREDENTIAL_REVOKED`·`CONNECTION_REVOKED`·Guard `TARGET_UNAVAILABLE`·`CONTROL_API_UNAVAILABLE`·`FENCE_EXPIRED`)에 한해 Dagster 상태 = SUCCESS 및 실패 알림 0. `INTERFACE_MISMATCH`(더 새 ACTIVE 있음 분기)·`DIRECT_LAUNCH_FORBIDDEN`·driver `SOURCE_ROLE_MISMATCH`·`DQ_FAILED`·`RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)`·`attempt-failure` 계열(`TARGET_UNAVAILABLE`·`CREDENTIAL_FAILURE` 포함)은 의도된 FAILURE로 제외 | 확정 | — |
| 500 Run queue 등록 | 5분 이내 | SC-02 `t0` 분포 | 확정 | — |
| queue→launch (`t0→t4`, 보호 지연 제외, `guard_result = OK` 모집단) | p95 2분, p99 5분 | SC-02, soak | 가설(H-10) | — |
| Spark Operator CR→driver RUNNING | p95 기준 PoC에서 확정 | SC-07 | 가설(H-10) | — |
| Job별 freshness (lateness) | 1h 주기 p95 ≤ 20분, 24h 주기 p95 ≤ 60분 (잠정) | soak lateness | 가설(H-12) | — |
| freshness breach 알림 정확도 | 기대 breach 집합 = NORMAL·CATCHUP occurrence 중 `SKIPPED_BY_HOLD`·`SUPERSEDED_BY(*)` 제외, (a) `finalized_at IS NULL ∧ now() − logical_scheduled_at > freshness_slo` 또는 (b) `finalized_at − logical_scheduled_at > freshness_slo`(`COALESCED_INTO(c)`는 c의 `finalized_at`; **`FINALIZED_NO_DATA`는 orchestration freshness**로 정시 집계 — v1.2.2 16.4: `target_unchanged = true`여도 breach가 아니며, data freshness는 16.2 read model 파생 지표 target publication age·coverage와 `target unchanged` 경고(연속 `target_unchanged_alert_count`회, Source 단위 1건 — FI-21 `RETAIN_PREVIOUS` 3연속 변형으로 검증: 경고 정확히 1, breach 0)가 맡는다) — 이벤트 `(job_id, logical_scheduled_at)`당 정확히 1건(누락 0·오탐 0·중복 0), 탐지 지연 ≤ `planned_scan_interval` + Outbox 전송, breach 후 finalize된 건은 `Job recovered` 1건; "occurrence 없음" 알림 ≤ `planned_stale_after` + `planned_scan_interval`(FI-38) | Outbox 이벤트 vs `contract.finalized_at`·`contract_state_history` 대조; payload의 `target_unchanged`·publication age·coverage는 contract 플래그·ledger `cas_at`·`window_high` 대조(v1.2.3 — coverage = `max(ledger.window_high WHERE cas_applied = true)`이므로 **`cas_applied = true` row만** 센다: 부분 CAS 뒤 종결된 계약의 `window.high`를 쓰면 과대 표시된다, A 16.2) | 확정 | — |
| Source별 ρ, queue 깊이 | (a) 시간대별 `runnable + submitted_queued + executing` 합계(§2.5 backlog 5분해)의 2~7일차 선형회귀 기울기 ≤ 0 또는 7일차 평균 ≤ 2일차 평균 × 1.1; (b) Dagster event_logs·Control ledger 일일 증가량의 3~7일차 변동계수 ≤ 20%(purge 후 기준); (c) lateness p95의 2~7일차 기울기 ≤ +1분/일 | soak 추세(FI 창 제외) | 확정 | — |
| Kafka 실패 알림 | p95 1분 | `consumer 수신 시각 − NotificationOutbox.created_at`, outage 창 제외 | 확정 | — |
| Job 목록 API | p95 2초 | Control API 계측 | 확정 | — |
| 표준 Job 신규 등록 | 운영자 median 10분 이내 | Wizard 계측 — v1.2 20장 Phase 2 항목 | 확정(Phase 2) | — |
| Source 절대 한도 | 단 한 번도 초과 금지 | stub connection 카운터 거부 0(양성 증거: 카운터 최대값 = C 도달 ≥ 1회), `GV$SESSION` 1초 샘플 ≤ 한도, ORA-02391 0 — **집계 창·모집단(v1.2.3)**: 집계 창 = soak 구간 전체에서 §2.1 (iv) cap+1 대조 창을 제외한 구간, 모집단 = **클라이언트 식별로 플랫폼 세션에 한정**한 ORA-02391(플랫폼 JDBC 오류 로그의 ORA-02391 ∪ Oracle unified audit `LOGON` 실패 row 중 `RETURN_CODE = 2391`이면서 ETL 계정·플랫폼 호스트인 것). DBA 호스트에서 나온 대조용 ORA-02391은 이 모집단 밖이다 | 확정 | ✔ |
| Run당 Oracle 세션 수 | ≤ lease weight(stub 정확, 실 DR 샘플은 최대 ≤ weight + 1) | §3.2 세션 수명 로그 / `GV$SESSION` 샘플 | 확정 | — |
| ORA-03172 / ORA-01555 | 의도된 fail-fast만 | Oracle 오류 로그 | 확정 | — |
| apply lag·`confidence = DEGRADED` | lag p95·최대 기록. v1.2 11.3: `DEGRADED`는 Guard 6번에서 `BEST_EFFORT` 계약에만 1회 기록하는 contract 속성(`confidence_reason = DATUM_STALE \| LAG_QUERY_FAILED \| NO_LAG_SIGNAL` — 마지막은 capability 3 Source, 알림 없음, v1.2.1); 실행 중 lag 재조회·CAS 거부·`ADJUDICATION_PENDING` 전이 없음 | FI-11 Guard 변형: BEST_EFFORT 계약 `confidence=DEGRADED` ≥ 1 ∧ 그 계약 ledger 전 row `confidence=DEGRADED` ∧ Outbox `degraded confidence` 1건(Source 집계); 같은 조건의 ZERO_GAP은 `guard_result=SOURCE_LAG_EXCEEDED`이고 `confidence=DEGRADED`인 ZERO_GAP 계약 0. 실행 중 변형: `chunks/{n}:commit` 거부 0·CAS 정상·stub 쿼리 로그에서 chunk 구간 모니터 세션 lag 조회 0 | 확정 | — |
| Source CPU / Read IO | Airflow baseline 대비 기준은 DBA와 확정(가설 CPU +5%p, Read IO +10%) | Phase 0 baseline 비교 | 가설 | — |
| AIStor 용량 증가 | 용량 모델 예산 내 | SC-10 | 가설(H-08) | — |

### 5.1 판정 쿼리 정의

판정 oracle은 **네 층**이다. 각 층은 v1.2.1 6.1 이력 테이블·`CommitEvidenceLedger`·`window_range`·`operation_class`와 Iceberg `snapshots`·K8s 감사 로그만으로 실행 가능해야 하며, 모든 SQL은 §8.1 8번 DDL 부록 위에서 dry-run을 통과한 것만 쓴다. row count가 아니라 **구간 겹침**으로 동시성을 재고, logical window 인접성은 데이터 완전성의 증거가 아니라 보조 불변식이다.

**판정 인프라(v1.2.3)**: window 값의 단위는 `window_kind`가 결정한다 — SCN 정수 또는 UTC epoch µs이며, 계약 `window_range`가 `numrange`이므로(13.4·A 13.1) 판정 SQL도 `tstzrange`가 아니라 `numrange`·`range_agg`를 쓴다(PostgreSQL 14+ — 이 최소 버전을 `versions.lock`에 명시한다). Q0의 main ancestry·미채택 집합·겹치는 쌍은 **판정용 뷰 3개** `v_poc_p`·`v_poc_u`·`v_poc_pairs`로 만들어 네 층의 쿼리가 공유한다 — 이 세 뷰는 **하네스 산출물이며 운영 테이블·운영 뷰가 아니다**(Control DB에 새 테이블·새 상태를 두지 않는다). 참조 열은 전부 A 6.1·13.1에 실재하는 것만 쓴다(`commit_evidence_ledger`, `contract_state_history`, `attempt_state_history`, `execution_attempt.adjudicated_head_snapshot_id`, `execution_contract.parent_contract_id`). 개입 snapshot id 목록의 권위는 A 16.4 `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 알림 payload의 `intervening_snapshot_ids[]`이며(같은 목록을 AuditEvent가 복사한다), 그 payload 스키마를 §8.1 8번 G0-2에서 고정하고 판정 SQL은 `jsonb_array_elements_text`로 평탄화해 조인한다 — 조인 대상 열이 하나뿐이라야 G0-2의 ‘미존재 열 참조 0’이 성립한다.

- **(1) Logical occurrence — 정상 Run 중복/누락(No-Go 2번)**: expected = Job별 cron 전개(logical_at ∈ soak 구간, Hold 구간 포함) ∪ Hold 해제마다 Hold 기록 Job의 CATCHUP 키(`job + hold_release_at`, 14.2). expected 키마다 occurrence **정확히 1개**(≥ 2 = 중복 — 9.2 unique key 위반이므로 0이 정상) ∧ disposition ∈ {EXECUTED, 설명된 disposition(`SKIPPED_BY_HOLD`·`COALESCED_INTO`·`SUPERSEDED_BY(*)`·`EXPIRED_UNLAUNCHED`·`REJECTED_AT_GUARD`)}. occurrence 0인 expected 키는 같은 `(job_id, logical_scheduled_at)`의 Outbox `occurrence item rejected` row(9.1 batch create-or-get savepoint 거부 — v1.2.2 9.1·16.4가 payload에 `logical_scheduled_at`을 명시)가 있을 때만 **설명된 누락(`ITEM_REJECTED`, 알림됨)**이다 — 거부 항목에는 occurrence row가 의도적으로 없다(`REJECTED_AT_SCHEDULER` occurrence는 두지 않는다: 거부 원인이 occurrence row 자체의 제약 위반일 수 있어 같은 savepoint에서 생성 불가, 검토서 2 §5 7번). 설명된 누락 건수는 §8.2에 기록한 SC-02b poison item 주입 건과 **정확히 일치**해야 하며 그 외 설명된 누락 0, 설명되지 않은 누락(occurrence 0 ∧ Outbox row 0) 0, 설명되지 않은 disposition 0. FI-01(c)의 'non-terminal run ≤ 1, attempt 1, SA ≤ 1'은 (2)로 판정한다.
- **(2) Writer concurrency — 중복의 실체(No-Go 2·8·9번)**: 구간 세 종류를 유도한다 — (a) attempt SUBMITTED 구간 = `attempt_state_history`에서 `to_state = SUBMITTED` row의 `at`부터 같은 attempt의 다음 row `at`까지(연속 row 쌍 — **다음 row는 `from_state IS NOT NULL AND from_state <> to_state`인 것만 고른다**, v1.2.3: `bound_dagster_run_id` UPDATE 트리거가 만드는 재결합 `REATTACH` row는 `from_state = to_state`, `AFTER INSERT` 트리거가 만드는 `RECONSTRUCTED` row는 `from_state = null`이라 **둘 다 writer 구간의 경계가 아니다**(NULL을 3값 논리에 맡기지 않고 명시 제외한다). 이를 경계로 세면 하나의 SUBMITTED 구간이 둘로 쪼개져 같은 attempt가 자기 자신과 겹치는 오탐이 난다 — §8.1 8번 dry-run에서 REATTACH row를 포함한 **양성 표본 1건**으로 실증한다: 필터 적용 시 겹침 0, 미적용 시 오탐 ≥ 1); (b) SA 존재 구간 = K8s 감사 로그의 SparkApplication created → terminal(또는 Control의 삭제·pod 부재 확인 AuditEvent) 시각; (c) lease 구간 = `lease_state_history`의 `to_state = GRANTED` `at`부터 같은 `lease_id`의 `RELEASED` `at`까지(target·source 모두, v1.2.1 11.2; `table_id`·`partition_range`·`source_id`는 `lease_id`로 `target_lease`/`source_lease` row를 조인해 얻는다 — 6.1 구현 규약 제약 9번의 열). **lane**(v1.2.2 — (3)(b)와 같은 정의): ingest lane = 계약 `operation_class ∈ {NORMAL, CATCHUP, INITIAL_LOAD, RERUN_LATEST}`, repair/backfill lane = `operation_class ∈ {REPLAY, BACKFILL}` ∪ repair(`parent_contract_id` 보유). 판정: 같은 `(job, lane)`의 (a) 쌍별 겹침 0 ∧ 같은 `(job, lane)`의 (b) 쌍별 겹침 0(lane 간 동시 SA — FI-20(f)의 닫힌 구간 backfill SA + NORMAL SA — 는 14.4·13.4 설계상 정상이며 그 안전성은 아래 (c) lease 구간·13.4 `OVERLAPS_OPEN_RANGE`·(3)(b) logical window 겹침 0이 판정한다; lease conflict matrix는 v1.2.2 13.3·FI-51의 몫이지 이 층의 입력이 아니다) ∧ 같은 target의 `EXCLUSIVE_TABLE` (c) 쌍별 겹침 0 ∧ 같은 `(target, partition 범위)`의 `PARTITION_OR_FILESET` (c) 쌍별 겹침 0 ∧ 같은 Source의 (c) `token_weight` 합 ≤ C 전 시점 ∧ (b)가 (c) source 구간 밖에 있는 건 0. 순차 RETRY(attempt별 SA 이름 `sa-{contract_short}-a{attempt_no}`, 10.1)는 구간이 겹치지 않으므로 정상이다 — 같은 `(job, logical_at)`의 SA 개수로 세지 않는다.
- **(3) Commit identity — Append 이중 commit(No-Go 7번)**: (a) Iceberg `<table>.snapshots`에서 `(summary['etl.execution_contract_id'], summary['etl.attempt_no'], summary['etl.chunk_no'])` 그룹 count > 1인 건 0(attempt마다 chunk를 `low`부터 재번호하므로 `attempt_no` 없이 묶지 않는다, 13.4) ∧ ledger `(contract_id, attempt_no, chunk_no)` unique 위반 0 ∧ `cas_applied = true` row의 `committed_snapshot_id`가 snapshot에 실재하고 summary 키가 ledger row와 일치(13.1 lineage) ∧ 그 snapshot이 **판정 시점 `<table>.refs['main']`의 ancestry에 있음**(`snapshots.parent_id`를 main head부터 추적 — WAP 경로는 `fast_forward` 성공 뒤에만 `chunks/{n}:commit`·CAS가 허용되고 ledger `committed_snapshot_id = published_main_snapshot_id`이므로 attempt branch에만 있는 snapshot이 통과하면 불합격, v1.2.2 13.1; H-06 변형의 ff 실패 회차는 main 불변 ∧ ledger CAS row 0이 기대값). 다른 행의 '같은 `(contract, chunk)` snapshot 2개 0'은 모두 이 `(contract_id, attempt_no, chunk_no)` 키로 읽는다(attempt-local 재번호, 13.4); (b) **이중 commit oracle — 4-불변식(v1.2.3 재정의, `snapshot_id` allowlist 예외 없음)**: 모집단 **P** = 판정 시점 main ancestry 중 `summary['etl.writer_kind'] = 'ingest'`인 snapshot(뷰 `v_poc_p`); **채택** = 그 id를 `committed_snapshot_id`로 갖는 `cas_applied = true` ledger row가 존재; **U** = P 중 미채택(뷰 `v_poc_u` — 정상 soak 기대값 ∅); **겹치는 쌍** = 같은 `(job_id, lane)`에서 `[etl.logical_window_low, etl.logical_window_high)`가 겹치는 두 원소(뷰 `v_poc_pairs`, 비교는 `numrange` — 값의 단위는 `window_kind`가 결정한다). **lane 문구의 권위는 `operation_class`가 아니라 `etl.writer_kind`다**(v1.2.3 — 이 층 한정이다. (2)는 snapshot이 아니라 Control row를 세므로 lane을 종전대로 `operation_class` ∪ `parent_contract_id`로 읽으며, 두 정의는 같은 계약 집합을 가리킨다): repair/backfill writer는 `etl.writer_kind = 'repair'`로 stamp되므로 애초에 모집단 P에 들어오지 않고, 따라서 parent·닫힌 구간과의 겹침은 이 층의 입력이 아니다. 정상 RETRY의 재개 chunk는 `low = 마지막 CAS 값`이므로 겹치지 않는다. 판정은 아래 **네 불변식 전부의 논리곱**이다; (c) PK 유일성 위반 0(13.1 검사 2).
  - **불변식 1(예외 없음)** — 겹치는 쌍 중 **양쪽 다 채택된** 쌍 = **0**. FI-50 회차를 포함해 어떤 예외도, 어떤 `snapshot_id` allowlist도 두지 않는다(FI-50이 만드는 중복은 **양쪽 다 미채택**이므로 이 술어를 무력화하지 않는다 — allowlist가 불필요한 이유다).
  - **불변식 2(설명 책임)** — U의 **모든** 원소 m이 (a) 미채택 ∧ (b) 그 attempt가 Control 측에서 미채택 확정 — 세 분기 중 하나로: `ADJUDICATED ∧ verdict = 'NO_COMMIT'`, 또는 그 ledger row가 `dq_result = 'EXTERNAL_SNAPSHOT' ∧ cas_applied = false`, 또는 **다음 attempt**가 `FENCED ∧ verdict IS NULL ∧ adjudicated_head_snapshot_id = m` ∧ (c) m의 계약 **또는 m을 lineage에 실어 탐지한 계약**이 `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)`로 전이(`contract_state_history`) ∧ (d) **(c)에서 확정한 바로 그 계약**을 `parent_contract_id`로 갖는 repair REPLAY가 `FINALIZED`이고 그 계약 ledger의 `cas_applied = true` window 합집합(`range_agg`)이 m의 window를 포함. 하나라도 아니면 No-Go.
    > 불변식 1은 이중 commit이 **채택까지 된** 최악만 잡는다. **탐지 실패형**(한쪽만 채택되고 나머지가 설명되지 않는 형태)은 불변식 2가 잡는다 — (c)가 없으면 즉시 불합격이다.
  - **불변식 3(교차 검증)** — `|U|`와 겹치는 쌍 수가 §8.2의 **사후 case 귀속표**(주입 의도가 아니라 프록시 로그로 확정·재분류한 case)와 각 case의 FI-50 합격식이 규정하는 snapshot 수에서 계산한 기대값과 **정확히 일치**해야 한다: 초과는 설명되지 않은 외부 commit, 미달은 주입 미재현이며 **둘 다 불합격**이다. 하네스 주입 로그는 판정 모집단에서 빼는 **제외 목록이 아니라 대조값**이다(v1.2.3 — 6차의 allowlist 접근을 철회한다).
  - **불변식 4(노출)** — `repair_finalized_at − committed_at`(repair REPLAY `finalized_at` − late snapshot `committed_at`)의 p99 ≤ **§7 `external_snapshot_exposure_slo_seconds`**(A 13.1·19장 7번이 말하는 '선언 SLO'를 담는 자리 — §7 시작값을 FI-50 측정 ⑵로 확정한다)이고, **미해소 원소가 1건이라도 있으면 No-Go**다.
- **(4) Data completeness — watermark gap(No-Go 1·6번)**: 주입 truth vs target row-level 차집합 0. stub soak: 해당 계약 `visible_scn` 이하 stub 생성 row(`commit_scn ≤ visible_scn` ∧ window predicate + business WHERE)와 Iceberg `committed_snapshot_id`들의 `[etl.logical_window_low, high)` row를 §3.2 비교 A와 같은 식으로 비교 — 표본 = ZERO_GAP Append·Merge 각 ≥ 2 Job의 전 회차 + FI-10·11·37·40·41·47 대상 계약 전부. 실 DR: §3.2 비교 A·B. 차이는 "설명된 차이"만 허용. **ledger 연속성은 보조 불변식**이다: job별 ledger를 `cas_at` 순으로 정렬해 `cas_applied = true` row의 연속 쌍 (prev, next)에 대해 `next.window_low = prev.window_high`(gap 0) ∧ `next.window_high > next.window_low`(regression 0) — 예외: `INITIAL_LOAD`, `watermark:seed` AuditEvent 지점, repair REPLAY(`parent_contract_id`) 인수 row; 위반 0이되 이것만으로 No-Go 6번을 합격시키지 않는다(`dq_result ∈ {FAILED, EXTERNAL_SNAPSHOT}` row는 `cas_applied = false`라 제외).
- **Snapshot metadata 100%**: PoC namespace 전 테이블 `snapshots`에서 `summary` 키 `etl.writer_kind`·`etl.execution_contract_id`(maintenance는 `etl.writer_kind=maintenance` + lease id) 부재 건 0. 분모 = soak 중 생성된 snapshot 수(≥ 1) − §8.2에 `snapshot_id`로 기록한 FI 주입 snapshot(FI-31(a)(b)·FI-36(a)(b)(c)가 만드는 `etl.*` 없는 snapshot — **id allowlist**; `etl.*` 키가 없는 snapshot에는 `writer_kind` 표기를 붙일 수 없으므로 allowlist 또는 격리 table만 가능). allowlist 밖의 `etl.*` 부재 snapshot은 1건이라도 불합격. 이 allowlist는 **메타데이터 부재 판정 전용**이며 (3)(b) 이중 commit 4-불변식에는 적용하지 않는다(v1.2.3).

---

## 6. 즉시 No-Go와 검출 방법

| # (v1.2.3 19장) | No-Go | 검출 방법 | 관련 시험 |
|---|---|---|---|
| 1 | 설명할 수 없는 데이터 차이 | §3.2 비교 A·B, 미비교 0 — stub soak 표본은 §5.1 (4)와 같은 식 | 24 Job matrix, FI-12, FI-21, FI-37, FI-40, FI-41, FI-47, FI-48(Phase 3) |
| 2 | 정상 Run 중복 또는 누락 | §5.1 (1) expected 키당 occurrence 정확히 1개(occurrence 0은 같은 `(job_id, logical_scheduled_at)`의 Outbox `occurrence item rejected` row가 있을 때만 설명된 누락 — 건수 = SC-02b 주입 건, 그 외 0) + (2) 같은 `(job, lane)`의 attempt SUBMITTED·SA 존재·lease 구간 쌍별 겹침 0(순차 RETRY, ingest lane과 repair/backfill lane의 동시 SA는 정상) | FI-01, 02, 04, 05, 09c, 19, 20, 34, 35, 36, 38, SC-02b, SC-04b, SC-06 |
| 3 | 30분을 넘는 반복적 daemon/code-location 복구 실패 | FI-01(a), FI-03 복구 시간 | FI-01, 03, 34 |
| 4 | Source session/connection 절대 한도 초과 | stub 거부 카운터, `GV$SESSION` 샘플, ORA-02391 0 — 집계 창·모집단은 §5 'Source 절대 한도' 행과 같다(soak 구간에서 §2.1 (iv) cap+1 대조 창 제외, 플랫폼 JDBC 오류 로그 ∪ unified audit `LOGON` 실패 `RETURN_CODE = 2391` 중 ETL 계정·플랫폼 호스트, v1.2.3) | SC-02, SC-05, SC-06, FI-17 |
| 5 | Primary DB 자동 fallback(role check 미검출·DB identity 상이 포함) | stub 접속·쿼리 로그(endpoint·role·dbid) | FI-13, FI-26, FI-42, FI-45 |
| 6 | Watermark gap / regression(ZERO_GAP) | §5.1 (4) 주입 truth vs target 차집합 0 — §3.2 비교 A(stub soak는 stub 생성 row truth, 실 DR·DG 시험 인스턴스는 `AS OF SCN` truth; 권위는 §8.3 'Oracle ZERO_GAP Go') + 보조 불변식(ledger `cas_applied` 연속 row `window_low = prev.window_high`, regression 0) | FI-10, 11, 12, 37, 40, 41, 47 |
| 7 | CAS/watermark 오염 또는 미탐지·미복구 이중 snapshot(v1.2.3 19장 재정의 — 종전 문구 'Append 이중 commit'은 §3.4 No-Go 연결 열의 별칭으로만 남는다) | §5.1 (3)(a) `(contract, attempt, chunk)` 증거 유일성 + 13.1 lineage(main ancestry) + **§5.1 (3)(b) 4-불변식**(v1.2.3, allowlist 예외 없음): 불변식 1 = 겹치는 쌍 중 양쪽 다 채택된 쌍 0 ∧ 불변식 2 = U의 모든 원소가 (a) 미채택·(b) Control 측 미채택 확정 3분기 중 하나·(c) `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)` 전이·(d) 그 계약을 부모로 하는 repair REPLAY FINALIZED의 `cas_applied` window 합집합이 포함 ∧ 불변식 3 = `\|U\|`·겹치는 쌍 수가 §8.2 **사후 case 귀속표**와 각 case의 FI-50 합격식에서 계산한 기대값과 정확히 일치(초과·미달 모두 불합격 — 주입 회차 수가 아니다, §5.1 (3)(b)) ∧ 불변식 4 = 노출 p99 ≤ 선언 SLO·미해소 0. 모집단 P의 lane 권위는 `etl.writer_kind`이며 repair writer(`'repair'`)는 P 밖이다 | FI-06, 09, 09c, 22, 23c, 37, 48(Phase 3) |
| 8 | Commit Adjudication 없이 또는 `WRITER_FENCED` 확정 없이 새 attempt/SA 제출 | attempt 생성 트랜잭션의 verdict·FENCED 기록 검사(`attempt_state_history`) | FI-06, 06b, 07, 14, 15, 33, 36 |
| 9 | lease `RELEASED` 전 Source token 재부여(모든 경로에서 `RECLAIMED` 확인·attempt 세션 0 probe 이전 — 정상 `finalize` 포함, v1.2.2 11.2), 또는 grant 식에 `reserved_unrealized`를 더하지 않은 부여 | `lease_state_history`(`RECLAIMED`·`RELEASED` 별도 row — 정상 finalize 계약도 두 row) + stub 세션 수명 로그 + `guard_result.lease_grant`의 grant 식 평가값(`observed`, `reserved_unrealized`, `requested_weight`, `pool_cap`) | FI-17, FI-43, FI-43c |
| 10 | `DQ_FAILED` 계약에 RETRY 수락(`dq:accept` 전), 또는 `dq:accept`·`resolve`가 미실행 chunk를 watermark로 덮음 | RETRY API 응답 로그; FI-23c `dq:accept` 직후 watermark = `high_k` 대조 ∧ `resolve {OPERATOR_ACCEPT}` 409 | FI-23, FI-23c (Phase 2) |
| 11 | Template rollback 불능 | FI-24a/b | FI-24 (Phase 2) |
| 12 | Hold 중 신규 SparkApplication 제출 | Hold 구간 SA 생성 시각 대조(K8s 감사 로그) + 양성 증거(`SKIPPED_BY_HOLD` ≥ 1) | FI-05, FI-18, FI-30, SC-06 |
| 13 | Kafka Outbox event 유실 | outbox vs consumer sink `event_id` 집합 대조 | FI-29 |
| 14 | UI/API로 필수 운영 절차 불가 | 절차별 증거 row — 각 절차를 Control API에서 1회 이상 수행하고(Phase 1 게이트 필수), Custom UI 측은 Phase 2 게이트(§2.2 'UI 없음'); AuditEvent(actor·idempotency_key)와 수행 FI를 기록: Hold 생성·해제(FI-18 hold_id, FI-28 `PLATFORM_BREAKER` Hold 수동 release; FI-16은 revision ACTIVE에 의한 자동 해제이며 수동 release는 412 `CREDENTIAL_REVOKED` 증거), RETRY 성공(FI-15(b)·FI-21 operation_id; FI-22는 409 거부 응답 로그), abort(FI-25(a)), resolve(FI-36), dq:accept(FI-23 — Phase 2), accept-empty(FI-21), watermark:seed(FI-22·36), cancel(PLANNED, FI-10(d)), adjudicate 수동 트리거(FI-14), REPLAY repair(FI-36(g); FI-23 — Phase 2), RERUN_LATEST replace_with_empty(FI-21 — Phase 2), BackfillPlan 생성·start·pause·resume·cancel(§3.1 닫힌 구간 1건), `GET /v1/operations/{id}` 폴링. 해당 Phase의 체크리스트 미완 1건이라도 있으면 No-Go | 해당 FI |

---

## 7. 운영 파라미터 초기값 (PoC)

v1.2 22장 22번의 잠정 기본값(이 표의 시작값을 v1.2가 채택)과 튜닝 범위. 최종값은 §4 결과로 확정해 22번에 되돌린다.

| 파라미터 | 시작값 | 튜닝 범위 / 근거 |
|---|---|---|
| `planned_stale_after` | max(5분, `start_timeout_seconds`) | FI-35(b)로 확정 |
| `planned_scan_interval` | 60초 | 30~120초, FI-35로 확정 |
| `guard_backoff_initial_seconds` | 30초 (2배 증가, 상한 period/2) | — |
| `guard_retry_budget_seconds` | 120초 | FI-34(b)로 확정 |
| `max_auto_attempts` | 3 | RUN_WORKER_LOST 전용, Phase 0 재시도 분포 참고 |
| `adjudication_pending_alert_after` | period/2 | Phase 0 실패 분포 참고 |
| ~~`adjudication_delay_seconds`~~ | (v1.2.2 폐지) | 13.2 2번 head-settle(`WRITER_FENCED` 뒤 `target_health_timeout_seconds` 간격 2회 연속 같은 `current-snapshot-id`)이 대체 — FI-50 |
| `adjudication_retry_backoff_initial_seconds` | 30초 (2배 증가, 상한 300초) | fenced ∧ verdict NULL 계약의 Polaris 조회 실패 재시도 backoff(v1.2.1 5.4·9.3) — FI-28(c)로 확정 |
| `orphan_min_age` | max(`run_monitoring.max_runtime_seconds`) + `reattach_grace_seconds` + Adjudication 조사 기간 + margin(1일) | `remove_orphan_files(older_than)` 하한(v1.2.1 18장) — SC-11에서 하한 미만 JobSpec 422 확인 |
| `reattach_grace_seconds` | `start_timeout_seconds` + 60 (= 360) | H-04로 확정 |
| `chunk_proceed_timeout_seconds` | `reattach_grace_seconds` + 120 (= 480) | driver의 `proceed` 대기는 재결합(FI-06, H-04)을 덮어야 하므로 `reattach_grace_seconds`보다 커야 한다. 여유 120초 = run_monitoring poll 60 + sensor 간격 30 + 여유 30(탐지 지연 예산). FI-07 변형이 이 값 초과 경로. 정상 경로 CAS p99 × 여유는 하한일 뿐 |
| `monitor_query_timeout_seconds` | 5초 | Guard 트랜잭션 p99와 연동 |
| `adapter_sync_timeout_seconds` | 5초 | — |
| `lease_try_timeout_seconds` | 10초 | — |
| `drain_timeout_seconds` | 예상 chunk 시간 × 2 | Hold 생성 시 지정 |
| `credential_grace_seconds` | `run_monitoring.max_runtime_seconds` | Source별 |
| `datum_stale_seconds` | 30초 | Oracle 문서 기준 |
| `lease_release_probe_interval_seconds` | 10초 | v1.2.1 11.2 `RECLAIMED → RELEASED` probe 간격 — FI-17(c)·FI-43으로 확정 |
| `lease_release_zero_count` | 3(정상 `finalize` COMPLETED 관측 경로는 1 — v1.2.2 11.2) | 연속 0건 관측 횟수 — FI-43, FI-17(c)·FI-43c(b) |
| `lease_release_max_wait_seconds` | `sqlnet_expire_time_seconds` + 60(미등록 600) | 초과 시 `source session lingering` 알림, 자동 반환 없음 — FI-43(120초로 축소 시험) |
| `daemon_heartbeat_gap_seconds` | 2 × 최소 period (= 120분) | FI-02로 확정 |
| `gap_recovery_max_range_seconds` | 7일 | Schedule Gap Recovery 수동 기동 range 상한(v1.2 9.3), 튜닝 1~14일 — FI-02 범위 초과 요청 400으로 검증 |
| `target_unchanged_alert_count` | 3 | 같은 Job의 `FINALIZED_NO_DATA ∧ target_unchanged = true` 연속 횟수 → 16.4 `target unchanged` Source 단위 경고(v1.2.2 신설) — FI-21 `RETAIN_PREVIOUS` 3연속 변형으로 검증(경고 1, `freshness breach` 0) |
| `max_chunk_span` | Source별(예: PT6H) | Append-large 실측 |
| `safety_lag`, `clock_skew` | DBA 승인값, skew ≥ 3초 | 11.3 |
| `freshness_slo` (Job class별) | 1h 주기 p95 20분, 24h 주기 p95 60분 | H-12로 확정 |
| `credential_breaker_failures` | 1 | attempt당 1건 집계, 첫 `CREDENTIAL_FAILURE`에서 Source HOLD_NEW(v1.2 6.2); Source validator `credential_breaker_failures + 최대 총 JDBC connection weight(Σ numPartitions + driver_sessions; `password_rollover_registered = true` Source만 최대 동시 ETL Job) + 1(모니터 세션) + legacy_concurrent_sessions ≤ FAILED_LOGIN_ATTEMPTS − 1`(v1.2.2 6.2·22장 9번, `ADG_ACCOUNT_INFO_TRACKING` 단위) — FI-16(e)(e′)로 검증 |
| `platform_breaker_failures` | 3 | breaker key(Polaris catalog·AIStor) 단위 연속 `TARGET_UNAVAILABLE` 시 `PLATFORM_BREAKER(key)` HOLD_NEW(Job 목록/Global scope, v1.2 5.4) — FI-28로 확정 |
| `target_health_timeout_seconds` | 5초 | Guard 5번 health check 총 예산(row lock 중 외부 I/O 상한), v1.2 5.4 — FI-28·SC-02 Guard p99로 확정 |
| dagster `max_concurrent_runs` | 800 | 최대 동시 Spark + 여유 |
| dagster `max_tick_retries` | 2 | — |
| dagster `sensors.use_threads` / `num_workers` / `num_submit_workers` | true / 4 / 4 | v1.2.3 A 5.1·9.1 — 미설정 시 burst 500이 1건씩 직렬 제출. SC-02d의 1/4/16 대조로 확정 |
| shard sensor `minimum_interval_seconds` | 30초 | v1.2.3 A 9.1 burst 제출 지연 상한식의 첫 항 — SC-02d에서 고정·확정 |
| `submissions:poll` `limit` | 500 | v1.2.3 A 9.1 — SC-02d에서 고정, 상한식 `limit × 직렬 제출비용 / num_submit_workers`의 항 |
| `submission_recheck_after` | `minimum_interval_seconds` × 3 (= 90초) | v1.2.3 A 9.3 in-flight 재확인 대기 — FI-35(e)로 확정 |
| `external_snapshot_exposure_slo_seconds` | 잠정 3600초 | A 13.1·19장 7번의 late-apply ‘선언 노출 SLO’ — §5.1 (3)(b) 불변식 4의 임계, FI-50 측정으로 확정해 22장 22번에 되돌린다 |
| dagster `run_monitoring.start_timeout_seconds` | 300 | burst 기동 분포로 조정 |
| dagster `run_monitoring.poll_interval_seconds` | 60 (기본 120) | 재결합 탐지 지연 상한 — `chunk_proceed_timeout_seconds` 여유 120초의 근거 |
| dagster `run_monitoring.max_runtime_seconds` | max(period, 예상 chunk 시간 × expected_chunk_count) + 여유 | `credential_grace_seconds`의 근거, 실행 중 계약의 유일한 시간 상한(9.3) |

---

## 8. 산출물, 리포트 양식, 게이트

### 8.1 산출물

1. 시험 결과 리포트(§8.2 양식) — FI 40 + 신규 16건(5차 신규: FI-05b·FI-06b·FI-09c·FI-23c·FI-37a·37b·37c·FI-40~FI-45·FI-47~FI-49 — FI-46은 결번, 머리말의 ‘신규 FI 13’은 37a/b/c를 1건으로 센 값; FI-24는 a/b 각 1건, FI-48은 Phase 3 게이트 보고서로 분리) + **6차 신규 9건**(FI-50·FI-51·FI-43c·FI-47c·FI-44e·FI-24c·FI-41e/f·FI-05c·FI-45d — FI-41e/f를 1건으로 센 값; FI-42(e)·FI-35(e)·FI-22(a′)·FI-16(e′)·FI-48(e)·FI-49 순서 교정·H-06 변형·SC-11 fixture 보강·FI-07/23c/40 재작성은 기존 행 내 변경이라 세지 않음)· **7차 신규 2건**(FI-41g·SC-02d) · **8차 신규 2건**(FI-41h — `TXN_AGE_KILL_JOB` BEST_EFFORT 안전망, G2 목록 밖 · FI-41i — G0 무효화 전파 드릴) · SC 11 + 신규 4건(SC-02b·SC-02c·SC-02d·SC-04b)·H 13건 각각
2. 병목 순위표(SC-04, 미실시 시 SC-03 관측으로 대체) — 구성요소별 한계 도달 순서와 수치
3. Source 용량 모델(H-11) — 계수, ρ 임계, Source별 현재 ρ
4. Storage 용량 모델(H-08) — retention 등급별 GB/일
5. 노드 용량표(H-10) — burst 500 기준 vCPU/GiB
6. 확정 파라미터 표(§7 최종값)
7. v1.2 22장 잠정값 확정 입력 — 가설 결과로 확정할 22장 항목(1·3·6·7·8·11·20·22번)의 측정값과 권고값(§8.4 잔여 미결 포함). §8.3 축소로 미뤄진 항목은 "미결·기본값"으로 명시

8. **G0 산출물 — Executable Spec Go(G0-1~G0-5)**(§8.3 G0 게이트, **G0 PASS 없이 주차 2 soak 미착수**; v1.2.3에서 기존 주차 1 산출물 (i)(ii)(iii)을 G0-1·G0-2·G0-3으로 정식화하고 G0-4·G0-5를 더한다): **G0-1** DDL 부록 — v1.2.1 6.1 구현 규약의 이력·계측 테이블 5종 + `CommitEvidenceLedger`(`(contract_id, attempt_no, chunk_no)` unique, `cas_applied`, `cas_at`, `window_low/high`, `actor`, receipt 복사 컬럼 포함) + `execution_contract`·`execution_attempt`·lease 핵심 열과 제약 10종(v1.2.2 (10) operation 단일성 포함)(unique·exclusion·상태 UPDATE 트리거·`bound_dagster_run_id` UPDATE 트리거 — 재결합 `REATTACH` row의 생성 경로)을 실행 가능한 SQL로 빈 PostgreSQL에 **오류 0으로 적용**하고, **제약 10종 각각에 대해 위반 INSERT/UPDATE가 거부된 양성 증거 1건씩(총 10건, 오류 코드·제약 이름 포함)**을 남긴다 — 제약이 존재하기만 해서는 PASS가 아니다; **G0-2** §5.1 판정 SQL 4층 + 보조 불변식 + §6 검출 쿼리 전부를 그 DDL 위에서 dry-run(빈 테이블 + 합성 row 표본으로 양성·음성 각 1건, EXPLAIN 확인)한 결과 — 참조 열·enum 누락 0; **G0-3** §3.2 canonical hash 규격(v1.2.3 — NFC → AL32UTF8 바이트 → 길이-prefix → 2단 SHA-256)의 검증 벡터 **V-01~V-16**을 Oracle 시험 인스턴스(없으면 Oracle 호환 stub)와 Spark 양측에서 **실 실행**해 벡터별 일치·불일치가 규격의 기대와 정확히 같은지 확인하고(좁은 행 `STANDARD_HASH(RAW,'SHA256')` 경로와 광폭·LOB `DBMS_CRYPTO.HASH(BLOB, HASH_SH256)` 경로의 교차검증 포함), `hash_participation = EXCLUDED`로 비교에서 제외한 컬럼 목록을 함께 기록한 결과 — 실행 불가 SQL(v1.2.1의 `ora_hash(row)` 류)과 양측 canonical 함수의 drift를 여기서 잡는다; **G0-4** pinned Dagster에서 제출 경로 실증 — sensor `RunRequest.run_key`가 **tick 경계를 넘어 멱등**임(같은 `run_key`로 연속 3 tick 재평가, run 중복 0)·dedup scope가 (sensor 이름, repository selector)임·`fetch_existing_runs`의 500-key 소요 계측·GraphQL introspection으로 `ExecutionMetadata`에 `runId` 입력 필드가 **없음**을 확인(9.2·9.3); private adapter(`create_run + submit_run`)를 차선책으로 채택할 때에만 **왕복 3건**(정상 왕복·중복 `run_id` 거부·`NOT_STARTED` 잔존 run 회수)을 추가로 남긴다; **G0-5** 위 넷을 **모두 같은 `versions.lock`**(동일 digest) 아래에서 실행했음을 증거로 남긴다 — 넷 중 하나라도 다른 lock에서 실행되었으면 G0은 PASS가 아니다. 이 부록은 문서 우선순위(머리말·v1.2.1 19장)의 2단(executable schema)이다

   **G0 증거 레코드 `g0_evidence`**(`zero_gap_evidence`와 같은 형식, §2.4 계측 산출물): `g0_report_id` · `executed_at` · `versions_lock_digest` · `ddl_digest` · `verdict_sql_digest` · `canonical_hash_spec_digest` · `hash_vector_result`(V-01~V-16 벡터별 판정) · `submission_path_result`(G0-4 관측값) · `source_kind ∈ {ORACLE_TEST_INSTANCE, ORACLE_COMPATIBLE_STUB}` · `oracle_env{nls_characterset, nls_nchar_characterset, max_string_size}`(v1.2.3.1 — `NLS_NCHAR_CHARACTERSET` 추가: STRING 경로가 `TO_NCHAR` 경유이므로 NCHAR charset이 `DECOMPOSE`/`COMPOSE`의 실제 입력 charset을 결정하고, §2.3이 이미 이 값을 수집한다).

   **G0 무효화 8항**(v1.2.3.1 — (8) 추가; 하나라도 발생하면 `g0_pass := false` + Outbox `g0 invalidated`이며, **그 시점부터** G1·G2 판정은 **미판정**이다 — 무효화 이전 구간의 판정 기록은 취소하지 않되 리포트에 무효화 시각을 병기한다): (1) `versions.lock` 변경(patch 포함), (2) DDL digest 변경, (3) 판정 SQL digest 변경, (4) canonical hash spec digest 변경 — 명시 입력 다섯(매핑표 digest, 타입 태그·정규화 규칙·NUMBER/TIMESTAMP 실행 규격 텍스트 digest, **`ETL_CANON` 함수 source digest**, Spark 정규화 UDF source digest, canonical hash probe 3항 결과) 중 하나라도 바뀌면 해당한다(v1.2.3.1 — `ETL_CANON`의 본문 변경·재배포는 그 자체로 무효화 사유이며 §3.2 ‘`canonical_hash_spec_digest`의 명시 입력’이 그 목록의 권위다), (5) Source `NLS_CHARACTERSET` 변경, (6) Source `MAX_STRING_SIZE` 변경, (7) sensor 이름 규칙·repository selector 변경(G0-4 dedup scope 전제), (8) Source **`NLS_NCHAR_CHARACTERSET`** 변경(v1.2.3.1 신설 — STRING 경로의 `TO_NCHAR` 결과 charset이 바뀌면 `DECOMPOSE`/`COMPOSE` 입력과 AL32UTF8 변환 전제가 바뀐다; §2.3 수집 항목). `source_kind`를 stub → 실 Oracle로 바꾸는 것은 무효화가 아니라 **재실증**이며, 새 `g0_report_id`를 발급하고 이전 id를 `supersedes`로 참조한다(v1.2.3).

### 8.2 시험 기록 양식

```text
시험 ID:            FI-06
실행 일시 / 환경:   2026-xx-xx hh:mm~hh:mm, soak 2일차, burst 직후 (FI 창으로 §2.5 추세 집계에서 표시)
절차:               (주입 명령·훅 값, 대상 contract_id/attempt_no)
기대 동작:          v1.2 10.2 복구 절차 2 (재결합), 10.1 Pipes 재생
관측:               (Control DB 판정 쿼리 결과, Dagster run id, SA 상태, ledger row, history row)
합격 조건 판정:     재결합 1회 ✔ / fencing 0 ✔ / chunk 2 snapshot 1개 ✔ / 양성 증거 ✔
No-Go 연결:         Append 이중 commit — 해당 없음
설명된 차이 목록:   (job_id, pk, 사유, 근거 row id) — 해당 시
결론:               PASS | FAIL | PARTIAL (사유) | 관측(v1.2 입력)
첨부:               로그 경로, 재현 스크립트
```

### 8.3 게이트와 일정

**Phase 1 판정 범위**: v1.2 20장 Phase 1 산출물 + §2.2 구현 범위 표(Phase 1에 앞당긴 최소 구현)로 실행 가능한 시험이 Phase 1 게이트 대상이다. 따라서 FI-12(b)·FI-16·FI-20(backfill 분기 포함)·FI-21(accept-empty까지)·FI-22(seed 분기 포함)·FI-25·FI-28·FI-33·FI-36·FI-38과 §3.1 닫힌 구간 backfill은 **Phase 1 게이트**다. **Phase 2 진입 게이트(Phase 2 구현 후·24 Job shadow 비교 시작 전)**로 이관해 §1.2 Go 판정의 "§5 확정 기준"에서 제외하는 것은 §2.2 표 밖 기능에 의존하는 시험뿐이다: FI-21(RERUN_LATEST 분기), FI-23(`DQ_FAILED` 전이·검사 1·5는 v1.1 20장 Phase 2 'MVP DQ' 산출물), FI-24a/b, FI-27, FI-32, SC-08(purge 행), §5 "표준 Job 신규 등록", §6 운영 절차 체크리스트의 Custom UI 측. No-Go 연결이 있는 FI-23(DQ_FAILED RETRY)·FI-24(Template rollback)는 Phase 2 게이트의 No-Go로 유지한다. §2.2 표 항목을 Phase 1에서 빼기로 결정하면 그 의존 시험을 이 목록으로 옮기고 §3.4 No-Go 열·§3.5·§6에 "(Phase 2 게이트)"를 함께 표기한다.

**세 게이트(v1.2.3 — G0·G1·G2)**: 판정 앞에 **선행 게이트 G0**이 있다. **(G0) Executable Spec Go** — §8.1 8번의 G0-1~G0-5 전부 PASS(넷이 같은 `versions.lock` 아래). **G0 PASS 없이 주차 2 soak를 착수하지 않는다.** G0가 무효화되면(§8.1 8번 무효화 8항) 그 시점부터 G1·G2 판정은 **미판정**이고, **무효화된 `g0_report_id`에 의존한 모든 Source의 현재 `zero_gap_verified`가 같은 트랜잭션에서 false가 되며**(v1.2.3.1 — 과거 보고서 자체는 보존한다: 무효화 이전에 얻은 `g2_report_id`·`zero_gap_evidence`·종료된 계약의 판정 기록은 지우거나 고치지 않고, 다만 그 근거로 **현재** ZERO_GAP을 계속 내보내지는 못한다) 그 Source의 ACTIVE `ZERO_GAP` Job에는 A 6.1의 기존 무효화 규칙이 그대로 적용돼 자동 `MaintenanceHold` `HOLD_NEW(ZERO_GAP_INVALIDATED)`가 걸린다(새 상태·새 필드 없이 기존 규칙의 적용 범위만 넓힌 것). 재실증 뒤 다시 `zero_gap_verified = true`가 되려면 새 `g0_report_id`(이전 id는 `supersedes`) 위의 G2가 필요하며, G1·G2 판정은 그 재실증 뒤 구간부터 다시 기록한다 — 무효화 이전 구간의 기록은 취소하지 않되 리포트에 무효화 시각을 병기한다. 나머지 둘은 v1.2.1 정의 그대로다: Phase 1 Go는 모집단이 다른 두 판정을 따로 기록한 합이다. **(G1) Scale/Control Go** — stub Source 위의 규모·장애·의미론 시험(SC-01~11, FI 전부, H-01·02·04·05·07~13). stub은 프로토콜 결함을 반복 재현하는 도구이지 Oracle 의미론의 증거가 아니므로 G1만으로는 No-Go 6번(ZERO_GAP)을 합격으로 판정하지 않는다. **(G2) Oracle ZERO_GAP Go** — 실 primary → physical standby(DBA가 준비한 DG 시험 인스턴스, §2.1) 위에서 long transaction(`max_commit_minus_watermark_seconds` 경계 ±), late commit(FI-12 절차), role transition(switchover — FI-13·FI-45 절차), undo churn(FI-44 절차), executor reconnect(FI-45 절차), old/NULL timestamp(FI-41 절차), **Oracle probe 4종(v1.2.3 — long-open transaction bound 초과 commit의 거부(FI-41g 절차: `SYNC_COMMIT_GUARD` Source에서 bound 초과 commit이 거부돼 Source row 자체가 생기지 않고 이후 회차 gap 0. **v1.2.3.1 — `TXN_AGE_KILL_JOB` Source의 kill miss 회차(FI-41h)는 BEST_EFFORT 안전망 시험이므로 이 G2 목록에서 제외한다** — 누락을 재현하고 Audit이 repair하는 시험은 ZERO_GAP 통과 증거가 아니다), 같은 CDB·다른 PDB endpoint(FI-42(e) 절차), LOB flashback(§3.1 #7 CLOB 보유 Merge-large Job에 FI-44(a) undo 만료 절차를 적용한 회차 — ORA-01555·ORA-22924 분류), Source 세션 절대 한도 cap+1 양성 대조(§2.1 DB hard cap 증거 (iv) 절차의 ORA-02391 — DBA 호스트·집계 창 밖))**, role 전환 시 기존 read-only 세션 종료 여부(FI-45d 절차 — switchover 중 init 성공 세션이 유지되는지·그 세션의 SELECT가 어떤 오류로 끝나는지, v1.2.2 12.3)를 주입하고 §5.1 (4) `AS OF SCN` truth 차집합 0으로 판정한 결과 + 실 DR 24 Job H-03 결과 + 22장 2·3·4번 DBA 확인. G2를 통과한 Source만 `SourceCapability.zero_gap_verified = true`로 등록하고(v1.2.1 6.1·7.2 5번), 미통과 Source의 Job은 `BEST_EFFORT`로만 publish된다. DG 시험 인스턴스를 확보하지 못하면 G2는 "미통과"로 기록하고 Phase 1 판정은 G1 + `zero_gap_verified = false` 전제까지만 가능하다(§1.2).

- 주차 1: 환경·stub(§2.3 전 요건, FI-40 자가검증 통과)·계측(§2.4 테이블·훅) 준비, SC-01 완료(shard·전달 방식 결정). **주차 1 산출물(필수, §8.1 8번)**: (i) DDL 부록 — v1.2.1 6.1의 이력·계측 테이블 5종 + `CommitEvidenceLedger`(`cas_applied`, `cas_at`, `window_low/high`, `actor` 포함) + `execution_contract`·`execution_attempt`·lease 핵심 열과 제약(unique·exclusion·상태 UPDATE 트리거·binding 컬럼 트리거)을 실행 가능한 SQL로; (ii) §5.1 판정 SQL 4층 + 보조 불변식 + §6 검출 쿼리 전부를 그 DDL 위에서 dry-run(빈 테이블 + 합성 row 표본)해 존재하지 않는 열·enum 참조 0; (iii) §3.2 canonical hash 검증 벡터 V-01~V-16 실 실행(벡터별 일치·불일치 판정, `hash_participation = EXCLUDED` 목록), (iv) pinned Dagster 제출 경로 실증(sensor `run_key` tick 경계 멱등·`fetch_existing_runs` 500-key 소요·`ExecutionMetadata.runId` 부재 introspection), (v) 넷이 모두 같은 `versions.lock` 아래임을 확인 — 상세는 §8.1 8번 G0-1~G0-5와 동일(ledger는 `published_main_snapshot_id`·`dq_basis`, 제약 10종과 그 위반 거부 양성 증거 10건 포함). **다섯 항 중 하나라도 없거나 PASS가 아니면 주차 2 soak를 시작하지 않는다**(v1.2.3)
- 주차 2: SC-02, soak 시작, soak 1~3일차 FI(§3.5 2번)
- 주차 3: soak 4~7일차 FI·SC-06·SC-11(§3.5 3·4번), 실 DR 정합성 1차(닫힌 구간 backfill·WAP 경로 포함)
- 주차 4: soak 종료 후 시험(FI-05·05b, SC-04·04b·02c·05·07), 실 DR 2차(H-03·H-06) + **DG 시험 인스턴스 G2 시험**(확보 시 — long transaction·late commit·role transition·undo churn·executor reconnect·old timestamp 주입, §5.1 (4) 판정), 가설 결론, Phase 1 판정 회의(G1·G2 각각 기록)
- Phase 2 구현 후(24 Job shadow 비교 전): Phase 2 게이트 시험(§3.5 7번)

게이트 통과 조건은 §1.2. 일정 초과 시 축소 우선순위: SC-04 stress → H-06 WAP → FI-31 → FI-39 → 실 DR Merge-large MoR 비교 순으로 뒤로 미룬다(No-Go 검증 FI는 축소 대상이 아니다). 미뤄진 항목은 §8.1 산출물 7번에 "미결·기본값"으로 명시한다.

### 8.4 v1.2 반영 결과 (v1.1 밖에서 전제했던 것 — 전부 v1.2에 반영, 변경 이력 ID 병기)

| 항목 | 전제했던 곳 | v1.2 반영 | 결정 요지 |
|---|---|---|---|
| PoC 계측 테이블 5종의 운영 채택 | §2.4 | D1 (6.1, 5.3, 5.4, 16.1, 16.2) | 운영 1급 append-only 테이블로 채택, 상태 UPDATE 트리거 강제 |
| `DEGRADED_CONFIDENCE` 발생 조건·주체 | §5, FI-11 | A1 (11.3, 13.4, 10.2 6번) | **PoC 가정 기각** — Guard 6번에서 BEST_EFFORT 계약에만 기록하는 contract 속성, 실행 중 lag 재조회·CAS 거부 없음 |
| Guard `TARGET_UNAVAILABLE`·`platform_breaker_failures` | FI-28 | A2 (5.4, 10.2, 14.1) | Guard 5번 target health check, 200 거부 + backoff, breaker key별 `HOLD_NEW(PLATFORM_BREAKER(key))` |
| `credential_breaker_failures` | FI-16 | C1 (6.2, 10, 14.1, 16.4, 22장 9번) | attempt당 1건 계수, 새 reason `CREDENTIAL_FAILURE`, 해제는 새 revision ACTIVE 전이에서만 |
| terminal 반입 후·재결합 전 zombie 호출 | FI-09(A) | A3 (10.2 소유권 검사, 6.1) | `terminal_ingested_at` 조건 추가, `412 ATTEMPT_FENCED {fence_reason}`; 반입 전 호출은 정상 수락 |
| 외부 snapshot 발견 시 명시 결과 | FI-31(a) | A4 (13.1, 13.2, 6.2) | lineage 본문 + Control 분류 → `RECONCILIATION_REQUIRED(EXTERNAL_SNAPSHOT)`, Adjudication 없음 |
| ConnectionRevision REVOKED 중 실행 attempt | FI-27 | C2 (6.2, 7.1, 17) | 운영자 revoke 명령, 기본 412 거부 / `force`면 FORCE_STOP → `CANCELLED(CONNECTION_REVOKED)` |
| Gap Recovery 수동 기동 | FI-02 | B4 (9.3, 17) | `POST /v1/schedule-gap-recoveries`, 자동 경로와 같은 operation, 멱등, `gap_recovery_max_range_seconds` |
| lateness sensor = breach 계산 주체 | §2.2, §5 | C5 (16.4, 6.1, 22장 20번) | `planned_scan_interval` 주기 읽기 전용, `contract.finalized_at` 기준, (job, logical_at)당 1건 |
| CATCHUP `expires_at` vs 긴 대기열 | SC-06 | B1 (9.3, 14.2, 13.4) | 면제하지 않음 — PLANNED CATCHUP·NORMAL 공존, 만료 알림 `hold_id` 집계, `expires_before_completion` |
| Release rollback 엔드포인트 | FI-24a | C3 (17, 8.2, 6.2) | `POST /v1/releases/{id}/rollback`, `409 RELEASE_NOT_ACTIVE`, 문제 release pin PLANNED 전부 VOID |
| 진행 중 계약 RETRY 거부 코드 | FI-20 | B2 (14.3, 17) | `409 ATTEMPT_IN_PROGRESS`(+required_action), PLANNED는 `409 CONTRACT_NOT_STARTED` |
| 수동 NORMAL의 Run 추가 제출 여부 | FI-20(b) | B3 (9.2, 9.3, 17) | 추가 제출 없음 — `launch_result: ALREADY_SUBMITTED \| NOT_LAUNCHABLE`, 재제출 단일 경로 |
| validate overlap 실패 응답 코드 | FI-12 대조군 | C4 (7.2, 11.3, 17) | `422 VALIDATION_FAILED` + `OVERLAP_BELOW_MINIMUM`(`computed_minimum` 동봉) |

잔여 미결(v1.2 22장 잠정 항목으로 이관): `freshness_slo` 수치(H-12), Full snapshot retention 등급(H-08), 용량 모델 계수(H-11), WAP 적용 등급(H-06), shard·Bundle 전달(H-13), 이력 테이블 보관 기간(22장 7번).

### 8.5 v1.2.1 반영 (Codex 교차 리뷰 → 검토서 §5)

| 리뷰 ID | 이 문서의 변경 |
|---|---|
| P0-09 | §2.3 stub 가시성 모델을 시간 커서 prefix 모델로 교체, FI-40 자가검증 |
| P0-10 | §5.1 판정 쿼리를 4층 oracle(logical occurrence / writer·lease interval overlap / commit identity+lineage+window 쌍별 겹침 / data completeness)로 교체, §5·§6 행 갱신, FI-31/36 snapshot id allowlist, §2.5 backlog 4분해 |
| P0-01 | FI-28 변형 (c) outage > `expires_at` |
| P0-02 | FI-37a/b/c (0-row 접두부, 기대값 watermark = `high_k`) |
| P0-03 | FI-23 재작성(검사 5 pre-commit) + FI-23c (Incremental partial 뒤 `dq:accept`·`resolve` 제한) |
| P0-04 | FI-05b (soak 중 PITR + live Run Pod/SA + 0-row CAS + 미발행 Outbox) |
| P0-05 | §2.3 identity 상이 endpoint, FI-27(a) identity 단언, FI-42 |
| P0-06 | §2.1 capability 행, FI-12 정정, FI-41, H-03 문구 |
| P0-07 | §2.3 ‘≤5초 세션 종료’ 구성 가능(기본 off), FI-17 정정, FI-43, §6·§7 |
| P0-08 | FI-24b split 변형, §5 Guard 오탐 행 |
| P1-03·P1-07·P1-20·P2-08 | SC-02b·SC-02c·SC-04b, FI-06b, FI-25(c), SC-08 retention, FI-34(c′)(c″) |
| P1-04 | FI-09c |
| P1-05 | FI-13/FI-32 24h catch-up 조건, FI-49 |
| P1-12·P1-13·P1-14·P1-16 | FI-44, FI-45, FI-16(B) ORA-28002, FI-47, §3.2 비교 A |
| P1-18 | FI-48 (Phase 3 진입 게이트) |
| P1-19·P1-22 | §2.4 컬럼 정합, FI-22 정정, §8.1 8번 DDL 부록 + §5.1 dry-run |
| P1-21 | §8.3 ‘Scale/Control Go’·‘Oracle ZERO_GAP Go’ 분리, §1.2 Go 연동, §2.1 DG 시험 인스턴스 |
| P2-02·P2-04 | 머리말 문서 우선순위(4단), §2.1 `versions.lock` |

### 8.6 v1.2.2 반영 (Codex 2차 리뷰 → 검토서 2 §6)

검토서 2 §2·§3의 채택 방식(ADOPT / ADOPT_MODIFIED)대로 반영했다. 검토서 2 §5가 기각한 처방 — catalog gateway·`commit_intent_id` journal, `EXTRACT_ONCE`-only INITIAL_LOAD, candidate code location·activation barrier, exact recovery journal(RPO 0), `run_submission` 테이블, maintenance fencing generation, sentinel 메타데이터, 세 clock freshness, `REJECTED_AT_SCHEDULER` occurrence, typed exact 양방향을 권위로 — 는 이 문서에도 들어오지 않았다. oracle 자기모순 5건(P0-10-A~E)은 시험을 고쳐 설계를 통과시킨 것이 아니라 머리말 우선순위 4단의 traceability failure를 v1.2.2 규범으로 해소한 것이다.

| 리뷰 ID | 검토서 2 | 이 문서의 변경 |
|---|---|---|
| P0-01 | §2 ADOPT_MODIFIED(P0) | FI-50(late apply — head-settle·`adjudicated_head_snapshot_id`·`chunks:begin(1)` base 연속성; 합격식은 **착지 시점별 5-case 규칙**이다(v1.2.3.1 정정 — 6차의 '착지 시점 무관 중복 구간 CAS 0 ∧ `RECONCILIATION_REQUIRED` 정확히 1'은 (iv) 착지 실패·(v) 성공 분기에서 거짓): case (i)(ii)(iii)만 `RECONCILIATION_REQUIRED` 정확히 1, (iv)는 0 ∧ 정상 `FINALIZED`, (v)는 pinned 버전 분기. 공통 술어는 중복 구간 CAS 0 ∧ repair 뒤 diff 0), §3.5 3번; §7 `adjudication_delay_seconds 0`은 head-settle 규칙으로 대체 |
| P0-02 | §2 ADOPT(P0) | §2.3 identity 상이 endpoint tuple(`cdb_dbid`·`pdb_dbid`·`pdb_con_uid`·`pdb_guid`)·§2.3 세션 수명 로그 `db_identity` tuple 열(6항 + `con_name`), FI-42 정정, FI-42(e)(같은 CDB·다른 PDB — FI-42 행 내 변형), §2.1 `V$CONTAINERS` 권한·`NOT_APPLICABLE` |
| P0-03 | §2 ADOPT_MODIFIED(P1) | §2.1 DB hard cap 증거 4항(`RESOURCE_LIMIT`·실제 `SESSIONS_PER_USER`·적용 범위·cap+1 ORA-02391), FI-17 grant 식 `reserved_unrealized`·정상 finalize `RECLAIMED` 경유, FI-43c, §6 9번 문구 |
| P0-04 | §2 ADOPT(P0) | §3.2 비교 A overlap 조건(`window.low == original_logical_low`·Guard 응답 `extract_window_low`), FI-47 정정, FI-47c |
| P0-05 | §2 ADOPT_MODIFIED(P1) | FI-44(d) PER_CHUNK_FENCE final sweep chunk, FI-44e |
| P0-06 | §2 ADOPT_MODIFIED(P1) | FI-24c(미래 `effective_from` 422·release 말미 자동 Gap Recovery) |
| P0-07 | §2 ADOPT(P1) | FI-51(cross-type target lease 3쌍 × 100회, grant 정확히 1), §5.1 (2) conflict matrix 위치 주석 |
| P0-08-A | §2 ADOPT_MODIFIED(P1) | §2.1 heartbeat witness 행, §2.3 heartbeat 행 에뮬레이션, FI-41e, H-03 문구 |
| P0-08-B | §2 ADOPT_MODIFIED(P1) | §3.2 비교 A·B 양방향 차집합·`PK_RECONCILE.interval`, FI-41f |
| P0-09 | §2 ADOPT_MODIFIED(P1) | FI-05 conservative 기대집합(0-row CAS 제외·후퇴 방향만), FI-05c(resync 단계별 재사망) |
| P0-10-A | §2 ADOPT_MODIFIED(P0) | §2.3 'SCN 기반 가시성' system tick 3초 모델(idle = table commit 0, 커서 = tick 포함), FI-40 (b)(d) 양성 증거 정정 |
| P0-10-B | §2 ADOPT_MODIFIED(P0) | §5.1 (1) Outbox `occurrence item rejected(job_id, logical_scheduled_at)` = 설명된 누락 `ITEM_REJECTED`(건수 = SC-02b 주입, 그 외 0), SC-02b·§5 행·§6 2번 정합; A 9.1/16.4 payload |
| P0-10-C | §2 ADOPT_MODIFIED(P0) | §5.1 (2)(a)(b) `(job, lane)` 판정, lane 정의 (3)(b)와 공유, FI-20(f) 주석 |
| P0-10-D | §2 ADOPT_MODIFIED(P0) | §3.2 비교 A/B `STANDARD_HASH(canonical_concat, 'SHA256')` + canonicalization 규칙 + 양방향 PK 차집합(불일치 PK만 typed exact), §8.1 8번 (iii) 실 실행 |
| P0-10-E | §2 ADOPT_MODIFIED(P1) | §2.4 ledger actor `OPERATOR`·`published_main_snapshot_id`, FI-23c attempt 2 = chunk 1·2 재번호, FI-07 문구; DDL 부록은 기존 주차 1 게이트 |
| P1-01 | §3 ADOPT_MODIFIED | FI-35(e) submission in-flight·deterministic runId, FI-01(c) 문구 |
| P1-02 | §3 ADOPT | FI-22(a) RETRY 선행 순서 변형 |
| P1-03 | §3 ADOPT | FI-23c attempt 전이(FENCED 경유, `DQ_SEALED` 없음), §2.4 actor |
| P1-05 / 매트릭스 P1-09 | §3 ADOPT_MODIFIED | §5.1 (3)(a)·§3.2 비교 A main-ancestry 조건, §2.4 `published_main_snapshot_id`, H-06 변형(§3.5 6번) |
| P1-06 | §3 ADOPT_MODIFIED(P2) | SC-11 fixture 보강(§3.5 4번 — expire/orphan/branch ref/하한 422, §7 `orphan_min_age` 행과 일치) |
| P1-07 | §3 ADOPT_MODIFIED | FI-23 검사 1 식(`inserted + updated + deleted + ignored`·`dq_basis = APP_COUNTER` fallback)은 Phase 2 게이트 실행 시 v1.2.2 13.1 식을 적용(FI-23 행 자체는 변경 없음 — §3.1 #6 Merge-large 확인 항목에 식·`dq_basis` 명시) |
| P1-08 | §3 ADOPT_MODIFIED | FI-45d, §3.5 6번 DG G2 항목 'role 전환 시 기존 read-only 세션 종료 여부' |
| P1-09 | §3 ADOPT_MODIFIED | §2.1 `PASSWORD_ROLLOVER_TIME`; 상한식 항(최대 총 JDBC connection weight)은 v1.2.2 6.2·22장 9번이 권위 — FI-16(e)·§7 `credential_breaker_failures` 행은 그 식을 인용 |
| P1-11 | §3 ADOPT | FI-48 순서 재배열·캡처 전 old commit 주입 변형(§3.5 8번) |
| P1-14 | §3 ADOPT_MODIFIED | §2.5 backlog 5분해(`runnable` = non-terminal run 없음, `submitted_queued` 신설), SC-04b (a) 식 교체 |
| P1-15 | §3 ADOPT | FI-49 경합 단계를 H1 생성 전으로(§3.5 4번) |
| P1-04 | §3 P2 | §5 'freshness breach 알림 정확도' 행(`FINALIZED_NO_DATA` = orchestration freshness, `target unchanged` 경고·payload `target_unchanged`·publication age·coverage), §7 `target_unchanged_alert_count` 행, FI-21(d) `RETAIN_PREVIOUS` 3연속 변형 |
| P1-10·P1-12·P1-13 | §3 P2 | 이 문서 변경 없음(A v1.2.2의 attempt `connection_revision_id`, scheduler 단일성, `zero_gap_evidence`) — FI-35(e)가 P1-12의 단일 프로세스 전제를 공유하고, `db-identity:rotate` 뒤 `zero_gap_verified = false`는 §2.3 fixture 대조군(`ZERO_GAP_REQUIRES_VERIFIED_SOURCE` 422)이 이미 검출 |
| 매트릭스 P0-05·P1-22 | §4 PARTIAL | 위 P0-02·P0-10-E 행으로 폐쇄; versioned artifact 요구는 §8.1 8번(i)(ii)(iii)·§8.3 주차 1 게이트로 충족(추가 장치 없음) |

잔여(6차 밖): fence precursor 비교안 채택 여부(SC-02c → H-11, v1.2.x 입력), FI-48 실 실행(Phase 3 게이트), `freshness breach` payload의 `target_unchanged`·coverage 노출 검증(P1-04, A v1.2.2 read model 구현 뒤).

### 8.7 v1.2.3 반영 (Codex 3차·4차 리뷰 → 검토서 v3.1 §7)

검토서 v3.1은 **semantic errata**다 — 기능 추가 0, 새 상태·새 테이블·새 구성요소 0. 아래는 이번 개정이 닫은 항목과 이 문서(7차)의 반영 위치다.

| 검토서 v3.1 | 판정 | 이 문서의 변경 |
|---|---|---|
| §7.1 FI-50 5 timing case | 확인(P0) | §3.4 FI-50 전면 재작성((i)~(v), (iv)는 현행 (iii)가 재현 불가임을 증명하는 대조군), FI-14 문구 정정; **v1.2.3.1** — (i) 앵커를 'Guard 9번 응답 후 ∧ 첫 base `loadTable` 이전'으로 정정(A 10.2대로 base `loadTable`·`chunks:begin(1)`이 SA 생성보다 먼저라 옛 앵커는 도달 불가), (iv) 합격식을 'late A snapshot 0 ∧ B chunk 1 snapshot 1 ∧ `snapshots` 불변'으로 교체, 공통 양성 증거를 case별로 분리, (v) ≤1.10 분기에 Adjudication의 ledger·CAS·finalize 대행 1회를 합격 조건으로 추가, §2.2 훅 목록에 프록시 `hold_then_apply(release_anchor)` 명시 |
| §7.2 이중 commit oracle | 확인(P0) | §5.1 4-불변식 술어 — `snapshot_id` allowlist 철회, 하네스 주입 기록은 제외 목록이 아니라 **대조값** |
| §7.3 / P1-01 Dagster 제출 경로 | 확인(P0 상향) | **§3.3 SC-02d 신설**(tick→마지막 run 제출 end-to-end·`fetch_existing_runs` 구간·`num_submit_workers` 1/4/16), §8.1 8번 G0-4 |
| §7.4 보증 축 분리 | 확인(P1) | §3.2 비교 B '설명된 차이' 수치화(`delete_lag_slo_seconds`), §3.4 FI-44e (b) |
| §7.5 canonical hash | 확인(P0) | §3.2 hash 규격 전면 교체, §8.1 8번 G0-3(V-01~V-16 + `hash_participation = EXCLUDED` 목록) |
| §7.6 G0 정식화 | 확인 | **§8.1 8번 = G0 5항** + `g0_evidence` + 무효화 8항, §8.3 세 게이트(G0·G1·G2) 문단·주차 1 다섯 항 |
| §7.7 ZERO_GAP — ENFORCED 근거는 `SYNC_COMMIT_GUARD`뿐(2026-08-24 최종, 조건부 ENFORCED 이견 철회) | 확인(P0) | §3.4 **FI-41g**(`SYNC_COMMIT_GUARD` Source 전용 — bound 경계에서 초과 commit이 거부되어 Source row 미존재·이후 gap 0, C4 발동 0이 합격식)·**FI-41h 분리**(`TXN_AGE_KILL_JOB` Source의 kill miss는 `DETECT_AND_REPAIR` 안전망 시험이며 §8.3 G2 목록에서 제외)·**FI-41i 신설**(G0 무효화 전파 드릴), §8.3 G2 Oracle probe 4종 병합 |
| P1-02 / P1-13 operation 재개 | 부분 | §3.4 FI-24c (c) 포인터 변경 전후 crash 2지점, FI-02 (g) scheduler leader kill·인수 |
| P1-03 / P2-02 cross-type lease | 부분 | §3.4 FI-51 (iv) raw PostgreSQL session 직접 DML(권한 거부 / 트리거 `RAISE`) |
| P1-04 Guard 자동 Hold 경합 | 확인(P1) | §3.4 FI-42 (f) switchover 20-Guard 동시 도달 — deadlock 0, insert-or-get 1건, Source `FOR UPDATE`는 credential breaker 한정 |
| P1-06 Outbox transition key | 확인(P1) | §3.4 FI-06 (c) 같은 attempt 재결합 2회(`rebind_count = 2`, `event_id` 상이) |
| P1-10 credential fan-out | 부분 | §3.4 FI-16 (C) precheck 통과 뒤 rotation → executor fan-out(실패 로그인 ≤ Σ `numPartitions`, task retry 0, ORA-28000 0) |
| P1-11 fence provenance | 부분 | §3.4 FI-27 (d) ACTIVE 전이 직후 첫 Guard의 모니터 세션 endpoint |
| P1-12 ORA-01722 오분류 | 부분 | §3.4 FI-45d 데이터 유발 ORA-01722 대조군(Hold 0 · `SPARK_FAILED` 1) |
| P0-05 PER_CHUNK_FENCE sweep | 부분 | §3.4 FI-44e (b) 적재 중 hard delete 20건, (c) heartbeat 60초 지연 + sweep 하한(`T_lb_1`) 대조 |

잔여(7차 밖): §7.7 C1~C4의 상시 관측(`Q+L` 관측 최대)은 PoC 실측을 v1.2.3 22장 입력으로 올린 뒤 운영 단계에서 닫는다; private adapter 차선책은 SC-02d 결과가 선언 상한을 넘길 때에만 G0-4 왕복 3건과 함께 재개한다.
